"""
Shared types re-export module.

Centralizes cross-domain types so consumers import from a single
canonical path instead of reaching into other domains.

Usage:
    from apps.reference.shared.types import Bar, NormalizedRejectReasons
"""
