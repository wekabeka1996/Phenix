# BTCUSDT acknowledgement case review

- Parity: `conservative_mismatch`.
- Compatibility: `compatible_conservative`.
- Real acknowledgement file: absent.
- Effective acknowledgement: `unacknowledged`.
- Validation: `missing`.
- Operator id: absent.
- Runtime reason: `operator_acknowledgement_file_missing`.
- Review required: yes.

An example exact-state file is included only as a report sample and was never copied to the runtime path. Synthetic validation proves it would produce `acknowledged_conservative/valid` until its review expiry. It would not relax YAML or grant authority.
