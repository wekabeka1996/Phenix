## Conclusion

Unable to complete the task due to an environment issue preventing shell command execution. Specifically, the `Invoke-WebRequest` command failed with an error related to the `node-pty` package, which is essential for running shell commands. This prevents fetching metrics, reading log files, and performing other necessary shell operations.