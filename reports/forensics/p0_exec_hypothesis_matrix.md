# P0 Execution Split-Brain Hypothesis Matrix

| Hypothesis | Description | Verdict | Evidence |
| :--- | :--- | :--- | :--- |
| `H1: WS / ORDER UPDATE LOSS` | WebSocket or adapter path dropped the exit/bracket update. | `LIKELY` | No raw incident event proof was recovered. The runtime contains explicit polling fallback for missed fills/cancels in `apps/reference/domains/execution_position/watchdog.py:319-436`, so the scenario remains plausible but not confirmed. |
| `H2: FSM MISSED TRANSITION / EXIT MATCHER GAP` | Local execution received a valid exit signal but failed to classify or consume it. | `PROVEN` | Historical proof came from the pre-fix repro baseline; the repaired invariant now lives in `tests/domains/execution_position/test_split_brain_repro.py`. P1 observability hardening added explicit matcher telemetry in `apps/reference/domains/execution_position/fsm_manage.py` and validated it in `tests/domains/execution_position/test_execution_observability_hardening.py`, so match success/failure is now forensic-readable without changing business semantics. |
| `H3: OPEN-OVERWRITE / LOCAL OPEN GUARD MISSING` | `DecisionMaking` / orchestration can emit a new `CMD:OPEN` while local execution still tracks the old lifecycle. | `PROVEN` | Historical proof came from the pre-fix repro baseline; the repaired invariants now live in `tests/domains/execution_position/test_split_brain_repro.py`. P1 observability hardening added structured `EVT:EXECUTION_GUARD_BLOCKED` and `EVT:EXECUTION_DIVERGENCE_DETECTED` emission in `apps/reference/domains/execution_position/fsm.py`, validated by `tests/domains/execution_position/test_execution_observability_hardening.py`. |
| `H4: SPLIT-BRAIN CACHING / STALE BRACKET CARRY-OVER` | Old bracket state survives into the new lifecycle and suppresses fresh bracket placement. | `PROVEN (REFRAMED)` | Historical proof came from the pre-fix repro baseline; the repaired invariant now lives in `tests/domains/execution_position/test_split_brain_repro.py:170-207`. Important correction remains unchanged: the current code does not suppress via `_has_brackets()`; `_place_brackets()` clears phantoms in `apps/reference/domains/execution_position/fsm_manage.py:663-695`. The real suppressor was stale `TRACKING` routing, now guarded in `apps/reference/domains/execution_position/fsm_manage.py:593-609`. |
| `H5: ORPHAN / TTL CLEANUP` | Cleanup/tidy side effects are being mistaken for a valid close. | `PROVEN AS SYMPTOM, NOT ROOT CLOSE` | The invariant remains covered by `tests/domains/execution_position/test_split_brain_repro.py`. P1 observability hardening kept tidy as tidy-only, enriched `EVT:SYMBOL_TIDY` with `business_close_reconciled=false`, added `EVT:EXECUTION_TIDY_PERFORMED`, and separated business reconcile into `EVT:EXECUTION_CLOSE_RECONCILED` in `apps/reference/domains/execution_position/order_guardian.py`. |
| `H6: SAME-SYMBOL NEW LIFECYCLE OVER STALE EXECUTION STATE` | One symbol can continue with a new lifecycle while local execution still carries stale state from the old one. | `PROVEN` | Historical proof came from the pre-fix repro baseline. The repaired invariants now live in `tests/domains/execution_position/test_split_brain_repro.py:140-248`, and the corresponding fail-closed guards are in `apps/reference/domains/execution_position/fsm.py:1499-1537` plus `apps/reference/domains/execution_position/fsm_manage.py:593-609`. |

## Notes

- `H1` is intentionally downgraded from the previous `CONFIRMED` label. Without raw event proof, it must stay `LIKELY`.
- The old "`_has_brackets()` blocks new bracket placement" theory is stale for the current repo state. The reproduced suppress-path was stale `TRACKING` state, not `_has_brackets()`.
- Repair status as of `2026-03-13`: `H2`, `H3`, `H4`, and `H6` are now guarded by fail-closed code paths; `H5` remains intentionally symptom-only; `H1` remains `LIKELY` until raw event evidence is recovered.
- Observability status as of `2026-03-13`: guard blocks, divergences, matcher success/failure, tidy-only maintenance, and authoritative close reconcile now have dedicated structured execution events registered in `apps/reference/dictionaries/verb_registry_v1.yaml`.
