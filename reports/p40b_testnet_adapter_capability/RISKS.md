# P40B Risks

## FACTS
- No mainnet/live path was executed.
- No raw exchange client call was added.
- No order placement, cancel, modify, or close was executed.
- No YAML/business config was edited.
- No secrets were read or written.

## P0
- Real exchange execution remains unproven. P40B validates the fail-closed guard contract only.

## P1
- Runtime adapter descriptor production is not wired here. A later execution path must provide `AdapterCapability` from a trusted runtime/config source.
- Existing callers now need `AgentActionCommand.rationale`; tests in this focused surface were updated, but broader callers should be checked during integration.

## P2
- Compatibility alias preserves old `AdapterCapabilityDescriptor` imports, but downstream code should migrate to `AdapterCapability`.
- Local untracked `.agent_memory` files were generated by the first focused test run and intentionally left untouched under project rules.

## UNKNOWNS
- Whether the final P40 order proof branch will use this guard directly or adapt it through another gateway layer is not proven here.
