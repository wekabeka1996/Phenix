# Patch Diff

Implementation commit: `c300a5f8`.

- Extended dashboard Pydantic/YAML config with safe LAN settings and runtime-view paths.
- Parameterized Compose host exposure and startup script port/LAN behavior.
- Added `/arena`, `/arena/runtime`, Host/Origin boundary middleware, and registered control ingress.
- Added read-only runtime projection with strict evidence classification and no fallback state.
- Added dedicated responsive dual-agent UI and truth badges.
- Registered seven non-order control events before route wiring.
- Added focused LAN, uvicorn bind, runtime projection, stale heartbeat, ownership, kill-switch, badge, and no-raw-route tests.

No exchange adapter, strategy gate, memory core, FSM execution, authentication, or raw order route was changed.

