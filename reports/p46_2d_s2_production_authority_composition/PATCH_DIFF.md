# Patch Diff

This is a report-only fail-closed package.

Added:

- fifteen S2 forensic/validation reports;
- one coordination status report.

Not changed:

- application source;
- YAML/Pydantic business configuration;
- Cockpit source;
- FSM, adapter, exposure, sizing, provider, or exchange code.

Reason: no canonical context/lifecycle/snapshot reader exists to wire without creating prohibited duplicate authority or fixture fallback.
