# P11 recommendation

Collect additional completed ActionReview revisions across BTCUSDT/ETHUSDT and multiple horizons, then rebuild the same deterministic index. Add a scheduled read-only rebuild only after fresh-runtime validation.

Do not add embeddings, vector search, LLM summarization, automatic actions or execution authority.
