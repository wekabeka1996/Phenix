# P5 recommendation

Keep the next package read-only and narrow:

1. Add an execution-owner capability descriptor for exchange-filter cache population without order authority.
2. Expose explicit read-only reduce-only close/protect capability flags.
3. Separate account/portfolio observation from the minimal market-publication runtime if credential-free process composition is required.
4. Add a rendered six-card UI test when an in-app browser is available.

Do not add AgentIntent, dry-run execution gateways, action persistence, policy bypasses, strategy activation, or any order routing.
