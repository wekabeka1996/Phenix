# P3 recommendation

The smallest next package should remain read-only:

1. Add a runtime-owned, immutable readiness publication seam from initialized `ExecPosFSM` to the approved read host. It may be an in-memory provider when co-located or a bounded authenticated diagnostic transport when processes remain separate.
2. Attach the existing `ExchangeFilterCache` to the runtime owner and expose only presence/freshness/constraint metadata, never credential or mutable methods.
3. Add an explicit reduce-only capability/readiness flag owned by `CloseExecutor` rather than inferring it from class presence.
4. Repair the runtime snapshot publication path so fresh BTCUSDT/ETHUSDT market/features reach the read host.
5. Profile the 2.1-second bounded packet path and avoid increasing poll rate until measured.
6. Repeat UI observation when the in-app browser surface is available and perform a non-destructive live file rollover observation.

Do not add AgentIntent, dry-run execution, action review, order routing, policy disabling, council, ranking, reputation, auto-loop, provider arbitration, or testnet orders.
