# ACTIVE_POSITION_LIFECYCLE_CONTEXT_EVIDENCE_APPENDIX

| Claim | File path | Function / block | Exact runtime role | Evidence summary | Classification |
| --- | --- | --- | --- | --- | --- |
| Order state owner is `OrderIndex` | `apps/reference/domains/execution_position/domain_dict.json` | `state_ownership` | Declared SSOT split | Domain dictionary assigns order state to `OrderIndex` | runtime-proven |
| Position state owner is `ManageFlowFSM` | `apps/reference/domains/execution_position/domain_dict.json` | `state_ownership` | Declared SSOT split | Domain dictionary assigns position state to `ManageFlowFSM` | runtime-proven |
| EP maintains per-symbol manage and close flows | `apps/reference/domains/execution_position/fsm.py` | `__init__`, `_get_or_create_flows` | Runtime orchestration | `manage_flows` and `close_flows` dicts exist and are populated per symbol | runtime-proven |
| `ManageFlowFSM` owns local position/bracket fields | `apps/reference/domains/execution_position/fsm_manage.py` | class fields, `_on_fill()` | Position lifecycle truth | Code stores qty, entry price, side, timestamps, and bracket IDs on the FSM | runtime-proven |
| Bracket IDs alone on `FLAT` are not active lifecycle truth | `apps/reference/domains/execution_position/fsm_manage.py` | `has_active_lifecycle()` | Lifecycle predicate | Function ignores bracket-only metadata on `FLAT` | runtime-proven |
| False active lifecycle regression is covered | `tests/domains/execution_position/test_false_active_lifecycle_fix.py` | `test_flat_with_only_bracket_ids_is_not_active` | Regression proof | Test asserts FLAT + bracket IDs still returns false | test-proven |
| `CloseFlowFSM` no longer owns autonomous close logic | `apps/reference/domains/execution_position/fsm_close.py` | `_check_close_conditions()` | Explicit close soldier | Method returns `None`; comments say autonomous close removed | runtime-proven |
| `CMD:CLOSE` still emits `DEC:CLOSE` | `tests/domains/execution_position/test_close_flow_cmd_close_always_emits_dec_close.py` | full test | Explicit close contract | Test proves close emission behavior | test-proven |
| Reduce-only intents route to `CMD:CLOSE` | `apps/reference/domains/execution_position/intent_router.py` | `on_trade_intent_proposed()` | Execution boundary routing | `reduce_only` branch builds `CMD:CLOSE` | runtime-proven |
| Entry fill activates position and bracket lifecycle | `apps/reference/domains/execution_position/fsm_manage.py` | `_on_fill()`, `_place_brackets()` | Open-position activation | First fill initializes position and places brackets | runtime-proven |
| TP1 partial exit is live | `apps/reference/domains/execution_position/fsm_manage.py` | `_handle_bracket_fill()` | Partial reduce path | TP1 reduces qty and preserves runner logic | runtime-proven |
| TP2 / legacy TP full exit is live | `apps/reference/domains/execution_position/fsm_manage.py` | `_handle_bracket_fill()` | Full close path | TP2 and legacy TP clear lifecycle and flatten state | runtime-proven |
| SL close is live | `apps/reference/domains/execution_position/fsm_manage.py` | `_handle_bracket_fill()` | Protective exit | SL fill clears lifecycle and cancels TP | runtime-proven |
| Exchange-side trailing stop replacement is live | `apps/reference/domains/execution_position/fsm_manage.py` | `_check_trailing_stop()`, `_adjust_trailing_stop()` | Dynamic SL mutation | Code cancels old SL and places new SL | runtime-proven |
| Break-even after TP1 is not live | `apps/reference/domains/execution_position/fsm_manage.py` | TODO in `_handle_bracket_fill()` | Absent behavior | Comment exists, runtime branch does not | runtime-proven that absent |
| Max-hold close is in `ManageFlowFSM` | `apps/reference/domains/execution_position/fsm_manage.py` | `_check_max_hold_time()` | Position lifetime timer | Emits reduce-only `DEC:CLOSE` | runtime-proven |
| Max-hold close is routed and executed | `tests/domains/execution_position/test_execpos_max_hold_close_routed_v1.py`, `tests/domains/execution_position/test_execpos_max_hold_close_executes_adapter_v1.py` | full tests | Runtime verification | Tests prove routing and adapter execution | test-proven |
| `CloseExecutor` owns imperative close execution | `apps/reference/domains/execution_position/close_executor.py` | `execute_close()` | Close executor | Performs partial/full close, teardown, and reconcile | runtime-proven |
| Duplicate-close suppression exists | `apps/reference/domains/execution_position/fsm.py` | close handling in `handle()` and `_execute_decision()` | Hardening | EP can suppress duplicate close propagation/execution | runtime-proven |
| Tidy is explicitly not business close | `apps/reference/domains/execution_position/order_guardian.py` | `_emit_tidy_events()` | Cleanup telemetry | Emits `business_close_reconciled=false` | runtime-proven |
| Close reconcile is separate from tidy | `apps/reference/domains/execution_position/order_guardian.py` | `reconcile_symbol()` | Business-close confirmation | Emits `EVT:EXECUTION_CLOSE_RECONCILED` only on authoritative reconcile | runtime-proven |
| Tidy vs close-reconcile split is tested | `tests/domains/execution_position/test_execution_observability_hardening.py` | two close/tidy tests | Observability correctness | Tests prove tidy-only and business-close semantics are distinct | test-proven |
| Order timeout is order-centric | `apps/reference/domains/execution_position/entry_manager.py` | `handle_order_timeout()` | Timed-out order handling | Cancels timed-out order and emits `EVT:ORDER_TIMEOUT` | runtime-proven |
| Limit timeout is order-centric | `apps/reference/domains/execution_position/limit_order_monitor.py` | `_cancel_expired_order()` | Limit-order expiry | Cancels expired LIMIT and emits `EVT:LIMIT_ORDER_TIMEOUT` | runtime-proven |
| Watchdog has separate ACK/FILL timers | `apps/reference/domains/execution_position/watchdog.py` | `OrderTimeoutType`, `track_order_placed()`, `on_order_ack()` | Order-timeout monitoring | Separate ACK and FILL deadlines are maintained | runtime-proven |
| Watchdog can REST-recover missed fills | `apps/reference/domains/execution_position/watchdog.py` | `_emit_recovered_trade_executed()` | Recovery path | Emits canonical `EVT:TRADE_EXECUTED` on REST-recovered fill | runtime-proven |
| Pending entries can be cancelled on regime change | `apps/reference/domains/execution_position/event_handlers.py` | `on_regime_detected()` | Pending-entry stale cancel | Regime handler may cancel pending entries with `CANCEL_STALE_REGIME` | runtime-proven |
| Decision-side regime-flip close is live | `tests/decision_making/test_regime_flip_close_path_p0.py` | full file | Strategy-side close trigger | Tests prove reduce-only close request path | test-proven |
| Aurora runs `ExitManager` while a position is open | `apps/reference/domains/decision_making/aurora_decision.py` | `check_exit()` call site | Strategy-side post-entry evaluation | `exit_manager.check_exit()` is called when `current_position_side != ""` | runtime-proven |
| `ExitManager` evaluates danger-zone, trailing, time exit, and signal reversal | `apps/reference/domains/decision_making/exit_manager.py` | `check_exit()` | Strategy-side policy helper | Helper returns exit/tighten outcomes for open positions | runtime-proven |
| End-to-end execution effect of every `ExitManager` branch is not fully traced here | no single proving path found | n/a | Missing proof | Evaluation is proven, universal downstream close effect is not | unproven |
| `EVT:FEATURES_CALCULATED` exposes microstructure and motion fields | `apps/reference/domains/feature_engineering/schemas/features_calculated_v1.json` | schema | Sidecar-readable feature contract | Schema carries OBI, TFI, delta_price, liquidity_kappa, absorption, and price_motion | runtime-proven |
| Shared bar contract still treats `absorption` as placeholder | `apps/reference/domains/feature_engineering/schemas/features_calculated_v1.json` | `features.absorption` | Signal quality note | Description says placeholder / always `0.0` | runtime-proven |
| OBI and TFI already affect runtime elsewhere | `reports/OBI_TFI_RUNTIME_INFLUENCE_RESEARCH.md` | consumer map | Existing evidence packet | Report traces live consumers in risk scoring, mean-reversion veto, and Aurora entry plan | doc-only |
| `EXECUTION_CLOSE_RECONCILED` schema marks true business close | `apps/reference/domains/execution_position/schemas/execution_close_reconciled_v1.json` | schema | Reconcile contract | Schema requires `business_close_reconciled=true` | runtime-proven |
| `EXIT_MATCH_ATTEMPTED` / `FAILED` are rich forensic contracts | `apps/reference/domains/execution_position/schemas/exit_match_attempted_v1.json`, `apps/reference/domains/execution_position/schemas/exit_match_failed_v1.json` | schemas | Exit attribution | Schemas include incoming IDs, local expected IDs, reasons, and state snapshots | runtime-proven |
| Watchdog `check_interval_ms` and `rps_limit` are not wired from FSM boundary | `REPORT.md`, `apps/reference/domains/execution_position/fsm.py`, `apps/reference/domains/execution_position/watchdog.py` | timing census + constructor path | Config/runtime drift | Existing repo audit proves only ACK/FILL TTLs are threaded through from EP | doc-only + runtime-aligned |
