

---

# Задание для Copilot (RU)

**TASK_ID:** `FSMP-P0-T02`
**Title:** Завершить P0 в `vfoundation-reference`: /metrics, idempotency TTL, WAL integrity, правки core/router/CB
**Branch:** `fix/p0-delta-metrics-idem-wal`

## Цели (DoD/Gates)

1. Добавить `/metrics` и учёт p95(router), timeout_rate, queue_depth.
2. Ра� ширить `/debug/{rid}`: вернуть `why_chain`, `integrity_ok`, `merkle_root`, `count`.
3. `/replay/{rid}`: dry-run + `integrity_ok:true`, `replayed:N`.
4. Idempotency: вве� ти `idempotent_key` + TTL-кеш (по умолчанию 10 мин). Повтор �  тем же ключом — без запи� и в WAL, ответ `dedup:true`.
5. И� править заглушки/ошибки в `core/routing.py`, `core/retry_cb.py` (никаких `...`, валидная логика CB.allow, ERR-ветки).
6. Валидация `why` (≤80 � имволов) уже е� ть — убедить� я, что она реально задей� твована на входе роутера; при нарушении → 400 и **не** пи� ать в WAL.
7. Обновить те� ты: покрытие ≥ 90%, e2e round-trip PASS, RBAC 403 для `/debug`/`/replay` без `Authorization`.

## Ограничения

* Contract-first, additive-only, fail-closed. Не ломаем � уще� твующие контракты; только добавляем.
* Лёгкий XAI: только `why` + `why_chain`; тяжёлые поя� нения и� ключить из рантайма.
* RBAC о� тавляем: `require_admin()` для `/debug`/`/replay`.

## Изменения по файлам (точечно)

```
vfoundation/obs/debug_api.py
  + GET /metrics → {router_p95_ms, timeout_rate, queue_depth}
  ~ GET /debug/{rid} → добавить why_chain, integrity_ok, merkle_root
  ~ GET /replay/{rid} → добавить integrity_ok, replayed

vfoundation/core/routing.py
  ~ Завершить метод route(): убрать "..." и ERR-ветку допи� ать как VALID enum
  ~ Перед вызовом handler валидировать msg (why<=80), ttl/expiry; on invalid → 400/ERR без WAL

vfoundation/core/retry_cb.py
  ~ Допи� ать CircuitBreaker.allow(): CLOSED→true; HALF_OPEN→первый запро� ; OPEN→пока cool_down не и� тёк
  ~ Убрать "..." и завершить метод allow() корректно

vfoundation/core/idempotency.py
  ~ Заменить ключ на idempotent_key (string) + TTLCache
  + методы: seen(key), remember(key, result, ttl_ms), get(key)

vfoundation/dr/wal.py
  ~ В append(): о� тавить prev_hash/_hash как е� ть
  + В� пом. функция verify_chain(records)->bool (� верка _prev/_hash)
  + Опционально: � читать и возвращать merkle_root по ма� � иву _hash

vfoundation/dr/replay.py
  ~ replay_for_rid: � обрать events, вернуть (events, integrity_ok, merkle_root)

vfoundation/obs/why.py
  ~ ensure append_why(chain, msg.why) и� пользует� я в debug_api для why_chain

tests/
  + test_metrics_smoke.py: 200x /health → p95(router) ≤ 10м�  (локально), /metrics shape OK
  + test_debug_replay_integrity.py: � оздать RID → проверить why_chain, integrity_ok, merkle_root
  + test_idempotency_ttl.py: два POST �  одинаковым idempotent_key в пределах TTL → одна запи� ь в WAL, второй ответ dedup:true
  ~ test_routing_idempotency.py: адаптировать под idempotent_key
  ~ test_wal_replay.py: добавить проверку integrity_ok
```

## Контракты/форматы (добавка к � уще� твующим)

* **/metrics**:

  ```json
  { "router_p95_ms": <number>, "timeout_rate": <number>, "queue_depth": <integer> }
  ```
* **/debug/{rid}**:

  ```json
  { "rid": "<rid>", "count": <int>, "events": [ ... ], "why_chain": ["..."], "integrity_ok": true, "merkle_root": "<hex>" }
  ```
* **/replay/{rid}**:

  ```json
  { "rid":"<rid>", "replayed": <int>, "integrity_ok": true }
  ```
* **Idempotency**: входящее � обытие � одержит `idempotent_key` (string). Повтор по ключу → `{"dedup": true}`.

## CI/покрытие

* `pytest -q --cov=vfoundation --cov-report=term-missing`
* `ruff`, `black --check`, `mypy` (� трогий на новые модули)
* Цель покрытия ≥ 90%; падение — блокирующий Gate.

## Что положить в PR

1. Спи� ок правок по файлам (как выше).
2. Скрин/лог: e2e round-trip PASS; `/metrics` shape; RBAC 403; coverage ≥ 90%.
3. Обновить `ops/COPILOT_PROGRESS.md` (по� тавить галочки P0) и `JOURNAL.md`:

   ```
   RID=FSMP-P0-T02
   Why=finish P0 delta (metrics+idempotency+WAL-integrity)
   ```

## Коммит

`fix(p0): metrics/idempotency TTL/wal integrity + router/cb finalize [FSMP-P0-T02]`

---


---

# Задание для Copilot (RU) — 1/2

**TASK_ID:** `FSMP-P0-T03`
**Title:** Конкурентобезопа� ный WAL и атомарная идемпотентно� ть (locks + CAS)
**Branch:** `fix/p0-wal-and-idem-locks`

## Цели (DoD/Gates)

1. **WAL:** у� транить race при append: един� твенная корректная цепочка `prev_hash→hash` даже при N конкурентных запи� ей (мультипоточно/мультипроце� � но).
2. **Idempotency:** «single-flight» на `idempotent_key`: для конкурентных запро� ов �  одним ключом выполняет� я ровно один handler, прочие получают `dedup:true`/`inflight:true` без побочного эффекта.
3. **Fail-closed:** при невозможно� ти захватить необходимые замки ≤ `lock_timeout_ms` → возврат 503/429, **без запи� и** в WAL.
4. **Те� ты:** во� произве� ти гонки и подтвердить от� ут� твие нарушений цепочки и дублей; покрытие ≥ 90%.

## Ограничения

* Contract-first: интерфей� ы публичных эндпоинтов не ломаем; добавления — назад � овме� тимы.
* Additive-only: новый � лой � инхронизации/замков и CAS-проверок; � уще� твующую бизне� -логику не изменять.
* Лёгкий XAI: фик� ируем короткие `why` для путей «LOCK_ACQUIRED / LOCK_TIMEOUT / IDEM_DEDUP / IDEM_INFLIGHT».

## Изменения по файлам (точечно)

```
vfoundation/dr/wal.py
  + WALFileLock: кро� � -платформенная эк� клюзивная блокировка файла (fcntl|msvcrt; аб� тракция внутри).
  ~ append(...):
      - Захватить file lock (LOCK_EX).
      - Прочитать хво� т → вычи� лить prev_hash.
      - Сформировать запи� ь, по� читать hash.
      - Атомарная запи� ь (файл открыт �  O_APPEND; по� ле запи� и при необходимо� ти fsync).
      - О� вободить lock.
  + append_cas(expected_prev_hash?): е� ли задан и не � овпал �  фактиче� ким хво� том → вернуть CONFLICT (409) без запи� и.
  + verify_chain(records)->bool: и� пользовать при /debug и /replay.
  + метрики: � четчик lock_contention, lock_wait_ms (для /metrics).

vfoundation/core/idempotency.py
  + KeyedLocks: «single-flight» на ключ (внутрипроце� � ные локи по idempotent_key; �  TTL-о� вобождением).
  + Inflight registry: � о� тояния {pending, done(result)} c TTL.
  ~ API:
      - begin(key) -> {"acquired":true}|{"inflight":true}
      - complete(key, result)  # � охраняет результат для по� ледующих "догоняющих" запро� ов в пределах TTL
      - get_if_done(key) -> Optional[result]
      - remember_dedup(key)  # � тарое поведение для уже завершенных

vfoundation/core/routing.py
  ~ Порядок:
      1) Проверить валидно� ть контракта (why≤80, TTL ok) — иначе 400, без WAL.
      2) Идемпотентно� ть:
         - е� ли get_if_done(key) е� ть → вернуть cached result (dedup:true).
         - иначе begin(key): е� ли inflight → вернуть 202/209 �  {"inflight":true} (без WAL).
         - е� ли acquired → переходим к обработке.
      3) При у� пешном handler: запи� ать в WAL; complete(key, result); вернуть 200.
      4) При и� ключении: complete(key, error as ERR); возврат 500/4xx.
  + Логи/метрики: idem_acquired, idem_inflight, idem_dedup.

vfoundation/obs/debug_api.py
  ~ /debug/{rid}, /replay/{rid}: включить verify_chain() и поля integrity_ok (true/false).

docs/ADR-004-WAL-Concurrency.md  (новый)
  - Решение: file-lock + O_APPEND; опциональный CAS; причины отказа от «беззамковых» � тратегий при мультипроце� � е.
```

## Алгоритмиче� кие детали (без кода)

* **WAL file-lock:**

  * На время «прочитать хво� т → запи� ать новую запи� ь» держать **эк� клюзивный** замок файла.
  * На Unix — `fcntl.flock(LOCK_EX)`, на Windows — `msvcrt.locking()`. Обернуть в единый helper.
  * Открывать файл �  `O_APPEND`; это гарантирует атомарно� ть «� мещения позиция→запи� ь» для одного проце� � а; но чтение хво� та без lock недопу� тимо → поэтому lock обязателен.
  * По желанию: `append_cas(expected_prev_hash)` для оптими� тиче� кой � хемы (в будущем).

* **Idempotency single-flight:**

  * Первому конкурентному запро� у по ключу — `acquired:true` (он выполняет handler).
  * О� тальные получают `inflight:true` (можно 202, но в нашем P0 до� таточно `200{"inflight":true}` либо `209` е� ли принято).
  * По завершении первый кладёт результат в кэш (TTL) — догоняющие читают готовый результат как `dedup:true`.
  * Межпроце� � ная идемпотентно� ть в P0 не вводит� я (наш Router — single worker по умолчанию). Для multi-worker добавим Redis-lock в P1/P2.

## Те� т-план (ча� ть 1, в этом PR)

* **WAL race (threaded):** 100 конкурентных append → `verify_chain=True`, длина N, ни одного «выпавшего» звена.
* **WAL race (multiprocess, optional):** 8 проце� � ов × 100 append → цепь валидна (Linux CI — обязательный; Windows — best-effort).
* **Idem single-flight:** 50 потоков �  одинаковым `idempotent_key` → 1 вызов handler; о� тальные `inflight:true` или получают тот же cached result; в WAL — ровно 1 запи� ь на ключ.
* **Fail-closed:** и� ку� � твенно замедлить удержание lock → ча� ть вызовов получает 503 (lock timeout), и в WAL нет лишних запи� ей.
* **Метрики:** lock_contention > 0 под � тре� � ом; timeout_rate ≤ 1% в smoke.

## Метрики/перф

* Цель: удержание file-lock ≤ 2–5 м�  на обычных запи� ях.
* p95(router) не ухудшить > +2 м�  отно� ительно дофик� ов (� м. /metrics).

## Что положить в PR

* ADR-004, � пи� ок изменений, логи � тре� � -те� тов, coverage ≥90%, обновлённые чек-бок� ы P0, запи� ь в JOURNAL (`FSMP-P0-T03`).

**Коммит:** `fix(p0): WAL file-lock + single-flight idempotency; integrity checks [FSMP-P0-T03]`

---

# Задание для Copilot (RU) — 2/2

**TASK_ID:** `FSMP-P0-T04`
**Title:** Monotonic TTL + фоновая очи� тка (janitor) для идемпотентно� ти
**Branch:** `fix/p0-idem-monotonic-janitor`

## Цели (DoD/Gates)

1. Перейти �  `time.time()` на `time.monotonic()`/`monotonic_ns` для в� ех ра� чётов TTL/временных интервалов.
2. Добавить **background janitor**: периодиче� кая очи� тка и� тёкших запи� ей (`cleanup_expired`) без блокировок роутера.
3. Экономия памяти подтверждена те� том под длительной нагрузкой (без утечек).

## Изменения по файлам

```
vfoundation/core/idempotency.py
  ~ TTL: хранить expiry в монотонных единицах (ns или ms).
  + Janitor: фоновая нить/та� к �  периодом (например, 60� ), безопа� но вызывает cleanup_expired().
  + Параметры: idem_ttl_ms, janitor_interval_ms, верхние лимиты на размер кэша (evict policy = LRU при overflow).
vfoundation/obs/debug_api.py
  + /metrics: добавить idem_cache_size, janitor_runs, janitor_evicted.
docs/Operations.md
  + Раздел: об� луживание Idempotency Store (мониторинг, тюн).
```

## Те� т-план (ча� ть 2)

* **Monotonic correctness:** � ме� тить � и� темные ча� ы назад/вперёд (мокаем time) → TTL не ломает� я.
* **Janitor under load:** 10^5 уникальных ключей �  TTL=1–3�  → по� ле 2 минут память � табильна; /metrics отражает evicted > 0.
* **LRU cap:** при до� тижении max_size → прои� ходят ожидаемые эвикции, без и� ключений.

**Коммит:** `fix(p0): monotonic TTL + janitor for idempotency; metrics and docs [FSMP-P0-T04]`

---

## Единый QA-� тре� -план (привязать к обоим PR)

**RID:** `FSMP-P0-QA01`
**Цель:** доказать от� ут� твие гонок и утечек; подтвердить цело� тно� ть WAL и «single-flight» в у� ловиях конкурентного до� тупа.

**Нагрузочные � ценарии:**

1. 1K/5K конкурентных append в WAL (threads→processes).
2. 1K запро� ов �  одинаковым `idempotent_key` за 1 � екунду.
3. 10^5 уникальних ключів за 2 хвилини (перевірка janitor).
4. Отказоу� тойчиво� ть: и� ку� � твенно увеличить lock contention → проверяем fail-closed (503/429), без «грязных» запи� ей.

**Критерии PASS:**

* `verify_chain=True` для в� ех � ерий; `integrity_ok:true` в `/debug`/`/replay`.
* На ключ — мак� имум **1 запи� ь** в WAL; в� е о� тальные — `inflight:true` или `dedup:true`.
* Память � табільна (±10%) пі� ля janitor 2 хв; немає безконтрольного ро� ту.
* p95(router) не погіршив� я більше ніж на +2м�  vs дофик� ов.

---

## Журнали/документи

**JOURNAL.md (додати рядки):**

```
RID: FSMP-P0-T03
Actor: copilot
Why: WAL lock + single-flight idempotency
Links: PR#..., ADR-004

RID: FSMP-P0-T04
Actor: copilot
Why: monotonic TTL + janitor
Links: PR#..., /metrics updates
```

**ADR:** `docs/ADR-004-WAL-Concurrency.md` — рішення, альтернативи (single-writer очередь, Redis-lock), обґрунтування вибору file-lock + single-flight для P0.

---

### Коротке XAI-логування (WHY-коди для тра� ування)

* `WAL_LOCK_ACQUIRED`, `WAL_LOCK_TIMEOUT`, `WAL_APPEND_DONE`, `WAL_VERIFY_FAIL`
* `IDEM_BEGIN`, `IDEM_INFLIGHT`, `IDEM_RESULT_CACHED`, `IDEM_DEDUP`
* `TTL_MONOTONIC`, `JANITOR_EVICT`



---

# Задание для Copilot (RU) — HOTFIX Pydantic V2

**TASK_ID:** `FSMP-P1-HOTFIX-PYD-001`
**Title:** Миграция `@validator` → `@field_validator` и ввод кро� � -полейной проверки для `execution_position/contracts.py`
**Branch:** `fix/p1-pydantic-v2-validators`

## Цели (DoD/Gates)

1. Заменить V1-� тиль `@validator` на V2-� тиль `@field_validator` в `apps/reference/domains/execution_position/contracts.py`.
2. Е� ли е� ть межполевые инварианты (напр., `qty*price ≥ min_notional`, � огла� ованно� ть `price` �  шагом цены, `qty` �  шагом лота) — выне� ти их в `@model_validator(mode="after")`.
3. Не и� пользовать `float` в нормализации — только `Decimal` �  правильным `quantize` по шагам (price_step, qty_step) из � ловника/конфіга.
4. MyPy clean, те� ты зелёные, покрытие ≥ 90% по изменённым файлам.
5. Предотвратить регре� � : включить жё� ткий gate на депрекейты Pydantic V1 у `pytest.ini`.

## Детали миграции (без кода)

* Импорт: `from pydantic import field_validator, model_validator, BaseModel` и `from pydantic import ConfigDict` (е� ли и� пользует� я конфиг).
* Был `@validator("qty")` → � танет `@field_validator("qty", mode="after")` (е� ли нормализация значения по� ле пар� инга) или `mode="before"` (е� ли пар� инг � трок/преобразование типов до).
* Был `@validator("price")` → аналогично `@field_validator("price", mode="after")`.
* Межполевые проверки (зави� ящие от `symbol`/`price`/`qty` одновременно) выне� ти в один `@model_validator(mode="after")`.
* Конфиг модели для Pydantic v2: при необходимо� ти задать `model_config = ConfigDict(validate_assignment=True, str_strip_whitespace=True)`.

## Политика шагов и квантования

* `qty`: приве� ти к `Decimal`, квантовать по `qty_step` (наприклад, 0.001).
* `price`: `Decimal`, квантовать по `price_step` (наприклад, 0.01).
* Проверить границы: `min_qty`, `min_notional` (через `qty*price`).
* Любая коррекция должна � опровождать� я коротким `why` (≤80) в DEC/CMD на уровне FSM при формировании решения.

## Те� т-план

Добавить/обновить те� ти (новий файл можна `tests/test_execpos_contracts_pydantic_v2.py`):

1. **Validator on `qty`:** � троки/float → `Decimal`, корректный `quantize` до `qty_step`.
2. **Validator on `price`:** аналогично; проверка округления вверх/вниз по политике (указать в � ловнике правило — обычно вниз для лимит-ордера BUY, вверх для SELL — поки в shadow можна фік� увати “round_to_step, mode='nearest'” для детермінізму).
3. **Cross-field:** `qty*price < min_notional` → ValidationError; на корректном наборе проходить.
4. **Edge cases:** пу� тые/нулевые значения, очень длинные � троки, NaN/Inf (отклоняют� я).
5. **Coverage:** по `contracts.py` ≥ 90%.

## CI/Gates

* В `pytest.ini` добавить фильтр предупреждений:

  ```
  [pytest]
  filterwarnings =
      error::pydantic.warnings.PydanticDeprecatedSince20
  ```

  (Е� ли модульная ма� ка отличает� я — и� пользовать эквивалентное «error::PydanticDeprecatedSince20».)
* Запу� к: `pytest -q --cov=...` — зеленый, без депрекейт-ворнингов.

## Документация

* В `docs/CLI.md`/`docs/Schemas.md` коротко: “Модельні валідатори — Pydantic v2: `@field_validator`, кро� � польова валідація — `@model_validator`”.
* `JOURNAL.md` добавити:

  ```
  RID: FSMP-P1-HOTFIX-PYD-001
  Actor: copilot
  Why: migrate validators to Pydantic v2; add cross-field checks
  ```

**Коммит:** `fix(execpos): migrate to @field_validator + cross-field model_validator; enable Pydantic v2 warn gate [FSMP-P1-HOTFIX-PYD-001]`

---

Прийнято. Які� на робота: ядро **drift monitor** готове, те� ти й продуктивні� ть — ок. Але за нашим DoD для T03 бракує двох вбудувань, тому формально це ще **NO-GO** до мержа.

# Що ще потрібно для PASS T03 (мінімально)

## Задание для Copilot — FSMP-P1-T03-GATE

**Branch:** `feat/p1-drift-monitor` → `chore/p1-t03-gate-wire`

**Цілі (ровно 2):**

1. **/metrics wiring:** під’єднати `aggregate_drift_metrics()` і додати поля:

   * `confusion_tp_total`, `confusion_fp_total`, `confusion_fn_total`, `confusion_tn_total`
   * `drift_pct_last`, `accuracy_last`
   * Те� т: `/metrics` мі� тить ці ключі та валідні значення.
2. **/debug/{rid} wiring (RBAC):** додати � екцію `drift_report`:

   * `{confusion:{tp,fp,fn,tn}, mismatches:[…≤5], computed_at, records_processed}`
   * Те� т: `/debug/{rid}` повертає `drift_report` для e2e кей� а.

**Обмеження:** жодних нових ендпоінтів, тільки розширення і� нуючих; без алертів/� трімінга.
**DoD:** 100% те� ти по новому; repo coverage ≥90%; mypy clean.
**Commit:** `chore(p1-t03): wire drift to /metrics and /debug; tests [FSMP-P1-T03-GATE]`

Пі� ля цього — **GO/PASS T03**.

---

# Відповідь щодо «захардкожених» параметрів

Ти правий: для P1 нам до� татньо **мінімального конфігу через ENV** (� екрети та критичні «ручки»), без фреймворків.

## Задание — FSMP-P1-T04 (Minimal Config)

**Branch:** `feat/p1-config-minimal`

**Scope (рівно це):**

* Створити `vfoundation/config.py` (ENV-змінні з безпечними дефолтами):

  * `RBAC_ADMIN_TOKENS`, `SIGNING_KEY`
  * `WAL_DIR`, `WAL_LOCK_TIMEOUT_SEC`
  * `CB_THRESHOLD`, `CB_COOLDOWN_SEC`
  * `IDEM_TTL_MS`, `IDEM_MAX_ENTRIES`
  * `DRIFT_TIME_WINDOW_SEC`
* Підключити у відповідних модулях (rbac, wal, retry_cb, idempotency, drift).
* Те� ти: `test_config_env_overrides.py` (override через `monkeypatch`), без зміни поточної поведінки за замовчуванням.
* Док: короткий `ADR-005-Config.md` (чому ENV, що � аме вине� ено).

**Commit:** `feat(config): minimal ENV config for secrets + critical knobs [FSMP-P1-T04]`
**DoD:** те� ти зелені, coverage ≥90%, дефолти «fail-obvious» у dev.

---

## Під� умок

* **T03:** core готовий, треба лише підв’язати `/metrics` і `/debug` (T03-GATE).
* **Config:** вино� имо SECRET/knobs у ENV як окремий T04 пі� ля мержа T03-GATE.

Коли зробите T03-GATE — дам офіційний PASS і � тартуємо T04.


Задание для Copilot — FSMP-P1-T04 (Minimal Config)

TASK_ID: FSMP-P1-T04
Branch: feat/p1-config-minimal

Ціль (рівно одна)

Вине� ти � екрети та критичні “ручки” в ENV через один модуль конфігурації, без зміни бізне� -логіки FSM.

Scope (� трого мінімум)

Створити vfoundation/config.py з ENV-параметрами:

RBAC_ADMIN_TOKENS, SIGNING_KEY

WAL_DIR, WAL_LOCK_TIMEOUT_SEC

CB_THRESHOLD, CB_COOLDOWN_SEC

IDEM_TTL_MS, IDEM_MAX_ENTRIES

DRIFT_TIME_WINDOW_SEC

Підключити у модулях:

vfoundation/security/rbac_abac.py (RBAC токени)

vfoundation/dr/wal.py (WAL_DIR, lock timeout)

vfoundation/core/retry_cb.py (CB thresholds)

vfoundation/core/idempotency.py (TTL/entries)

apps/reference/domains/execution_position/drift_monitor.py (time window)

Додати про� ту валідацію конфіга (fail-obvious dev дефолти: попередження, не падати).

STOP-� коп

Жодних live-reload/фреймворків/ YAML/JSON.

Бізне� -параметри FSM (trail/breakeven/time_stop) не вино� имо.

Жодних нових ендпоінтів, лише внутрішнє викори� тання.

Те� ти

tests/test_config_env_overrides.py:

monkeypatch ENV → перевірити, що значення підхопили� ь у відповідних мі� цях.

Негативні/дефолтні значення (без ENV) → дефолти активні, попередження показані.

Перевірка, що поведінка � и� теми без ENV не змінена (і� нуючі те� ти залишають� я зеленими).

Документація

docs/ADR-005-Config.md — коротко:

Що вине� ли в ENV і чому.

Спи� ок змінних та дефолти (позначити dev-only).

Обмеження (немає live reload).

DoD / Gates

В� і те� ти зелені; repo coverage ≥90%, mypy clean.

У коді немає dev-токенів/ключів за замовчуванням, лише fail-obvious dev-значення через ENV.

/metrics, /debug, WAL/Idempotency — без регре� ій.

Коміт:
feat(config): minimal ENV config for secrets + critical knobs [FSMP-P1-T04]

JOURNAL.md

RID: FSMP-P1-T04
Actor: copilot
Why: minimal ENV config for secrets & critical knobs

Прийнято. **FSMP-P1-T05 — PASS.** CLI-shadow (replay/drift) виконано акуратно: 11/11 те� тів, нові команди покриті на 100%, офф-патч — ок.

Жодних git-дій з мого боку — ти мерджиш � ам.
І так, **нагадую: пі� ля кожного та� ку оновлюй `TODO.md`.**

---

# Задание для Copilot (RU) — FSMP-P1-T06 (CI/QA — P1 Gate)

**ВАЖЛИВО:** жодних git-операцій з мого боку.
**НАГАДУВАННЯ:** по завершенні — **оновити `TODO.md`** і **JOURNAL.md**.

**TASK_ID:** `FSMP-P1-T06`
**Branch:** `chore/p1-ci-qa-gates` (� твориш � ам)

## Цель

Закріпити **Gate P1** у CI: � табільні перевірки яко� ті/безпеки/покриття для в� ієї гілки P1.

## DoD / Gates (жор� тко)

1. **CI (GitHub Actions) мінімальний пайплайн**:

   * `lint`: ruff (без автофік� ів), **0 помилок → PASS**
   * `type`: mypy **clean**
   * `test`: pytest з **порогом coverage ≥90%** (fall-under → RED)
   * Артефакти: `coverage.xml`, `htmlcov` (upload)
   * Кешування pip/venv (мінімально)
2. **Smoke e2e** (pytest, не окремий � ерві� ):

   * `/health` 200
   * `/metrics` мі� тить ключі drift і ACL/FSM лічильників (перевірка presence, не значення)
   * `/debug/{rid}` під RBAC: 403 без токена, 200 з валідним; якщо є drift-report — при� утній блок `drift_report`
3. **Контракти без регре� ій**:

   * WHY ≤ 80 (негативний кей�  дає 400 без WAL)
   * Ідемпотентні� ть: 50 паралельних `CMD:OPEN` → 1 DEC, інші `inflight|dedup` (� корочений те� т)
4. **Збірка коле� а**:

   * `python -m build` (wheel + sdist) — у� пішно, без помилок імпортів

## STOP-� коп

* Ніяких деплоїв/канарей/алертів.
* Без нових endpoint’ів.
* Жодних змін бізне� -логіки або метрик (тільки CI та те� ти).

## Структура/зміни (мінімально)

```
.github/workflows/ci.yml       # jobs: lint, type, test, build (ubuntu-latest)
pytest.ini                     # add: --cov-fail-under=90 (якщо ще нема)
tests/test_ci_smoke.py         # health/metrics/debug RBAC presence-те� ти
```

> Якщо `--cov-fail-under` уже у конфігах — викори� товуй і� нуюче; інакше додай у job’і прапор.

## Те� т-план (доповнення)

* `test_ci_smoke.py`:

  * `test_health_ok()`
  * `test_metrics_keys_present()` — presence `confusion_tp_total` … `drift_pct_last`, `acl_*`, `fsm_*`
  * `test_debug_rbac_denied()` → 403
  * `test_debug_rbac_allowed_with_drift()` → 200 + `drift_report` (на фік� турі)
* `test_idem_parallel_smoke.py` (� корочений варіант на 50 потоків)

## Acceptance

* CI зелений на PR у `chore/p1-ci-qa-gates`
* Repo coverage **≥90%** у CI
* mypy/ruff чи� то
* Smoke e2e пройшов
* Wheel зібрав� я у� пішно

**Commit (приклад):**
`chore(ci): P1 gates — lint/type/test(cov>=90)/build + smoke e2e [FSMP-P1-T06]`

**JOURNAL.md (додай):**

```
RID: FSMP-P1-T06
Actor: copilot
Why: enforce CI QA gates (lint/type/coverage>=90/smoke/build)
```

---

Коли T06 буде готовий і CI зелений — зафік� уємо **P1 = PASS** і я дам компактний план на **P2 (ACL-Adapter live + shadow→active canary)**. І так, **нагадування:** не забудь оновити `TODO.md`.

Прийняв. Рухаємо� ь рівно за STOP-рамкою. Нижче — готовий до відправки **TaskSpec для Copilot** (ро� ій� ькою) + короткі примітки для тебе.

---

## Що отримаєш пі� ля Т01

* **Тонкий execution-адаптер** поверх нашого ACL із режимами `dry_run/paper` (за замовчуванням — `dry_run`).
* **Ідемпотентні� ть на кордоні** (submit/cancel), детермінований `client_order_id`.
* **Надійні� ть**: retry + circuit breaker + backoff, повага до таймаутів/лімітів.
* **Спо� тережні� ть**: лічильники, p95 для submit/cancel, WHY≤80 в ERR.
* **Те� ти ≥90% покриття** на новому коді, `mypy` clean, без будь-яких live-угод.

---

## 🎯 Завдання для Copilot (RU). Отправь это ему как е� ть

**Заголовок:** FSMP-P2-T01 · Реальный SDK-адаптер (execution_adapter) · dry_run/paper

**Цель:** Подключить реальный биржевой SDK поверх � уще� твующего ACL-контракта без бизне� -логики. Поддержать режимы `dry_run` (дефолт) и `paper`. Любые live-ордеры запрещены, пока флаги не включены явно — в этом та� ке мы их **не** включаем.

**Scope (� трого минимум):**

1. **Папки/файлы (� оздай при от� ут� твии, не трогай о� тальное):**

* `core/adapters/execution_adapter.py` — реализация.
* `core/adapters/execution_exceptions.py` — нормализованные ошибки SDK→ERR.
* `core/adapters/idempotency_ledger.py` — легкий локальный ledger (in-proc) для submit/cancel (exactly-once по ключу).
* `configs/adapter.yaml` — параметры retry/CB/timeouts/limits.
* `tests/adapters/test_execution_adapter_*.py` — те� ты из раздела “Те� ты”.

2. **API адаптера (тонкий � лой над ACL):**

* `submit(order_dto) -> EVT:ORDER_PLACED | EVT:REJECTED`
* `cancel(order_id|client_order_id) -> EVT:CANCELLED | EVT:REJECTED`
* `stream()` (генератор/а� инх. итератор) SDK-� обытий → маппинг в `EVT:PARTIAL_FILL|FILL|EXPIRED|REJECTED`.

3. **Идемпотентно� ть (граница адаптера):**

* `client_order_id = hash64(symbol, side, qty, price_mode, reduce_only?, ts_bucket_1s, idempotent_key)`; тот же вход → тот же id.
* Повторные `submit`/`cancel` �  тем же ключом — **no-op** (верни last-known EVT).
* Ledger хранит минимум: `{key, first_seen_ts, last_status, last_payload_digest}`.

4. **Надёжно� ть:**

* **Retry**: эк� поненциальный backoff �  джиттером; лимиты из `configs/adapter.yaml`.
* **Circuit Breaker**: `open_threshold_error_rate`, `cooldown`, `half_open_probes`. В `OPEN` — `ERR.adapter.cb_open` (WHY≤80).
* **Timeouts**: на в� е I/O вызовы; по таймауту — чёткое ERR-� ообщение �  нормализованным кодом.
* **Rate-limits**: уважай бюджет запро� ов; при и� черпании — короткая � пячка + � чётчик `sdk_retries_total`.

5. **Конфиг/� екреты (только ENV, чтение уже через `config.py`):**

* `EXECUTION_MODE ∈ {dry_run, paper, live}`; **default= dry_run**.
* `EXCHANGE_API_KEY`, `EXCHANGE_API_SECRET`, `EXCHANGE_BASE_URL`, `EXCHANGE_WS_URL`.
* `ADAPTER_SUBMIT_TIMEOUT_MS`, `ADAPTER_CANCEL_TIMEOUT_MS`, `ADAPTER_STREAM_TIMEOUT_MS`.
* `ADAPTER_RETRY_MAX_ATTEMPTS`, `ADAPTER_RETRY_BASE_MS`, `ADAPTER_RETRY_MAX_MS`.
* `ADAPTER_CB_OPEN_THRESHOLD`, `ADAPTER_CB_COOLDOWN_MS`, `ADAPTER_CB_HALF_OPEN_PROBES`.

6. **Observability:**

* Счётчики (инкрементируй � трого в точках I/O):
  `sdk_submit_total`, `sdk_cancel_total`, `sdk_stream_events_total`, `sdk_retries_total`, `sdk_cb_open_total`.
* Ги� тограммы p95 (локальные метрики адаптера):
  `sdk_submit_latency_ms`, `sdk_cancel_latency_ms`.
* Лог-� троки по � тандарту: `ts|rid|op|verb|src→dst|key|lat_ms|err?` + `why (≤80)`.

7. **Guard-флаги:**

* В `dry_run` **запретить** любые торговые методы SDK. Делай только валидацию входа + лог/метрики + возврат � имулированного `EVT:ORDER_PLACED`/`CANCELLED`.
* В `paper` — разрешены чтения/quote/маркет-данные; торговые вызовы оборачивай, но **и� пользуй sandbox** (е� ли SDK умеет) и возвращай реальные коды только при до� тупно� ти “paper/testnet”. В этом та� ке не подключаем live.

8. **Маппинг � обытий SDK → EVT:***

* `NEW/ACCEPTED` → `EVT:ORDER_PLACED {client_order_id, exchange_order_id?, price, qty_remain, ...}`
* `PARTIAL_FILL` → `EVT:PARTIAL_FILL {filled_qty, remain_qty, last_price, ...}`
* `FILL` → `EVT:FILL {avg_fill_price, filled_qty, fees?}`
* `CANCELLED` → `EVT:CANCELLED {reason}`
* `EXPIRED` → `EVT:EXPIRED {reason}`
* Любая ошибка SDK нормализует� я в `EVT:REJECTED` + `ERR.code` из `execution_exceptions.py`.

9. **WHY (≤80) для ERR (обязательно):**

* Примеры:
  `why="CB open: submit suppressed"`
  `why="Timeout submit 250ms > 200ms limit"`
  `why="Idempotent duplicate: no-op"`

10. **Каче� тво и SLO для этого та� ка:**

* **Покрытие те� тами нового кода ≥90%**, включая ветвления ошибок/повторов/CB.
* `mypy --strict` clean; без новых warning.
* **Латентно� ть** (локально, мок): `p95(submit|cancel) ≤ 25ms`.
* `timeout_rate ≤ 1%` в мок-интеграции.

---

**DoD / Gates (принять работу только е� ли в� ё выполняет� я):**

1. `submit/cancel/stream` работают в `dry_run` и `paper` (е� ли е� ть paper-endpoint у SDK).
2. Повторные `submit/cancel` не � оздают дубликатов � обытий (проверено те� тами ledger).
3. ENV-� екреты требуют� я жё� тко: при от� ут� твии — fail-obvious �  понятным `ERR` и WHY≤80.
4. Метрики � читают p95 и � чётчики увеличивают� я корректно.
5. Документы добавлены:

   * `docs/ADAPTER_GUIDE.md` (интерфей� , режимы, метрики, env-переменные, пример WHY).
   * `JOURNAL.md`: `RID=FSMP-P2-T01`, краткая запи� ь (WHY≤80).
   * Обновлён `TODO.md`.

---

**Те� ты (минимальный набор, названия � трого):**

* `test_adapter_submit_dry_run_ok()` — � имулирует `ORDER_PLACED`.
* `test_adapter_submit_idempotent()` — повторный submit → **no-op** (тот же EVT).
* `test_adapter_cancel_dry_run_ok()` — корректный `CANCELLED`.
* `test_adapter_stream_mapping()` — SDK-ивенты корректно маппят� я в `EVT:*`.
* `test_adapter_cb_retry()` — инъекция ошибок → backoff/CB/ERR, WHY≤80 при� воен.
* `test_adapter_env_required()` — нет ENV → понятный отказ �  ERR/WHY.

**Out of scope (� трого запретить в этом та� ке):**

* Любые live-ордеры/торговля.
* Изменения FSM или ри� к-политик.
* Новые HTTP-эндпоинты.

---

## Короткі ін� трукції для тебе

* Надішли Copilot блок “🎯 Завдання для Copilot (RU)” без змін.
* Пі� ля його відповіді: я зроблю перевірку на відповідні� ть DoD/SLO і, за потреби, відкоригую на� тупні кроки (P2-T02…T07).
* Пам’ятаємо про **WHY≤80**, **coverage≥90%**, **timeout_rate≤1%**, **p95≤25 м� ** у мок-режимі.

В� е. Переходимо до виконання FSMP-P2-T01 без жодних відхилень від рамки.
