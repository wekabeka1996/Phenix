

---

#                       Copilot (RU)

**TASK_ID:** `FSMP-P0-T02`
**Title:**                    P0    `vfoundation-reference`: /metrics, idempotency TTL, WAL integrity,              core/router/CB
**Branch:** `fix/p0-delta-metrics-idem-wal`

##          (DoD/Gates)

1.                  `/metrics`             p95(router), timeout_rate, queue_depth.
2.                    `/debug/{rid}`:                `why_chain`, `integrity_ok`, `merkle_root`, `count`.
3. `/replay/{rid}`: dry-run + `integrity_ok:true`, `replayed:N`.
4. Idempotency:              `idempotent_key` + TTL-       (                        10       ).                                                                     WAL,            `dedup:true`.
5.                                    /                `core/routing.py`, `core/retry_cb.py` (               `...`,                               CB.allow, ERR-          ).
6.                    `why` (   80                 )                                       ,                                                                                       ;                               400    **    **                 WAL.
7.                            :                      90%, e2e round-trip PASS, RBAC 403        `/debug`/`/replay`        `Authorization`.

##                       

* Contract-first, additive-only, fail-closed.                                                              ;                                .
*              XAI:              `why` + `why_chain`;                                                                           .
* RBAC                   : `require_admin()`        `/debug`/`/replay`.

##                                      (              )

```
vfoundation/obs/debug_api.py
  + GET /metrics     {router_p95_ms, timeout_rate, queue_depth}
  ~ GET /debug/{rid}                      why_chain, integrity_ok, merkle_root
  ~ GET /replay/{rid}                      integrity_ok, replayed

vfoundation/core/routing.py
  ~                               route():              "..."    ERR-                                   VALID enum
  ~                           handler                          msg (why<=80), ttl/expiry; on invalid     400/ERR        WAL

vfoundation/core/retry_cb.py
  ~                  CircuitBreaker.allow(): CLOSED   true; HALF_OPEN                            ; OPEN            cool_down                
  ~              "..."                                  allow()                   

vfoundation/core/idempotency.py
  ~                                idempotent_key (string) + TTLCache
  +             : seen(key), remember(key, result, ttl_ms), get(key)

vfoundation/dr/wal.py
  ~    append():                  prev_hash/_hash                
  +           .                verify_chain(records)->bool (             _prev/_hash)
  +                       :                                        merkle_root                     _hash

vfoundation/dr/replay.py
  ~ replay_for_rid:                events,                (events, integrity_ok, merkle_root)

vfoundation/obs/why.py
  ~ ensure append_why(chain, msg.why)                             debug_api        why_chain

tests/
  + test_metrics_smoke.py: 200x /health     p95(router)     10     (                ), /metrics shape OK
  + test_debug_replay_integrity.py:                RID                        why_chain, integrity_ok, merkle_root
  + test_idempotency_ttl.py:        POST                         idempotent_key                     TTL                              WAL,                         dedup:true
  ~ test_routing_idempotency.py:                                 idempotent_key
  ~ test_wal_replay.py:                                   integrity_ok
```

##                   /               (                                          )

* **/metrics**:

  ```json
  { "router_p95_ms": <number>, "timeout_rate": <number>, "queue_depth": <integer> }
  ```
* **/debug/{rid}**:

  ```json
  { "rid": "<rid>", "count": <int>, "events": [ ... ], "why_chain": ["..."], "integrity_ok": true, "merkle_root": "<hex>" }
  ```
* **/replay/{rid}**:

  ```json
  { "rid":"<rid>", "replayed": <int>, "integrity_ok": true }
  ```
* **Idempotency**:                                                  `idempotent_key` (string).                                  `{"dedup": true}`.

## CI/                

* `pytest -q --cov=vfoundation --cov-report=term-missing`
* `ruff`, `black --check`, `mypy` (                                           )
*                               90%;                                           Gate.

##                            PR

1.                                             (               ).
2.           /      : e2e round-trip PASS; `/metrics` shape; RBAC 403; coverage     90%.
3.                  `ops/COPILOT_PROGRESS.md` (                                  P0)    `JOURNAL.md`:

   ```
   RID=FSMP-P0-T02
   Why=finish P0 delta (metrics+idempotency+WAL-integrity)
   ```

##             

`fix(p0): metrics/idempotency TTL/wal integrity + router/cb finalize [FSMP-P0-T02]`

---


---

#                       Copilot (RU)     1/2

**TASK_ID:** `FSMP-P0-T03`
**Title:**                                          WAL                                                      (locks + CAS)
**Branch:** `fix/p0-wal-and-idem-locks`

##          (DoD/Gates)

1. **WAL:**                    race        append:                                                              `prev_hash   hash`                 N                                         (                          /                              ).
2. **Idempotency:**   single-flight        `idempotent_key`:                                                                                                                        handler,                               `dedup:true`/`inflight:true`                                         .
3. **Fail-closed:**                                                                                            `lock_timeout_ms`                    503/429, **                   **    WAL.
4. **          :**                                                                                                                                       ;                      90%.

##                       

* Contract-first:                                                                               ;                                                         .
* Additive-only:                                               /                CAS-                ;                                      -                                  .
*              XAI:                                     `why`                     LOCK_ACQUIRED / LOCK_TIMEOUT / IDEM_DEDUP / IDEM_INFLIGHT  .

##                                      (              )

```
vfoundation/dr/wal.py
  + WALFileLock:           -                                                                                    (fcntl|msvcrt;                                  ).
  ~ append(...):
      -                    file lock (LOCK_EX).
      -                                                      prev_hash.
      -                                      ,                    hash.
      -                                 (                         O_APPEND;                                                           fsync).
      -                      lock.
  + append_cas(expected_prev_hash?):                                                                                                      CONFLICT (409)                    .
  + verify_chain(records)->bool:                                 /debug    /replay.
  +               :                lock_contention, lock_wait_ms (       /metrics).

vfoundation/core/idempotency.py
  + KeyedLocks:   single-flight                 (                                               idempotent_key;    TTL-                          ).
  + Inflight registry:                    {pending, done(result)} c TTL.
  ~ API:
      - begin(key) -> {"acquired":true}|{"inflight":true}
      - complete(key, result)  #                                                                     "                    "                                      TTL
      - get_if_done(key) -> Optional[result]
      - remember_dedup(key)  #                                                                     

vfoundation/core/routing.py
  ~               :
      1)                                                            (why   80, TTL ok)                400,        WAL.
      2)                               :
         -          get_if_done(key)                             cached result (dedup:true).
         -            begin(key):          inflight                    202/209    {"inflight":true} (       WAL).
         -          acquired                                             .
      3)                         handler:                     WAL; complete(key, result);                200.
      4)                            : complete(key, error as ERR);                500/4xx.
  +         /              : idem_acquired, idem_inflight, idem_dedup.

vfoundation/obs/debug_api.py
  ~ /debug/{rid}, /replay/{rid}:                  verify_chain()             integrity_ok (true/false).

docs/ADR-004-WAL-Concurrency.md  (          )
  -               : file-lock + O_APPEND;                          CAS;                                                                                                                   .
```

##                                             (               )

* **WAL file-lock:**

  *                                                                                                               **                        **                      .
  *      Unix     `fcntl.flock(LOCK_EX)`,      Windows     `msvcrt.locking()`.                                  helper.
  *                                `O_APPEND`;                                                                                                                                             ;                                       lock                                           lock                     .
  *                    : `append_cas(expected_prev_hash)`                                                  (                 ).

* **Idempotency single-flight:**

  *                                                                              `acquired:true` (                        handler).
  *                                     `inflight:true` (           202,                    P0                      `200{"inflight":true}`          `209`                        ).
  *                                                                                  (TTL)                                                                                `dedup:true`.
  *                                                              P0                       (       Router     single worker                        ).        multi-worker                Redis-lock    P1/P2.

##         -         (           1,             PR)

* **WAL race (threaded):** 100                          append     `verify_chain=True`,            N,                                                    .
* **WAL race (multiprocess, optional):** 8                       100 append                             (Linux CI                             ; Windows     best-effort).
* **Idem single-flight:** 50                                        `idempotent_key`     1            handler;                    `inflight:true`                                     cached result;    WAL                1                           .
* **Fail-closed:**                                                                lock                                                503 (lock timeout),       WAL                                   .
* **              :** lock_contention > 0                        ; timeout_rate     1%    smoke.

##               /        

*         :                    file-lock     2   5                                        .
* p95(router)                       > +2                                                (    . /metrics).

##                            PR

* ADR-004,                                ,                      -            , coverage    90%,                              -           P0,                 JOURNAL (`FSMP-P0-T03`).

**            :** `fix(p0): WAL file-lock + single-flight idempotency; integrity checks [FSMP-P0-T03]`

---

#                       Copilot (RU)     2/2

**TASK_ID:** `FSMP-P0-T04`
**Title:** Monotonic TTL +                               (janitor)                                      
**Branch:** `fix/p0-idem-monotonic-janitor`

##          (DoD/Gates)

1.                   `time.time()`      `time.monotonic()`/`monotonic_ns`                                  TTL/                                       .
2.                  **background janitor**:                                                                           (`cleanup_expired`)                                           .
3.                                                                                                                    (                   ).

##                                     

```
vfoundation/core/idempotency.py
  ~ TTL:                expiry                                          (ns        ms).
  + Janitor:                        /                             (                , 60  ),                                     cleanup_expired().
  +                   : idem_ttl_ms, janitor_interval_ms,                                                        (evict policy = LRU        overflow).
vfoundation/obs/debug_api.py
  + /metrics:                  idem_cache_size, janitor_runs, janitor_evicted.
docs/Operations.md
  +             :                          Idempotency Store (                    ,       ).
```

##         -         (           2)

* **Monotonic correctness:**                                                        /             (             time)     TTL                      .
* **Janitor under load:** 10^5                                      TTL=1   3                  2                                           ; /metrics                  evicted > 0.
* **LRU cap:**                             max_size                                                           ,                            .

**            :** `fix(p0): monotonic TTL + janitor for idempotency; metrics and docs [FSMP-P0-T04]`

---

##              QA-          -         (                                 PR)

**RID:** `FSMP-P0-QA01`
**        :**                                                                 ;                                               WAL      single-flight                                                                .

**                                       :**

1. 1K/5K                          append    WAL (threads   processes).
2. 1K                                          `idempotent_key`      1               .
3. 10^5                                        2                (                   janitor).
4.                                     :                                             lock contention                        fail-closed (503/429),                                         .

**                 PASS:**

* `verify_chain=True`                           ; `integrity_ok:true`    `/debug`/`/replay`.
*                                    **1             **    WAL;                               `inflight:true`        `dedup:true`.
*                                 (  10%)            janitor 2     ;                                                     .
* p95(router)                                                    +2     vs                 .

---

##               /                  

**JOURNAL.md (                       ):**

```
RID: FSMP-P0-T03
Actor: copilot
Why: WAL lock + single-flight idempotency
Links: PR#..., ADR-004

RID: FSMP-P0-T04
Actor: copilot
Why: monotonic TTL + janitor
Links: PR#..., /metrics updates
```

**ADR:** `docs/ADR-004-WAL-Concurrency.md`                   ,                          (single-writer               , Redis-lock),                                         file-lock + single-flight        P0.

---

###                XAI-                   (WHY-                                    )

* `WAL_LOCK_ACQUIRED`, `WAL_LOCK_TIMEOUT`, `WAL_APPEND_DONE`, `WAL_VERIFY_FAIL`
* `IDEM_BEGIN`, `IDEM_INFLIGHT`, `IDEM_RESULT_CACHED`, `IDEM_DEDUP`
* `TTL_MONOTONIC`, `JANITOR_EVICT`



---

#                       Copilot (RU)     HOTFIX Pydantic V2

**TASK_ID:** `FSMP-P1-HOTFIX-PYD-001`
**Title:**                  `@validator`     `@field_validator`                       -                                         `execution_position/contracts.py`
**Branch:** `fix/p1-pydantic-v2-validators`

##          (DoD/Gates)

1.                  V1-           `@validator`      V2-           `@field_validator`    `apps/reference/domains/execution_position/contracts.py`.
2.                                                             (        ., `qty*price     min_notional`,                                `price`                       , `qty`                       )                            `@model_validator(mode="after")`.
3.                               `float`                                              `Decimal`                         `quantize`                 (price_step, qty_step)                      /              .
4. MyPy clean,                          ,                      90%                                       .
5.                                          :                                 gate                           Pydantic V1    `pytest.ini`.

##                               (               )

*             : `from pydantic import field_validator, model_validator, BaseModel`    `from pydantic import ConfigDict` (                                              ).
*        `@validator("qty")`                  `@field_validator("qty", mode="after")` (                                                                              )        `mode="before"` (                                  /                                            ).
*        `@validator("price")`                          `@field_validator("price", mode="after")`.
*                                       (                        `symbol`/`price`/`qty`                         )                            `@model_validator(mode="after")`.
*                                  Pydantic v2:                                                `model_config = ConfigDict(validate_assignment=True, str_strip_whitespace=True)`.

##                                                      

* `qty`:                     `Decimal`,                           `qty_step` (                  , 0.001).
* `price`: `Decimal`,                           `price_step` (                  , 0.01).
*                                  : `min_qty`, `min_notional` (           `qty*price`).
*                                                                                          `why` (   80)    DEC/CMD                   FSM                                               .

##         -        

                /                            (                               `tests/test_execpos_contracts_pydantic_v2.py`):

1. **Validator on `qty`:**             /float     `Decimal`,                      `quantize`      `qty_step`.
2. **Validator on `price`:**                     ;                                                 /                               (                                                                                             -             BUY,                   SELL                 shadow                                  round_to_step, mode='nearest'                                   ).
3. **Cross-field:** `qty*price < min_notional`     ValidationError;                                                          .
4. **Edge cases:**             /                               ,                                       , NaN/Inf (                      ).
5. **Coverage:**      `contracts.py`     90%.

## CI/Gates

*    `pytest.ini`                                                           :

  ```
  [pytest]
  filterwarnings =
      error::pydantic.warnings.PydanticDeprecatedSince20
  ```

  (                                                                                                                      error::PydanticDeprecatedSince20  .)
*             : `pytest -q --cov=...`                   ,                          -                  .

##                         

*    `docs/CLI.md`/`docs/Schemas.md`               :                                              Pydantic v2: `@field_validator`,                                                 `@model_validator`   .
* `JOURNAL.md`                 :

  ```
  RID: FSMP-P1-HOTFIX-PYD-001
  Actor: copilot
  Why: migrate validators to Pydantic v2; add cross-field checks
  ```

**            :** `fix(execpos): migrate to @field_validator + cross-field model_validator; enable Pydantic v2 warn gate [FSMP-P1-HOTFIX-PYD-001]`

---

                .                          :          **drift monitor**             ,                                                    .                        DoD        T03                                         ,                                       **NO-GO**                .

#                                   PASS T03 (                    )

##                       Copilot     FSMP-P1-T03-GATE

**Branch:** `feat/p1-drift-monitor`     `chore/p1-t03-gate-wire`

**         (           2):**

1. **/metrics wiring:**                       `aggregate_drift_metrics()`                         :

   * `confusion_tp_total`, `confusion_fp_total`, `confusion_fn_total`, `confusion_tn_total`
   * `drift_pct_last`, `accuracy_last`
   *         : `/metrics`                                                                    .
2. **/debug/{rid} wiring (RBAC):**                           `drift_report`:

   * `{confusion:{tp,fp,fn,tn}, mismatches:[      5], computed_at, records_processed}`
   *         : `/debug/{rid}`                  `drift_report`        e2e           .

**                  :**                                             ,                                                   ;                      /                  .
**DoD:** 100%                             ; repo coverage    90%; mypy clean.
**Commit:** `chore(p1-t03): wire drift to /metrics and /debug; tests [FSMP-P1-T03-GATE]`

                          **GO/PASS T03**.

---

#                                                                                

                 :        P1                           **                                                   ENV** (                                                   ),                              .

##                    FSMP-P1-T04 (Minimal Config)

**Branch:** `feat/p1-config-minimal`

**Scope (               ):**

*                  `vfoundation/config.py` (ENV-                                                       ):

  * `RBAC_ADMIN_TOKENS`, `SIGNING_KEY`
  * `WAL_DIR`, `WAL_LOCK_TIMEOUT_SEC`
  * `CB_THRESHOLD`, `CB_COOLDOWN_SEC`
  * `IDEM_TTL_MS`, `IDEM_MAX_ENTRIES`
  * `DRIFT_TIME_WINDOW_SEC`
*                                                               (rbac, wal, retry_cb, idempotency, drift).
*           : `test_config_env_overrides.py` (override            `monkeypatch`),                                                                                      .
*       :                  `ADR-005-Config.md` (         ENV,                               ).

**Commit:** `feat(config): minimal ENV config for secrets + critical knobs [FSMP-P1-T04]`
**DoD:**                        , coverage    90%,                  fail-obvious      dev.

---

##                 

* **T03:** core               ,                                           `/metrics`    `/debug` (T03-GATE).
* **Config:**                  SECRET/knobs    ENV                     T04                       T03-GATE.

                        T03-GATE                               PASS                       T04.


                      Copilot     FSMP-P1-T04 (Minimal Config)

TASK_ID: FSMP-P1-T04
Branch: feat/p1-config-minimal

         (                   )

                                                                        ENV                                                          ,                               -             FSM.

Scope (                           )

                 vfoundation/config.py    ENV-                      :

RBAC_ADMIN_TOKENS, SIGNING_KEY

WAL_DIR, WAL_LOCK_TIMEOUT_SEC

CB_THRESHOLD, CB_COOLDOWN_SEC

IDEM_TTL_MS, IDEM_MAX_ENTRIES

DRIFT_TIME_WINDOW_SEC

                                      :

vfoundation/security/rbac_abac.py (RBAC             )

vfoundation/dr/wal.py (WAL_DIR, lock timeout)

vfoundation/core/retry_cb.py (CB thresholds)

vfoundation/core/idempotency.py (TTL/entries)

apps/reference/domains/execution_position/drift_monitor.py (time window)

                                                            (fail-obvious dev               :                         ,                  ).

STOP-        

             live-reload/                      / YAML/JSON.

            -                   FSM (trail/breakeven/time_stop)                      .

                                            ,                                                     .

          

tests/test_config_env_overrides.py:

monkeypatch ENV                         ,                                                                                    .

                  /                                  (       ENV)                                  ,                                          .

                  ,                                               ENV                     (                                                                 ).

                        

docs/ADR-005-Config.md                   :

                       ENV            .

                                                (                   dev-only).

                   (           live reload).

DoD / Gates

                              ; repo coverage    90%, mypy clean.

                       dev-              /                                            ,          fail-obvious dev-                            ENV.

/metrics, /debug, WAL/Idempotency                            .

          :
feat(config): minimal ENV config for secrets + critical knobs [FSMP-P1-T04]

JOURNAL.md

RID: FSMP-P1-T04
Actor: copilot
Why: minimal ENV config for secrets & critical knobs

                . **FSMP-P1-T05     PASS.** CLI-shadow (replay/drift)                                  : 11/11             ,                                             100%,       -                 .

             git-                                                          .
         , **              :                                                     `TODO.md`.**

---

#                       Copilot (RU)     FSMP-P1-T06 (CI/QA     P1 Gate)

**              :**              git-                                     .
**                      :**                               **               `TODO.md`**    **JOURNAL.md**.

**TASK_ID:** `FSMP-P1-T06`
**Branch:** `chore/p1-ci-qa-gates` (                     )

##         

                   **Gate P1**    CI:                                                   /              /                                              P1.

## DoD / Gates (              )

1. **CI (GitHub Actions)                                        **:

   * `lint`: ruff (                           ), **0                    PASS**
   * `type`: mypy **clean**
   * `test`: pytest    **               coverage    90%** (fall-under     RED)
   *                   : `coverage.xml`, `htmlcov` (upload)
   *                    pip/venv (                    )
2. **Smoke e2e** (pytest,                                 ):

   * `/health` 200
   * `/metrics`                           drift    ACL/FSM                        (                   presence,                      )
   * `/debug/{rid}`        RBAC: 403                    , 200                    ;             drift-report                                 `drift_report`
3. **                                          **:

   * WHY     80 (                                     400        WAL)
   *                               : 50                        `CMD:OPEN`     1 DEC,          `inflight|dedup` (                             )
4. **                         **:

   * `python -m build` (wheel + sdist)                   ,                                       

## STOP-        

*                            /              /              .
*                   endpoint       .
*                                   -                                 (             CI                ).

##                   /           (                    )

```
.github/workflows/ci.yml       # jobs: lint, type, test, build (ubuntu-latest)
pytest.ini                     # add: --cov-fail-under=90 (                      )
tests/test_ci_smoke.py         # health/metrics/debug RBAC presence-          
```

>          `--cov-fail-under`                                                                       ;                            job                  .

##         -         (                    )

* `test_ci_smoke.py`:

  * `test_health_ok()`
  * `test_metrics_keys_present()`     presence `confusion_tp_total`     `drift_pct_last`, `acl_*`, `fsm_*`
  * `test_debug_rbac_denied()`     403
  * `test_debug_rbac_allowed_with_drift()`     200 + `drift_report` (                     )
* `test_idem_parallel_smoke.py` (                                         50               )

## Acceptance

* CI                     PR    `chore/p1-ci-qa-gates`
* Repo coverage **   90%**    CI
* mypy/ruff           
* Smoke e2e               
* Wheel                                

**Commit (              ):**
`chore(ci): P1 gates     lint/type/test(cov>=90)/build + smoke e2e [FSMP-P1-T06]`

**JOURNAL.md (          ):**

```
RID: FSMP-P1-T06
Actor: copilot
Why: enforce CI QA gates (lint/type/coverage>=90/smoke/build)
```

---

         T06                            CI                                         **P1 = PASS**                                                 **P2 (ACL-Adapter live + shadow   active canary)**.          , **                      :**                                  `TODO.md`.

              .                                    STOP-            .                                                       **TaskSpec        Copilot** (                    ) +                                                .

---

##                                    01

* **             execution-              **                           ACL                       `dry_run/paper` (                                    `dry_run`).
* **                                                  ** (submit/cancel),                              `client_order_id`.
* **                    **: retry + circuit breaker + backoff,                                     /              .
* **                            **:                     , p95        submit/cancel, WHY   80    ERR.
* **              90%                 **                           , `mypy` clean,                -         live-        .

---

##                              Copilot (RU).                                             

**                  :** FSMP-P2-T01                     SDK-               (execution_adapter)    dry_run/paper

**        :**                                                        SDK                                         ACL-                                      -            .                                   `dry_run` (            )    `paper`.            live-                               ,                                                                                         **    **                 .

**Scope (                           ):**

1. **          /           (                                        ,                                     ):**

* `core/adapters/execution_adapter.py`                         .
* `core/adapters/execution_exceptions.py`                                                 SDK   ERR.
* `core/adapters/idempotency_ledger.py`                                     ledger (in-proc)        submit/cancel (exactly-once                ).
* `configs/adapter.yaml`                        retry/CB/timeouts/limits.
* `tests/adapters/test_execution_adapter_*.py`                                                    .

2. **API                  (                             ACL):**

* `submit(order_dto) -> EVT:ORDER_PLACED | EVT:REJECTED`
* `cancel(order_id|client_order_id) -> EVT:CANCELLED | EVT:REJECTED`
* `stream()` (                  /          .                 ) SDK-                                     `EVT:PARTIAL_FILL|FILL|EXPIRED|REJECTED`.

3. **                               (                               ):**

* `client_order_id = hash64(symbol, side, qty, price_mode, reduce_only?, ts_bucket_1s, idempotent_key)`;                                      id.
*                    `submit`/`cancel`                                 **no-op** (           last-known EVT).
* Ledger                            : `{key, first_seen_ts, last_status, last_payload_digest}`.

4. **                    :**

* **Retry**:                                  backoff                      ;                   `configs/adapter.yaml`.
* **Circuit Breaker**: `open_threshold_error_rate`, `cooldown`, `half_open_probes`.    `OPEN`     `ERR.adapter.cb_open` (WHY   80).
* **Timeouts**:             I/O             ;                                        ERR-                                                               .
* **Rate-limits**:                                           ;                                                               +                `sdk_retries_total`.

5. **            /               (             ENV,                                `config.py`):**

* `EXECUTION_MODE     {dry_run, paper, live}`; **default= dry_run**.
* `EXCHANGE_API_KEY`, `EXCHANGE_API_SECRET`, `EXCHANGE_BASE_URL`, `EXCHANGE_WS_URL`.
* `ADAPTER_SUBMIT_TIMEOUT_MS`, `ADAPTER_CANCEL_TIMEOUT_MS`, `ADAPTER_STREAM_TIMEOUT_MS`.
* `ADAPTER_RETRY_MAX_ATTEMPTS`, `ADAPTER_RETRY_BASE_MS`, `ADAPTER_RETRY_MAX_MS`.
* `ADAPTER_CB_OPEN_THRESHOLD`, `ADAPTER_CB_COOLDOWN_MS`, `ADAPTER_CB_HALF_OPEN_PROBES`.

6. **Observability:**

*                  (                                                        I/O):
  `sdk_submit_total`, `sdk_cancel_total`, `sdk_stream_events_total`, `sdk_retries_total`, `sdk_cb_open_total`.
*                        p95 (                                                  ):
  `sdk_submit_latency_ms`, `sdk_cancel_latency_ms`.
*       -                                    : `ts|rid|op|verb|src   dst|key|lat_ms|err?` + `why (   80)`.

7. **Guard-          :**

*    `dry_run` **                  **                                          SDK.                                                       +       /               +                                               `EVT:ORDER_PLACED`/`CANCELLED`.
*    `paper`                                    /quote/            -            ;                                                   ,      **                   sandbox** (         SDK           )                                                                                               paper/testnet   .                                                  live.

8. **                              SDK     EVT:***

* `NEW/ACCEPTED`     `EVT:ORDER_PLACED {client_order_id, exchange_order_id?, price, qty_remain, ...}`
* `PARTIAL_FILL`     `EVT:PARTIAL_FILL {filled_qty, remain_qty, last_price, ...}`
* `FILL`     `EVT:FILL {avg_fill_price, filled_qty, fees?}`
* `CANCELLED`     `EVT:CANCELLED {reason}`
* `EXPIRED`     `EVT:EXPIRED {reason}`
*                         SDK                               `EVT:REJECTED` + `ERR.code`      `execution_exceptions.py`.

9. **WHY (   80)        ERR (                      ):**

*               :
  `why="CB open: submit suppressed"`
  `why="Timeout submit 250ms > 200ms limit"`
  `why="Idempotent duplicate: no-op"`

10. **                    SLO                             :**

* **                                                         90%**,                                               /                /CB.
* `mypy --strict` clean;                   warning.
* **                      ** (                ,       ): `p95(submit|cancel)     25ms`.
* `timeout_rate     1%`          -                    .

---

**DoD / Gates (                                                                               ):**

1. `submit/cancel/stream`                     `dry_run`    `paper` (                  paper-endpoint    SDK).
2.                    `submit/cancel`                                                         (                                  ledger).
3. ENV-                                              :                                 fail-obvious                     `ERR`    WHY   80.
4.                               p95                                                                  .
5.                                      :

   * `docs/ADAPTER_GUIDE.md` (                  ,             ,               , env-                    ,              WHY).
   * `JOURNAL.md`: `RID=FSMP-P2-T01`,                             (WHY   80).
   *                  `TODO.md`.

---

**           (                                 ,                              ):**

* `test_adapter_submit_dry_run_ok()`                          `ORDER_PLACED`.
* `test_adapter_submit_idempotent()`                        submit     **no-op** (            EVT).
* `test_adapter_cancel_dry_run_ok()`                          `CANCELLED`.
* `test_adapter_stream_mapping()`     SDK-                                                    `EVT:*`.
* `test_adapter_cb_retry()`                                       backoff/CB/ERR, WHY   80                 .
* `test_adapter_env_required()`            ENV                                    ERR/WHY.

**Out of scope (                                                      ):**

*            live-            /                .
*                    FSM                -              .
*            HTTP-                  .

---

##                                                    

*                Copilot                                          Copilot (RU)                   .
*                                       :                                                                    DoD/SLO   ,                    ,                                                  (P2-T02   T07).
*                              **WHY   80**, **coverage   90%**, **timeout_rate   1%**, **p95   25     **          -            .

      .                                              FSMP-P2-T01                                                         .
