# Життєвий Цикл Ордера та Потік Виконання в Си� темі Aurora

**Вер� ія:** 1.0
**Дата:** 2025-10-26

## 1. Мета документа

Цей документ детально опи� ує проце�  прийняття торгових рішень та виконання ордерів у � и� темі. Мета — надати повне розуміння того, **які � игнали** викори� товують� я, **як на їх о� нові формуєть� я торговий намір**, **які типи ордерів** � творюють� я, та **в якому форматі** вони відправляють� я на біржу.

## 2. Фаза 1: Генерація Сигналу (Signal Generation)

В� е починаєть� я з � ирих ринкових даних, які перетворюють� я на � тандартизовані аналітичні � игнали.

**Вхідні дані:**
-   Потоки ринкових даних з біржі (через `MarketDataConnector`):
    -   `bookTicker`: Дані про найкращі ціни купівлі (bid) та продажу (ask) та їх об� яги.
    -   `trade`: Дані про кожну окрему угоду, що відбула� я на ринку.

**Проце�  (в домені `FeatureEngineering`):**
1.  **Розрахунок OBI (Order Book Imbalance):**
    -   **Формула:** `(bid_volume - ask_volume) / (bid_volume + ask_volume)`
    -   **Інтерпретація:** Показник ди� балан� у в книзі ордерів. Позитивне значення вказує на ти� к покупців, негативне — на ти� к продавців.
2.  **Розрахунок TFI (Trade Flow Imbalance):**
    -   **Логіка:** Аналізує потік угод, під� умовуючи об� яги угод, що відбули� я за ціною покупця (агре� ивні продажі) та ціною продавця (агре� ивні покупки).
    -   **Інтерпретація:** Показник агре� ії на ринку. Позитивне значення — домінують агре� ивні покупки, негативне — агре� ивні продажі.
3.  **Розрахунок `delta_price`:**
    -   **Логіка:** Зміна ціни за короткий проміжок ча� у.
    -   **Інтерпретація:** Показник локальної волатильно� ті та імпуль� у.
4.  **Розрахунок `absorption`:**
    -   **Логіка:** Оцінює, на� кільки ринок "поглинає" великі ринкові ордери без значної зміни ціни.
    -   **Інтерпретація:** Ви� ока аб� орбція може вказувати на наявні� ть великих лімітних ордерів, що � тримують ціну.

**Вихід:**
-   Подія `EVT:FEATURES_CALCULATED` з набором чи� лових значень (features) для конкретного ін� трументу (напр., ETHUSDT).

## 3. Фаза 2: Прийняття Рішення (Decision Making)

На цьому етапі � и� тема агрегує в� і до� тупні дані для прийняття зваженого рішення.

**Вхідні дані (в домені `DecisionMaking`):**
1.  `EVT:FEATURES_CALCULATED` (з `FeatureEngineering`)
2.  `EVT:RISK_ASSESSMENT_COMPLETED` (з `RiskManagement`)
3.  `EVT:PORTFOLIO_STATE_UPDATED` (з `PositionTracking`)
4.  `EVT:REGIME_DETECTED` (з `RegimeDetector`)

**Проце� :**
1.  **Розрахунок `signal_score`:**
    -   **Логіка:** Зважена � ума о� новних фіч (OBI, TFI, absorption). Ваги (`signal_weights`) беруть� я з конфігурації (`trading.yaml`).
    -   **Приклад:** `signal_score = (obi * 0.4) + (tfi * 0.4) + (absorption * 0.2)`
2.  **Визначення Напрямку (`side`):**
    -   Якщо `signal_score > signal_threshold` (напр., 0.2) → `side = "buy"` (LONG).
    -   Якщо `signal_score < -signal_threshold` → `side = "sell"` (SHORT).
    -   Інакше → **Рішення не приймаєть� я (нейтральний � игнал).**
3.  **Перевірка Ринкового Режиму (Regime Filter):**
    -   Си� тема перевіряє поточний ринковий режим (напр., `TREND_UP`, `TREND_DOWN`).
    -   **Логіка:** Блокують� я контртрендові � игнали. Наприклад, у режимі `TREND_UP` � игнал `side = "sell"` буде заблокований.
4.  **Розрахунок Розміру Позиції (Position Sizing):**
    -   **Базовий розмір:** Розраховуєть� я на о� нові **дробу Келлі**, � коригованого на кон� ервативні фактори (`kelly_alpha`, `kelly_conservative_factor`).
    -   **Обмеження (Caps):** Фінальний розмір позиції в USD є **мінімальним** з на� тупних значень:
        1.  Розмір, розрахований по Келлі.
        2.  Ліміт на о� нові ризику (`trade_cvar95_max_bps` від загального капіталу).
        3.  Ліміт ліквідно� ті (`liquidity_based_cap_usd`).
    -   **Перевірка мінімального розміру:** Якщо фінальний розмір в USD менший за `min_position_size_usd`, угода відхиляєть� я.
5.  **Конвертація в Кількі� ть Активу (`qty`):**
    -   **Формула:** `qty_raw = final_pos_size_usd / current_price`
    -   **Округлення:** `qty` округлюєть� я **вниз** до найближчого значення, кратного `step_size` ін� трумента (напр., 0.001 для BTC). Це робить� я за допомогою `Decimal.quantize`, щоб уникнути помилок точно� ті.
    -   **Перевірка:** Якщо пі� ля округлення `qty` дорівнює нулю, угода відхиляєть� я.
6.  **Формування Торгового Наміру (`Trade Intent`):**
    -   Якщо в� і перевірки пройдені, � и� тема генерує об'єкт `TradeIntent`.

**Вихід:**
-   Подія `EVT:TRADE_INTENT_PROPOSED` зі � труктурою, що відповідає � хемі `trade_intent.schema.json`.

## 4. Фаза 3: Виконання Ордера (Order Execution)

На цьому етапі торговий намір перетворюєть� я на конкретну команду для біржі.

**Вхідні дані:**
-   Подія `EVT:TRADE_INTENT_PROPOSED`.

**Проце� :**
1.  **Тран� формація (Bridge):**
    -   Обробник `on_trade_intent_proposed` у `main.py` перетворює аналітичний `EVT:TRADE_INTENT_PROPOSED` на виконавчу команду `CMD:OPEN`.
    -   Ключові поля (� имвол, � торона, кількі� ть, ціна) копіюють� я.
2.  **Обробка в `ExecPosFSM`:**
    -   Команда `CMD:OPEN` надходить до `OpenFlowFSM` для відповідного � имволу.
    -   **Ідемпотентні� ть:** Перевіряєть� я `idempotent_key`. Якщо команда з таким ключем вже обробляла� я нещодавно, вона відхиляєть� я.
    -   **Гарди (Guards):** Проводять� я фінальні перевірки:
        -   **Cooldown:** Чи не надто ча� то над� илають� я ордери?
        -   **Min Notional:** Чи відповідає варті� ть ордера (`qty * price`) мінімальному ліміту біржі?
3.  **Формування Ордера для Біржі:**
    -   Якщо в� і гарди пройдені, `OpenFlowFSM` генерує `DEC:OPEN`.
    -   Це повідомлення передаєть� я до **Адаптера** (`BinanceExecutionAdapter`).
    -   Адаптер форматує дані у � пецифічний для Binance API запит.

**Вихід (Типи та Формати Ордерів):**

На даний момент � и� тема генерує **один о� новний тип ордера** для входу в позицію:

-   **Тип Ордера:** `LIMIT` (Лімітний ордер)
    -   **Чому `LIMIT`?** Це дозволяє контролювати ціну входу і потенційно отримувати комі� ію "мейкера" (maker fee).
    -   **Ціна (`price`):** В� тановлюєть� я на о� нові `price_ref` з `TradeIntent`, яка, в � вою чергу, береть� я з поля `price` у `features`. Це, по � уті, о� тання відома ринкова ціна.
    -   **Ча�  в � илі (`timeInForce`):** `GTC` (Good-Till-Cancel) - ордер буде активним, доки не буде виконаний або � ка� ований.

**Формат відправки на біржу (приклад для `DEC:OPEN`):**
-   **Повідомлення в � и� темі:**
    ```json
    {
      "op": "DEC",
      "verb": "OPEN",
      "pld": {
        "symbol": "ETHUSDT",
        "side": "BUY",
        "qty": "0.123",
        "price": "4000.98",
        "order_type": "LIMIT",
        "tif": "GTC",
        "idempotent_key": "..."
      }
    }
    ```
-   **Запит до Binance API (� прощено):**
    ```
    POST /fapi/v1/order
    {
      "symbol": "ETHUSDT",
      "side": "BUY",
      "type": "LIMIT",
      "quantity": "0.123",
      "price": "4000.98",
      "timeInForce": "GTC",
      "newClientOrderId": "..."
    }
    ```

**Важливо:** Ордери для закриття позиції (Stop Loss, Take Profit), які генерують� я в `ManageFlowFSM`, також викори� товують `LIMIT` (для Take Profit) та `STOP_MARKET` (для Stop Loss) з прапором `reduceOnly=true`, щоб гарантувати, що ці ордери можуть лише закрити і� нуючу позицію, а не відкрити нову.

## 5. Ви� новок

Си� тема викори� товує багаторівневий підхід до прийняття рішень, де � ирі дані по� лідовно збагачують� я, фільтрують� я через ризик-менеджмент та ринкові режими, і лише потім перетворюють� я на конкретний, валідований **лімітний ордер**, який відправляєть� я на біржу. Кожен крок цього проце� у логуєть� я і має чіткий "причинний � лід" для подальшого аналізу.
