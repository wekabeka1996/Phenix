# 📋 ПЛАН ВИПРАВЛЕННЯ: LOG NameError

**Дата:** 3 листопада 2025
**Статус:** 📝 **PRE-CODING PLAN**
**Автор:** Copilot Research

---

## ВЕРДИКТ: РЕКОМЕНДОВАНИЙ ВАРІАНТ A

**Замінити `LOG` на `self.logger` у 5 місцях.**

**Причины:**
1. ✅ Файл уже використовує `self.logger` (лінія 198, 253, 262, ...)
2. ✅ Клас має `__init__()` який визначає `self.logger`
3. ✅ Консистентно з архітектурою класу
4. ✅ Безпечніше для тестування та многопотоковості

---

## ТОЧНИЙ ПЛАН ДІЙ (БЕЗ КОДУВАННЯ)

### КРОК 1: Перевірити структуру файлу

**Файл:** `apps/reference/domains/position_tracking/position_tracking.py`

**Що проверити:**
- [ ] Рядок 22: Глобальна `logger = logging.getLogger(__name__)` існує? ✅ (вже перевірено)
- [ ] Рядок 49: `self.logger = logging.getLogger(...)` у `__init__()`? ✅ (вже перевірено)
- [ ] Клас `PositionTracking` визначена? ✅ (лінія 40+)

**Залежність:** КРИТИЧНА — якщо немає `self.logger` у `__init__()`, план провалиться

---

### КРОК 2: Ідентифікувати всі помилкові виклики

**Потрібно замінити (5 місць):**

| № | Лінія | Поточний Код | Новий Код | Контекст |
|---|-------|-------------|----------|---------|
| 1 | 271 | `LOG.info(` | `self.logger.info(` | Синхр позицій від Binance |
| 2 | 291 | `LOG.info(` | `self.logger.info(` | Оновлення позиції |
| 3 | 296 | `LOG.info(` | `self.logger.info(` | Закриття позиції |
| 4 | 305 | `LOG.warning(` | `self.logger.warning(` | Попередження про невідому позицію |
| 5 | 308 | `LOG.info(` | `self.logger.info(` | Інформація про синхронізацію |

**Метод:** 5 окремих `replace_string_in_file()` операцій

---

### КРОК 3: Забезпечити контекст для кожної заміни

**Правило для replace_string_in_file:**
- Включити 3-5 рядків **ДО** target string
- Включити 3-5 рядків **ПІСЛЯ** target string
- Бути специфічним щоб НЕ замінити випадкові рядки

**Приклад для Лінія 271:**
```
ДО (3-5 рядків):
        # Update positions from account data
        account_positions = payload.get("positions", [])

TARGET (замінити це):
        LOG.info(
            f"📊 SYNC: Received {len(account_positions)} positions from Binance")

ПІСЛЯ (3-5 рядків):
        # Track which symbols are in Binance vs our internal state
        binance_symbols = set()
```

---

### КРОК 4: Перевірити немає інших LOG у файлі

**Операція:**
```bash
grep -n "LOG\." apps/reference/domains/position_tracking/position_tracking.py
```

**Очікуваний результат после заміни:**
- 0 (нічого не знайти)

**Если знайде ще чось:**
- [ ] Додатково замінити

---

### КРОК 5: Перевірити немає інших помилок у сусідніх методах

**Методи у `position_tracking.py`:**
- `__init__()` — ✅ OK
- `on_trade_executed()` — ✅ Перевірено (self.logger правильно)
- `on_account_update()` — 🔴 ПОМИЛКИ (5 місць)
- `on_balance_update()` — ✅ Перевірено (self.logger правильно)
- інші методи — ✅ OK

**Що робити:**
- [ ] Grep інші методи на предмет `LOG` (без self)

---

### КРОК 6: Синтаксична Валідація

**Перевірити:**
- [ ] Немає синтаксичних помилок у заміни
- [ ] Всі рядки залишаються на своєму місці (не рушимо позиції)
- [ ] Message форматування збережено

**Спосіб:**
- Візуально перевірити з читаємого файлу після заміни
- Запустити Python linter: `flake8` або `pylint`

---

### КРОК 7: Функціональна Валідація

**Unit тестування:**

**Тест 1: Перевірити, що on_account_update() працює**
```
Сценарій: Отримати EVT:ACCOUNT_UPDATE_RECEIVED
Очікування:
  - on_account_update() запускається БЕЗ NameError ✅
  - Логи виводяться (self.logger.info вмикається) ✅
  - Позиції оновлюються у self._positions ✅
```

**Тест 2: Перевірити логові повідомлення**
```
Сценарій: Отримати позицію BTCUSDT з Binance
Очікування:
  - LOG рядок "📊 SYNC: Received X positions from Binance" появляється ✅
  - LOG рядок "📈 SYNC: BTCUSDT position updated: ..." появляється ✅
```

**Тест 3: Перевірити EVT:PORTFOLIO_STATE_UPDATED**
```
Сценарій: Обробити на_account_update(), потім перевірити emitted events
Очікування:
  - EVT:PORTFOLIO_STATE_UPDATED емітується ✅
  - RID chain зберігається (Integrity OK) ✅
```

---

### КРОК 8: Інтеграційна Перевірка

**Сценарій:** Запустити систему з AccountConnector

```
1. Запустити: python -m apps.reference.main
2. Перечекати 30-60 секунд (AccountConnector опитує кожні 30с)
3. Перевірити логи:
   ✅ "📊 SYNC: Received X positions" появляється
   ✅ Нема NameError
   ✅ Позиції синхронізуються
```

---

### КРОК 9: Контроль Регресії

**Перевірити, що ІНШІ компоненти не постраждали:**

| Компонент | Перевірка | Результат |
|-----------|-----------|-----------|
| DecisionMaking | Отримує EVT:PORTFOLIO_STATE_UPDATED | ✅ OK |
| ExecutionPosition | Використовує позиції для сайзингу | ✅ OK |
| PositionTracking.on_trade_executed() | self.logger все ще працює | ✅ OK |
| PositionTracking.on_balance_update() | self.logger все ще працює | ✅ OK |

---

### КРОК 10: Final QA

| Пункт | Статус | Примітка |
|-------|--------|---------|
| Всі 5 LOG замінені на self.logger | [ ] | Перевіри вручну |
| Немає інших LOG у файлі | [ ] | Grep перевірка |
| Python синтаксис OK | [ ] | flake8/pylint |
| Unit тести проходять | [ ] | Запустити тести |
| Інтеграційна перевірка OK | [ ] | Запустити систему |
| Регресія перевірена | [ ] | Інші методи работают |

---

## ПОРЯДОК ЗАМІН (РЕКОМЕНДОВАНИЙ)

**Замінювати послідовно, щоб відслідковувати результат:**

1. Лінія 271: `LOG.info(f"📊 SYNC: ...`
2. Лінія 291: `LOG.info(f"📈 SYNC: ...`
3. Лінія 296: `LOG.info(f"📉 SYNC: ...`
4. Лінія 305: `LOG.warning(...`
5. Лінія 308: `LOG.info(...`

**Після кожної заміни:**
- Перевірити, що рядок змінився правильно
- Перевірити сусідні рядки не впали

---

## КОНТРОЛЬНИЙ СПИСОК (ПЕРЕД СТАРТОМ)

- [ ] LOG_NAMEREF_INVESTIGATION.md прочитаний (ви тут)
- [ ] ПЛАН_ВИПРАВЛЕННЯ.md готовий (цей файл)
- [ ] Всі 5 помилкових ліній ідентифіковані
- [ ] `self.logger` присутня у `__init__()` методу
- [ ] Немає глобальної `LOG` у файлі (яку забути лишити)
- [ ] Готові до 5 replace_string_in_file() операцій

---

## РИЗИКИ ТА МИТИГАЦІЯ

| Ризик | Сценарій | Рішення |
|-------|---------|---------|
| Неправильна заміна | replace_string_in_file() замінює не те | Перевірити старий та новий текст, включити контекст |
| Пропущення замін | Забути замінити одне місце | Після заміни: grep "LOG\." — повинно бути 0 |
| Синтаксична помилка | Случайно пошкодити синтаксис | Запустити flake8, перевірити Python parse |
| Регресія | Ламається інший функціонал | Перевірити: EVT:PORTFOLIO_STATE_UPDATED émis, позиції update |

---

## УСПІШНИЙ КРИТЕРІЙ

**Виправлення вважається успішним, коли:**

1. ✅ Всі 5 LOG замінені на self.logger
2. ✅ Немає NameError при on_account_update()
3. ✅ Система НЕ крашить при EVT:ACCOUNT_UPDATE_RECEIVED
4. ✅ Позиції синхронізуються від Binance (лог рядки появляються)
5. ✅ EVT:PORTFOLIO_STATE_UPDATED емітується коректно
6. ✅ Немає регресій в інших методах

---

**План готов до виконання!**

Наступні кроки:
1. Прочитати цей план
2. Перевірити всі пункти контрольного списку
3. Запустити заміни в порядку (5 операцій)
4. Запустити перевірки

**Готово? Дайте команду!**
