# SIDECAR_CLOSE_COMMAND_CONTRACT_EVIDENCE_APPENDIX

## Evidence Table

| Claim | File path | Function / class / schema / test | Exact runtime role | Evidence summary | Classification |
| --- | --- | --- | --- | --- | --- |
| Reduce-only trade intents are routed into close commands | `apps/reference/domains/execution_position/intent_router.py` | `IntentRouter.on_trade_intent_proposed` | decision-to-execution routing | reduce-only trade intent produces internal `CMD:CLOSE` payload with `symbol`, `reason`, `idempotent_key`, `retry_key`, `qty`, `trace` | runtime-proven |
| `CMD:CLOSE` becomes `DEC:CLOSE` with `reduce_only=True` | `apps/reference/domains/execution_position/fsm_close.py` | `CloseFlowFSM.handle`, `_emit_close` | close command translation | `CMD:CLOSE` emits `DEC:CLOSE`; autonomous close logic is removed; explicit command path only | runtime-proven |
| Current close executor needs `symbol` and optionally `qty` | `apps/reference/domains/execution_position/close_executor.py` | `CloseExecutor.execute_close` | imperative close execution | executor errors without `symbol`, reads live position from adapter, uses `qty` only to decide partial vs full reduce-only close | runtime-proven |
| Close execution is against current symbol position, not order/lifecycle id | `apps/reference/domains/execution_position/close_executor.py` | `CloseExecutor.execute_close` | target selection | executor re-reads `adapter.get_open_positions()` and computes close side/qty from current symbol `positionAmt` | runtime-proven |
| `idempotent_key` on close is not target selection | `apps/reference/domains/execution_position/close_executor.py` | `CloseExecutor.execute_close` | close order client id generation | `idempotent_key` contributes to generated `new_client_order_id`; no branch uses it to select what gets closed | runtime-proven |
| `CMD:CLOSE` still emits from flat | `tests/domains/execution_position/test_close_flow_cmd_close_always_emits_dec_close.py` | `test_cmd_close_always_emits_dec_close_even_when_flat` | contract proof | proves `CMD:CLOSE` emits `DEC:CLOSE` even when flat; command is not conditional on existing lifecycle match | test-proven |
| `DEC:CLOSE` must carry `reduce_only=True` | `tests/domains/execution_position/test_execpos_contract_diagnostics_v1.py` | reduce-only contract test | contract proof | asserts emitted `DEC:CLOSE` has `reduce_only=True` | test-proven |
| Reduce-only close executes market reduce-only adapter path | `tests/domains/execution_position/test_execpos_max_hold_close_executes_adapter_v1.py` | close execution test | execution proof | proves a `DEC:CLOSE` path reaches reduce-only market adapter execution | test-proven |
| Max-hold emits `DEC:CLOSE` from manage side | `apps/reference/domains/execution_position/fsm_manage.py` | `_check_max_hold_time` | time-based local close policy | manage FSM emits `DEC:CLOSE` with `reduce_only=True`, symbol, opposite side, qty, reason | runtime-proven |
| Trailing stop replacement is execution-position owned | `apps/reference/domains/execution_position/fsm_manage.py` | `_check_trailing_stop`, `_adjust_trailing_stop` | live stop management | manage FSM cancels/replaces SL orders using current lifecycle state | runtime-proven |
| Bracket fills close/reduce lifecycle locally | `apps/reference/domains/execution_position/fsm_manage.py` | `_handle_bracket_fill` | bracket truth handling | TP1/TP2/SL fills mutate lifecycle state, cancel sibling orders, and clear local state on terminal exit | runtime-proven |
| Manage FSM keeps close-in-progress state | `apps/reference/domains/execution_position/fsm_manage.py` | state fields and lifecycle methods | local lifecycle ownership | local state includes `position_qty`, entry ids, bracket ids, `_closing_position`; this is not exported as a stable public close-init event | runtime-proven |
| Duplicate close hardening is symbol/effective-state based | `apps/reference/domains/execution_position/fsm.py` | `handle` close branch, `_execute_decision` close branch | duplicate suppression | guards evaluate symbol, requested qty, and portfolio position signature; not lifecycle id | runtime-proven |
| Repeated close suppression is state-based, not lifecycle-based | `tests/domains/execution_position/test_pre_stabilization_duplicate_fill_and_repeated_close_hardening.py` | repeated close hardening tests | guardrail proof | second close is suppressed until effective position state changes | test-proven |
| `OrderIndex` is order correlation, not position truth | `apps/reference/domains/execution_position/order_index.py` | `OrderIndex`, `OrderRef` | order identity map | tracks `rid`, `idempotent_key`, `clientOrderId`, `exchangeOrderId`; no position object or executable lifecycle target | runtime-proven |
| Fill handlers derive `lifecycle_id` for observability | `apps/reference/domains/execution_position/event_handlers.py` | `on_order_fill` | fill observability | caches `idempotent_key` as `lifecycle_id` and `tradeId` by symbol for later writes | runtime-proven |
| Closed-position observability emits cached lifecycle/trade identity | `apps/reference/domains/execution_position/event_handlers.py` | `on_portfolio_state_updated` | close observability | `POSITION_CLOSED` write includes top-level `lifecycle_id`, `trade_id`, and cached side, then clears caches | runtime-proven |
| Structured identity exists in observability | `tests/integration/test_close_structured_identity.py` | structured identity integration test | observability proof | proves `POSITION_CLOSED` style output carries `lifecycle_id` / `trade_id` for downstream parsing | test-proven |
| No proven `position_id` contract | searched runtime/schemas/tests | repository-wide identity search | identity gap | no traced end-to-end position identifier usable by current close path was found | unproven |
| `OrderGuardian` distinguishes tidy from business close reconcile | `apps/reference/domains/execution_position/order_guardian.py` | `reconcile_symbol`, `_emit_tidy_events`, `_emit_close_reconciled_event` | post-close cleanup / observability | tidy events use `business_close_reconciled=false`; close reconcile event uses `business_close_reconciled=true` | runtime-proven |
| `EXECUTION_CLOSE_RECONCILED` contract is business-close aftermath, not close initiation | `apps/reference/domains/execution_position/schemas/execution_close_reconciled_v1.json` | schema | contract meaning | schema requires `business_close_reconciled=true`; it is a reconciliation event after position absence is confirmed | runtime-proven |
| `ExitManager` has force-close, tighten, and no-op branches | `apps/reference/domains/decision_making/exit_manager.py` | `ExitManager.check_exit` | Aurora post-entry exit policy | returns `(should_exit, reason, new_sl)` with explicit branch ordering | runtime-proven |
| `ExitManager` full exits become opposite-side effective signal | `apps/reference/domains/decision_making/aurora_decision.py` | exit handling block around `check_exit` | Aurora exit handoff | when `should_exit`, effective side is forced opposite current position side | runtime-proven |
| Tighten-only branch mutates emitted signal payload | `apps/reference/domains/decision_making/aurora_decision.py` | stop override block | Aurora signal shaping | `stop_loss_override` updates emitted signal `tpsl_result` / context; direct exchange-side effect not proven here | runtime-proven |
| Strategy gateway routes Aurora opposite-side exits into flip orchestration | `apps/reference/domains/decision_making/strategy_gateway.py` | signal handling / flip path | handoff from decision to execution intent | Aurora signals go through flip orchestration rather than a direct standalone close executor call | runtime-proven |
| Flip orchestration emits canonical reduce-only close | `apps/reference/domains/decision_making/flip_orchestration.py` | `emit_reduce_only_close`, `handle_flip_orchestration` | canonical Aurora exit-to-close path | derives qty from current portfolio symbol position and emits reduce-only proposed intent | runtime-proven |
| Direct `CMD:CLOSE` helper exists but is compatibility-oriented | `apps/reference/domains/decision_making/flip_orchestration.py` | `initiate_flip_close` | compatibility command path | helper docstring calls it a compatibility alternative to canonical reduce-only close path | runtime-proven |
| Direct `CMD:CLOSE` helper is not proven as Aurora hot path | `apps/reference/domains/decision_making/decision_making.py` and caller search | `_initiate_flip_close` plus search results | caller coverage gap | helper is exposed, but traced Aurora hot path uses `emit_reduce_only_close`; live caller coverage remained incomplete | unproven |
| Regime-flip close uses canonical reduce-only close path | `apps/reference/domains/decision_making/intent_emitter.py` | regime-flip enforcement branch | decision-side forced close | comments and logic route regime-flip close through canonical reduce-only close function | runtime-proven |
| Current registry gives `CMD:CLOSE` / `DEC:CLOSE` no schema | `apps/reference/dictionaries/verb_registry_v1.yaml` | verb entries | contract registry | both verbs are present with owner `execution_position` and `schema: null` | runtime-proven |
| Null schema for `DEC:CLOSE` is validated in tests | `tests/vfoundation/core/test_schema_registry.py` | schema registry check | contract proof | confirms `DEC:CLOSE` remains schema-null in registry expectations | test-proven |
| Reduce-only intent reaches internal close command then decision close | `tests/e2e/test_e2e_bridge_intent_to_execpos_dec_open.py` | E2E smoke test | end-to-end routing proof | reduce-only intent is routed to internal `CMD:CLOSE` then emits `DEC:CLOSE` | test-proven |
| Reduce-only bridge dispatch captures `CMD:CLOSE` to execution position | `tests/integration/test_bridge_dispatch_close_from_reduce_only_intent.py` | integration test | DM-to-EP bridge proof | test expects reduced-only intent bridge to pass `CMD:CLOSE` to execution position handle | test-proven |
| `ExitManager` branch ordering is deterministic | `tests/test_exit_manager.py` | exit manager tests | policy proof | tests prove danger-zone branches outrank trailing/time/signal reversal and tighten branch remains non-exit | test-proven |
| Aurora regime-flip close path is fail-closed around timing contract | `tests/decision_making/test_regime_flip_close_path_p0.py` | time-contract tests | routing semantics | reduce-only exit proposals bypass entry timing assumptions and are treated differently from normal open intents | test-proven |

## Evidence Notes

- Runtime-proven means traced directly in current code path.
- Test-proven means asserted by a test, even if the path is compatibility-oriented or narrower than the operator assumption.
- `UNPROVEN` entries are deliberate fail-closed markers, not negative claims of impossibility.
