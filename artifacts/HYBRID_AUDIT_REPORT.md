# Звіт про аудит гібридного режиму та джерел даних

## Резюме

Аудит підтвердив, що � и� тема коректно працює в гібридному режимі, викори� товуючи ринкові дані в реальному ча� і для аналізу та дані з **testnet** для управління ризиками та виконання.

**Ключовий ви� новок:** Джерелом даних про � тан портфеля (позиції, PnL, капітал) для домену `risk_management` є домен `position_tracking`. У � вою чергу, `position_tracking` отримує дані про виконані угоди від `account_observer`, який налаштований на моніторинг � ередовища **testnet**, коли `trading_mode` в� тановлено як `hybrid_live_data_testnet_exec`.

Це означає, що **управління ризиками (ліміти, про� адка) базуєть� я на даних з testnet**, що відповідає вимогам безпечної гібридної торгівлі.

## Результати

### 1. Потік даних про � тан портфеля

1.  **`AccountObserver`**:
    *   Читає глобальний `trading_mode`.
    *   О� кільки режим `hybrid_live_data_testnet_exec` не є `"live"`, він підключаєть� я до **testnet** API.
    *   Опитує виконані угоди (`get_my_trades`).
    *   Для кожної нової угоди генерує подію `EVT:TRADE_EXECUTED`.

2.  **`PositionTracking`**:
    *   Підпи� аний на `EVT:TRADE_EXECUTED`.
    *   Отримує дані про угоди з testnet.
    *   Оновлює � вою внутрішню модель позицій та PnL.
    *   Генерує подію `EVT:PORTFOLIO_STATE_UPDATED` з актуальним � таном портфеля.

3.  **`RiskManagement`**:
    *   Підпи� аний на `EVT:PORTFOLIO_STATE_UPDATED`.
    *   Отримує � тан портфеля, який базуєть� я на даних з testnet.
    *   Викори� товує ці дані для розрахунку про� адки та інших лімітів ризику.

### 2. Аналіз конфігурації

*   **`config/aurora/system.yaml`**: В� тановлює `trading_mode: "hybrid_live_data_testnet_exec"`.
*   **`config/aurora/trading.yaml`**: Розділяє режими для доменів: `market_data` працює в режимі `live`, а `execution_position` — в режимі `testnet`.
*   **`apps/reference/domains/account_observer/account_observer.py`**: Логіка вибору � ередовища (live/testnet) базуєть� я на глобальному `trading_mode`, що забезпечує отримання даних про угоди з правильного джерела (testnet).

### 3. Прогалини та ризики

*   **Неявна залежні� ть:** Поточна архітектура має неявну залежні� ть. `RiskManagement` залежить від правильного налаштування `AccountObserver`, але це не є очевидним з конфігурації. Якщо хто� ь змінить логіку в `AccountObserver`, це може непомітно змінити джерело даних для ризиків.
*   **Від� утні� ть гнучко� ті:** Неможливо легко переключити джерело даних для ризиків без зміни коду.

## Рекомендований варіант дизайну

Рекомендуєть� я варіант **C) Явне перевизначення (`explicit override`)**.

Це передбачає додавання нової � екції до конфігурації, наприклад, у `trading.yaml`:

```yaml
risk_management:
  data_sources:
    market_data: "live"  # Вже і� нує опо� ередковано
    portfolio_state: "testnet" # Новий, явний параметр
```

Це зробить конфігурацію більш прозорою, надійною та легкою для розуміння.

## План правок (для майбутнього узгодження)

Для реалізації варіанту C, необхідно буде змінити на� тупні файли:

1.  **`apps/reference/config_loader.py`**:
    *   **Мета:** Додати логіку для читання нової � екції `risk_management.data_sources`.
    *   **Приблизний diff:** Додати валідацію та пар� инг нових ключів конфігурації.

2.  **`apps/reference/domains/position_tracking/position_tracking.py`**:
    *   **Мета:** Модифікувати логіку, щоб вона могла працювати з різними джерелами даних або бути ін� танційованою для різних � ередовищ. Або, що про� тіше, переконати� я, що вона правильно реагує на події, що надходять від різних `AccountObserver`.
    *   **Приблизний diff:** Можливо, не знадобить� я змін, якщо логіка залишить� я керованою подіями.

3.  **`apps/reference/domains/account_observer/account_observer.py`**:
    *   **Мета:** Змінити логіку, щоб вона ін� танціювала� я з явною конфігурацією � ередовища (`live` або `testnet`), а не виводила її з глобального `trading_mode`.
    *   **Приблизний diff:** Змінити кон� труктор, щоб він приймав параметр `environment` і викори� товував його для налаштування клієнта `python-binance`.

4.  **`apps/reference/main.py` (або інший файл ініціалізації)**:
    *   **Мета:** На о� нові нової конфігурації ініціалізувати відповідний `AccountObserver` (live або testnet) і переконати� я, що `PositionTracking` підпи� аний на правильні події.
    *   **Приблизний diff:** Додати логіку для � творення та зв'язування компонентів відповідно до нової, явної конфігурації.
