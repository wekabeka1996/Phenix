# Neocortex Gap Matrix

Date: 2026-03-10
Scope: `apps/reference/domains/neocortex`
Baseline audit: `reports/neocortex_deep_domain_audit_2026-03-10.md`
Source priority used in this package:
1. `docs/docs_vfoundation/Constitution_FSM.md` and fail-closed / SSOT invariants
2. Current code and local runtime logs
3. Baseline audit
4. Passports and supporting docs

## Verdict

`neocortex` is not a production-grade RL trading authority. On the current branch it is only defensible as a shadow observer / latent-research / diagnostics domain after remediation. All 13 requested findings are confirmed on the current codebase.

## Findings Matrix

| ID | Finding | Status | Code evidence | Runtime / log evidence | Severity | Exact root cause | Blast radius | Production shadow impact | Must fix first | Dependent phases | Tests needed | Rollout phase |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C1 | Regime-oracle PPO semantic contamination | CONFIRMED | `apps/reference/domains/neocortex/main.py:153-160`; `apps/reference/domains/neocortex/transport/adapter.py:403-431`; `apps/reference/domains/neocortex/transport/adapter.py:749-850`; `apps/reference/domains/neocortex/logic/brain/core.py:595-625` | Oracle mode creates regime-class episodes while log replay creates BUY/SELL execution episodes and both hit the same `_completed_episodes` buffer | Critical | One PPO head and one replay buffer are used for incompatible action semantics | PPO training, telemetry, checkpoint meaning, downstream diagnostics | Shadow metrics are non-interpretable; disagreement reporting cannot be trusted | Split objectives and buffers before any further PPO tuning | P4, P5, P6, P8, P9 | New `test_objective_split.py`; extend `test_oracle_wiring.py`, `test_ppo_loop.py` with semantic isolation fixtures | P4 |
| C2 | Temporal semantics broken | CONFIRMED | `apps/reference/domains/neocortex/logic/ingest/parsers/feature_parser.py:99-115`; `apps/reference/domains/neocortex/logic/ingest/parsers/order_parser.py:83-96`; `apps/reference/domains/neocortex/logic/ingest/parsers/core_parser.py:93-99`; `apps/reference/domains/neocortex/logic/ingest/multi_tailer.py:733-746`; `apps/reference/domains/neocortex/transport/adapter.py:663-672` | Feature rows are pure JSON without event timestamps; order log uses epoch ms; core log parser returns epoch seconds; replay keys depend on replay wallclock | Critical | No canonical event time contract; parser-specific substitutes leak into state, ordering, idempotency, and stale cleanup | Every sequence, replay, lifecycle, and reward path | Production shadow replay is non-deterministic and stale cleanup is mathematically wrong | Canonical `event_ts_ms` contract and unit normalization | P1, P2, P3, P5, P6, P7, P8, P9 | New `test_time_contract.py`; extend `test_ingest.py`, `test_multi_ingest.py`, `test_replay.py` | P1 |
| C3 | Episode builder not fill-aware and not lifecycle-safe | CONFIRMED | `apps/reference/domains/neocortex/logic/ingest/multi_tailer.py:189-191`; `apps/reference/domains/neocortex/logic/ingest/multi_tailer.py:477-491`; `apps/reference/domains/neocortex/logic/ingest/multi_tailer.py:603-649` | Local logs contain overlapping order intents, rejects, cancels, fills, and closes on the same symbols; current builder can only store one pending episode per symbol | Critical | Pending episodes are keyed by symbol, anchored on `ORDER_PLACED`, and closed by symbol-only matching | Episode assembly, reward alignment, partial fill handling, same-symbol concurrency | Shadow datasets cannot claim lifecycle correctness | Add canonical lifecycle identity and shift anchor to fill/open events | P2, P3, P5, P6, P8, P9 | New `test_lifecycle_contract.py`; extend `test_tailer.py`, `test_multi_ingest.py`, `test_integration.py` with partial-fill and overlapping-symbol fixtures | P2 |
| C4 | Core-log parser does not match actual reward format | CONFIRMED | `apps/reference/domains/neocortex/logic/ingest/parsers/core_parser.py:72-75`; `apps/reference/domains/neocortex/logic/ingest/parsers/core_parser.py:124-158`; `apps/reference/domains/neocortex/logic/ingest/multi_tailer.py:612-629` | `logs/aurora_core.log` contains close information embedded inside JSON `"message"` text, not structured `realized_pnl_net`; runtime sample counts show `realized_pnl=` mentions but zero `realized_pnl_net` mentions | Critical | Parser expects structured fields that are absent in actual log format; fallback regex exists but is unused | Reward extraction, pnl-mode training, offline evaluation | Shadow domain cannot produce reliable reward completeness | Prefer structured close/reward feed; keep parser only as transitional shim with explicit completeness status | P3, P6, P8, P9 | Extend `test_reward_parsing.py`; add `test_close_feed_contract.py` and reward completeness fixtures | P3 |
| C5 | Sequence modeling inconsistent | CONFIRMED | `apps/reference/domains/neocortex/logic/brain/core.py:362-370`; `apps/reference/domains/neocortex/PPO/ppo_library_v2/ppo_system/agent.py:85-86`; `apps/reference/domains/neocortex/PPO/ppo_library_v2/ppo_system/agent.py:100-158`; `apps/reference/domains/neocortex/PPO/ppo_library_v2/ppo_system/learning/updater.py:48-99`; `apps/reference/domains/neocortex/PPO/ppo_library_v2/ppo_system/models/actor_critic_lstm.py:96-117` | Local replay is multi-symbol; current recurrent state is global and training flattens trajectories to `SeqLen=1` | Critical | Train/inference parity is broken and recurrent state is not isolated per symbol / episode | All recurrent policy behavior, world model learning, offline evaluation | Shadow sequence diagnostics are invalid; latent memory claims are overstated | Repair sequence contract after time and lifecycle are canonical | P5, P8, P9 | New `test_sequence_contract.py`; extend `test_brain.py`, `test_ppo_loop.py`, `test_full_loop.py` | P5 |
| H6 | `state_dim` validated but ignored | CONFIRMED | `apps/reference/domains/neocortex/config_models.py:634-665`; `apps/reference/domains/neocortex/logic/brain/core.py:228-232` | No agent-state inputs appear in current ingest feature list or PPO observation construction | High | Config promises richer state than implementation actually passes into PPO | PPO observation design, future advisory features, config credibility | Shadow policy cannot become agent-state-aware under current contract | Either wire `state_dim` to a declared feature contract or remove it until implemented | P4, P9 | Extend `test_config.py`; add `test_state_vector_contract.py` | P4 |
| H7 | Fail-closed / strict-config philosophy drifted into hidden defaults and degraded modes | CONFIRMED | `apps/reference/domains/neocortex/config_models.py:55-65`; `apps/reference/domains/neocortex/config_models.py:97-112`; `apps/reference/domains/neocortex/config_models.py:179-217`; `apps/reference/domains/neocortex/config_models.py:356-398`; `apps/reference/domains/neocortex/logic/brain/core.py:81-89`; `apps/reference/domains/neocortex/transport/adapter.py:1051-1063` | Domain can continue in degraded modes with silent defaults, including no-torch and bridge-start failure | High | Domain contract is not enforced strictly at runtime; degraded behavior is hidden in business paths | Startup semantics, auditability, reproducibility, live-shadow trust | Production shadow must fail closed on contract violations and surface explicit mode changes | Add explicit startup gates and move degraded behavior behind declared shadow-only flags | P0, P4, P8 | Extend `test_config.py`; add `test_startup_guards.py`, `test_degraded_mode_contract.py` | P8 |
| H8 | Relative path resolution inconsistent with documented contract | CONFIRMED | `apps/reference/domains/neocortex/config_models.py` path validation resolves relative paths against process CWD instead of domain root; `apps/reference/domains/neocortex/config/system.yaml` comments imply domain-root-relative semantics | Repro depends on where the process is launched from | High | Path contract is implicit and not normalized in one place | Config loading, replay portability, CI determinism | Shadow domain cannot guarantee reproducible replays across environments | Normalize paths against explicit root and test both repo-root and domain-root launch modes | P0, P1, P8 | Extend `test_config.py`; add `test_path_resolution_contract.py` | P0 |
| H9 | Dataset contamination by `MagicMock` artifacts | CONFIRMED | Replay ingestion currently accepts raw log rows without quarantine layer; no contamination gate exists in `multi_tailer` / dataset assembly path | `logs/order_log_v1.jsonl` contains 34 rows with `MagicMock` artifacts | High | No quarantine or provenance layer before rows become training candidates | Dataset builder, model training, evaluation credibility | Production shadow cannot claim canonical datasets yet | Add contamination quarantine and explicit exclusion manifest | P6, P8, P9 | New `test_dataset_hygiene.py`; extend `test_replay.py` with contaminated fixtures | P6 |
| H10 | Shadow-intent hot path too expensive for replay volume | CONFIRMED | `apps/reference/domains/neocortex/transport/adapter.py:657-726` performs encode, act, telemetry, JSONL write, flush, and optional settlement per observation | Feature tape volume is 2,454,196 rows; synchronous file flush per emitted shadow intent does not scale | High | Live-shadow and offline-replay code paths are not separated; too much sync work occurs on the hot path | Replay throughput, CPU budget, disk I/O budget | Production shadow can overload itself and distort observed timing | Split live-shadow mode from replay mode; batch writes and decimate logging | P7, P8 | Extend `test_backpressure.py`; add `test_shadow_hot_path_budget.py` and replay throughput harness | P7 |
| M11 | Representation is market-only, not agent-state-aware | CONFIRMED | Current ingest feature list is market-feature-only; PPO obs construction uses only VAE latent | No inventory, exposure, margin, risk-gate, or action-history state appears in current feature path | Medium | Latent space is modeling market snapshots, not agent-in-market state | Advisory evolution, execution-quality analysis, conservative offline RL | Acceptable for observer stage only; insufficient for later advisory stages | Add agent-state contract only after shadow data plane is clean | P4, P9 | New `test_agent_state_features.py` | P9 |
| M12 | Dreamer / CausalGraph are aspirational scaffolding | CONFIRMED | `apps/reference/domains/neocortex/logic/dreamer.py:209-223` uses `z_next = z` placeholder; world model is not used in action selection path | Runtime action path remains `features -> VAE latent -> PPO head -> shadow intent` | Medium | Research scaffolding exists without causal-valid dataset contracts or online planner role | World-model credibility, architecture messaging | Fine for research notes, not fine as claimed planner capability | Re-scope world model to offline diagnostics until clean sequence contracts exist | P4, P5, P9 | Extend `test_dreamer.py`; add planner-role contract tests later | P9 |
| M13 | Event contract drift from `domain.yaml` | CONFIRMED | `apps/reference/domains/neocortex/domain.yaml:29-48` exports only shadow intent, legacy shadow intent, state updated, alert; `apps/reference/domains/neocortex/transport/adapter.py:675-679` emits `EVT:NEOCORTEX_REGIME_PREDICTION` | Runtime export set differs from domain manifest | Medium | Domain manifest and runtime emitter are out of sync | Registry compliance, downstream integration, auditability | Production shadow cannot pass contract-manifest consistency gate | Align runtime events with manifest or declare new event explicitly via YAML | P0, P8 | Add `test_domain_manifest_consistency.py`; extend `test_oracle_wiring.py` | P8 |

## External Unblockers

| ID | Finding | Status | Evidence | Severity | Why it matters | Required package |
| --- | --- | --- | --- | --- | --- | --- |
| X1 | Repo-level pytest collection blocker | CONFIRMED | `apps/reference/config_models.py:3572` uses `Tuple[float, float]` without importing `Tuple`; `pytest apps/reference/domains/neocortex/tests -q` fails during collection before domain tests run | Critical | Neocortex TDD packages cannot prove green collection until the repo-level type import is fixed | `NEO-UNBLOCK-PYTEST-COLLECT-TUPLE-IMPORT` |

## Interpretation

- Audit verdict: the audit was directionally correct and understated nothing material.
- Current safe role: read-only observer, latent research, disagreement reporting, drift diagnostics.
- Current unsafe role: live policy modulation, live sizing changes, any pnl-driven RL claims, any advisory claim that depends on clean lifecycle or reward semantics.
