# Agent Arena Rules

- Agent-facing actions must be registered events or commands before use.
- Agents do not place, cancel, or close exchange orders directly.
- Arena execution is testnet-only unless an operator changes the contract in a future approved package.
- Every action must carry agent identity, session id, timestamp, rationale, and source refs when available.
- Missing instruction files are blocking evidence, not defaults.
