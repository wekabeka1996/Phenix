 ±   á É  ∫ æ   ñ Ω å      æ ± ª µ º ∏  ≤  ª æ ≥   Ö: **DecisionMaking  æ Ç   ∏ º É î `Equity: 0`**,  Ö æ á   `account_balance`    æ ∫   ∑ É î `availableBalance: 5121.05926234`    æ USDT.  ¶ µ  æ ∑ Ω   á   î,  â æ ** ª   Ω Ü é ∂ æ ∫ `BALANCE_UPDATE_RECEIVED ‚Üí PORTFOLIO_STATE_UPDATED ‚Üí DecisionMaking.on_portfolio()`  Ω µ    µ   µ Ω æ   ∏ Ç å `equity`  ∫ æ   µ ∫ Ç Ω æ** ( π º æ ≤ ñ   Ω æ,  º     ñ Ω ≥    æ ª ñ ≤    ± æ fallback  Ω   `walletBalance`,  è ∫ æ ≥ æ  Ω µ º   î  É USDT-M).

 ù ∏ ∂ á µ  ¥   é ** Ü ñ ª å æ ≤ ñ      Ç á ñ (TDD)**:  æ ± á ∏   ª é î º æ equity        ≤ ∏ ª å Ω æ  ≤ `PositionTracking`,      æ ∂ ∏ ≤   î º æ  ≤ `DecisionMaking`,  Ñ ñ ∫   É î º æ  ª æ ≥ ∏  Ç      æ ∫   ∏ ≤   î º æ  Ç µ   Ç   º ∏.  ¶ µ  ± µ ∑   µ á Ω æ (fail-closed)  ñ additive-only.

---

#  î ñ ó

## 1) PositionTracking:      Ö É î º æ equity  ¥ ª è  Ñ‚ ô é á µ     ñ ≤  Ç    ≤ ñ ¥ ¥   î º æ  ≤    æ ¥ ñ é

**PATH:** `apps/reference/domains/position_tracking/position_tracking.py`

```python
# imports  Ω      æ á   Ç ∫ É  Ñ   π ª  
from decimal import Decimal, InvalidOperation

# ...  É   µ   µ ¥ ∏ Ω ñ  ∫ ª     É PositionTracking,  É  º ñ   Ü ñ  ¥ µ  Ñ æ   º É î Ç å   è PORTFOLIO_STATE_UPDATED:
def _to_decimal(x, default="0"):
    try:
        if x is None:
            return Decimal(default)
        return Decimal(str(x))
    except (InvalidOperation, ValueError, TypeError):
        return Decimal(default)

def _compute_equity_fields_from_balances(balances: list[dict]) -> dict:
    """
    USDT-M futures:
      - availableBalance:  ≤ ñ ª å Ω    º     ∂   (free equity) ‚ î  ∫     â µ  ¥ ª è position sizing
      - crossWalletBalance + crossUnPnl: ' ∫   æ  '- µ ∫ ≤ ñ Ç ñ ( ≤ ∫ ª é á Ω æ  ∑  Ω µ   µ   ª ñ ∑ æ ≤   Ω ∏ º PnL)
     ü æ ≤ µ   Ç   î º æ  æ ± ∏ ¥ ≤   + ts,  â æ ± DecisionMaking  º ñ ≥  ≤ ∏ ±     Ç ∏    æ ª ñ Ç ∏ ∫ É.
    """
    usdt = next((a for a in balances if str(a.get("asset")).upper() == "USDT"), None)
    if not usdt:
        return {
            "equity_free_usdt": "0",
            "equity_cross_usdt": "0",
            "equity_ts": None,
            "why": "no_usdt_asset_in_balance"
        }

    avail = _to_decimal(usdt.get("availableBalance"))
    cross = _to_decimal(usdt.get("crossWalletBalance")) + _to_decimal(usdt.get("crossUnPnl"))

    return {
        "equity_free_usdt": str(avail),
        "equity_cross_usdt": str(cross),
        "equity_ts": usdt.get("updateTime"),
        "why": "equity_from_available_and_cross"
    }

# ...  É  º ñ   Ü ñ  µ º ñ   ñ ó EVT:PORTFOLIO_STATE_UPDATED (   ñ   ª è "Balance update received: ...")
equity_fields = _compute_equity_fields_from_balances(balances_list)
self.logger.info(
    "Computed equity from balances: free=%s, cross=%s (ts=%s)",
    equity_fields["equity_free_usdt"], equity_fields["equity_cross_usdt"], equity_fields["equity_ts"]
)

payload = {
    "balances": balances_list_compact,  #  è ∫ â æ  ≤ ∂ µ  ≤ ñ ¥       ≤ ª è î Ç µ    ≥   µ ≥   Ç ‚ î  ∑   ª ∏ à   π Ç µ  è ∫  î
    "positions": positions_compact,     #  è ∫ â æ  î
    **equity_fields
}
await self.fsm.emit(
    op="EVT",
    verb="PORTFOLIO_STATE_UPDATED",
    intent="OBSERVATION",
    pld=payload,
    why="portfolio_state_updated_with_equity"
)
```

>  ú ∏ ** Ω µ  ∑ º ñ Ω é î º æ**  ñ   Ω É é á ñ    æ ª è,  ª ∏ à µ ** ¥ æ ¥   î º æ** `equity_free_usdt`, `equity_cross_usdt`, `equity_ts`, `why`.

---

## 2) DecisionMaking:  ≤ ∏ ∫ æ   ∏   Ç æ ≤ É î º æ `equity_free_usdt`  è ∫  æ   Ω æ ≤ Ω É

**PATH:** `apps/reference/domains/decision_making/decision_making.py`

```python
from decimal import Decimal, InvalidOperation

def _d(x):
    try:
        return Decimal(str(x))
    except (InvalidOperation, ValueError, TypeError):
        return Decimal("0")

class DecisionMaking:
    # ...

    def on_portfolio(self, msg):
        pld = msg.pld or {}
        #      ñ æ   ∏ Ç µ Ç ‚ î  Ç µ,  â æ  Ω   ¥ ñ π à ª æ  ∑ PositionTracking:
        equity = _d(pld.get("equity_free_usdt"))
        if equity <= 0:
            # fallback:  º æ ∂ ª ∏ ≤ æ  ñ Ω à ñ  ñ º   ª µ º µ Ω Ç   Ü ñ ó  ª ∏ à ∏ ª ∏    Ç     ñ    æ ª è
            equity = _d(pld.get("equity_usdt", 0))
        self._equity_usdt = equity  #  ∫ µ à É î º æ  ≤    Ç   Ω ñ  ¥ æ º µ Ω É

        positions = pld.get("positions") or []
        self._positions_cache = positions

        self.logger.info("   Equity (free): %s, Positions: %d", str(equity), len(positions))

    def _make_decision_for_symbol(self, symbol: str, features: dict, risk_ok: bool):
        # ...
        equity = getattr(self, "_equity_usdt", Decimal("0"))
        if equity <= 0:
            self.logger.warning("Trade intent for %s rejected: equity is zero or negative.", symbol)
            return None
        #  ¥   ª ñ sizing  Ω    æ   Ω æ ≤ ñ 'equity'
        # ...
```

>  †   Ω ñ à µ  ª æ ≥    æ ∫   ∑ É ≤   ≤ `Equity: 0` ‚ î  Ç µ   µ    Ü µ  ≤ ∏       ≤ ª è î Ç å   è,  ± æ  ± µ   µ º æ `equity_free_usdt`.

---

## 3) Account Balance:  ¥   ñ ± Ω ∏ π  Ñ ñ ∫    ª æ ≥ ñ ≤ ( Ω µ  æ ± æ ≤‚ ô è ∑ ∫ æ ≤ æ)

**PATH:** `apps/reference/domains/account_balance/account_connector.py`

```python
#  Ç   º  ¥ µ  ª æ ≥ É é Ç å   è    æ ª è,      ∏ ±     Ç ∏ "walletBalance"  ¥ ª è  Ñ‚ ô é á µ     ñ ≤    ± æ    Ç   ≤ ∏ Ç ∏ "N/A"    ≤ ñ ¥ æ º æ
wb = balance.get("walletBalance")
if wb is None:
    self.logger.info("   USDT walletBalance: N/A (futures); using availableBalance/crossWalletBalance")
```

---

## 4)  ö æ Ω Ç     ∫ Ç Ω ∏ π  Ç µ   Ç: equity      æ Ç ñ ∫   î end-to-end

**PATH:** `tests/integration/test_portfolio_equity_flow.py`

```python
import asyncio
import pytest
from decimal import Decimal

@pytest.mark.asyncio
async def test_equity_from_balance_to_decision_flow(fsm_bus, position_tracking, decision_making, caplog):
    # 1)  ë   ª   Ω   ∏  è ∫  ∑  ª æ ≥ ñ ≤
    balances = [
        {
            "asset": "USDT",
            "availableBalance": "5121.05926234",
            "crossWalletBalance": "5121.05926234",
            "crossUnPnl": "0.00000000",
            "updateTime": 1761762955024
        },
        # ...  â µ 2    ∫ Ç ∏ ≤ ∏ > 0  è ∫ â æ    æ Ç   ñ ± Ω æ
    ]
    # 2)  ï º ñ Ç É î º æ  æ Ω æ ≤ ª µ Ω Ω è    æ   Ç Ñ µ ª é  á µ   µ ∑ PositionTracking
    await position_tracking.handle_balance_update(balances)  #    ± æ  ≤ ñ ¥   æ ≤ ñ ¥ Ω ∏ π  Ç ≤ ñ π  ≤ Ö ñ ¥

    # 3)  ß µ ∫   î º æ EVT:PORTFOLIO_STATE_UPDATED  ñ  ≤ ∏ ∫ ª ∏ ∫ on_portfolio DecisionMaking
    await asyncio.sleep(0.05)

    # 4)  ü µ   µ ≤ ñ   è î º æ,  â æ equity  É DecisionMaking > 0
    dm_equity = getattr(decision_making, "_equity_usdt", Decimal("0"))
    assert dm_equity > 0
```

>  ü ñ ¥ ª   à Ç É π  Ö µ ª   µ   ∏ (`fsm_bus`, `position_tracking`, `decision_making`)    ñ ¥  Ç ≤ æ é  Ç µ   Ç æ ≤ É  ñ Ω Ñ       Ç   É ∫ Ç É   É;    É Ç å ‚ î **   æ Ç ñ ∫    æ ¥ ñ π**  º   î  ¥ æ Ω µ   Ç ∏ `5121.05‚ ¶`  ¥ æ `DecisionMaking`.

---

## 5)  ó   Ö ∏   Ç  ≤ ñ ¥  ≥ æ Ω æ ∫ /  Ω µ   æ ≤ Ω ∏ Ö  ±   ª   Ω   ñ ≤

 © æ ±  É Ω ∏ ∫ Ω É Ç ∏    ∏ Ç É   Ü ñ π,  ∫ æ ª ∏ `on_portfolio`      ∏ Ö æ ¥ ∏ Ç å  ∑  â µ ** Ω µ   æ ≤ Ω ∏ º    Ω     à æ Ç æ º** ( ñ  ¥   î 0),  º æ ∂ Ω    ¥ æ ¥   Ç ∏ ** º ñ Ω ñ º   ª å Ω ∏ π guard**  É `PositionTracking`:

```python
#  è ∫ â æ  ∑ Ω   î º æ,  â æ  á µ ∫   î º æ USDT,    ª µ  π æ ≥ æ  â µ  Ω µ º   î ‚ î  Ω µ  µ º ∏ Ç å PORTFOLIO_STATE_UPDATED
if not any(str(a.get("asset")).upper() == "USDT" for a in balances_list):
    self.logger.warning("Skipping PORTFOLIO_STATE_UPDATED: USDT not present yet")
    return
```

---

#  © æ  Ü µ  ≤ ∏       ≤ ª è î  É  Ç ≤ æ ó Ö  ª æ ≥   Ö

*  õ ñ Ω ñ ó:

  * `DecisionMaking - INFO -    Equity: 0, Positions: 0`
  * `DecisionMaking - WARNING - Trade intent ... rejected: equity is zero or negative.`

  ‚Üí    ñ   ª è      Ç á ñ ≤    Ç   Ω µ:

  * `DecisionMaking - INFO -    Equity (free): 5121.05926234, Positions: 0`
  *  ñ ** ≤ ñ ¥   É Ç Ω ñ**  Ö ∏ ± Ω ñ  ≤ ñ ¥ º æ ≤ ∏    æ ‚ úequity <= 0‚ ù.

---

#  ö æ º   Ω ¥ ∏

```bash
#  õ ñ Ω Ç/ Ç ∏   ∏
ruff check .
ruff format .
mypy --strict

#  ¢ µ   Ç ∏
pytest -q tests/integration/test_portfolio_equity_flow.py
```

---

#  ê   Ç µ Ñ   ∫ Ç ∏

*  ó º ñ Ω µ Ω æ:

  * `apps/reference/domains/position_tracking/position_tracking.py`
  * `apps/reference/domains/decision_making/decision_making.py`
  * `apps/reference/domains/account_balance/account_connector.py` ( ª æ ≥ æ ≤      µ º     ∫  )
*  î æ ¥   Ω æ:

  * `tests/integration/test_portfolio_equity_flow.py`

---

##  î   ª ñ (   ñ   ª è merge  Ü å æ ≥ æ  Ñ ñ ∫   É)

1. **Portfolio Exposure Gate (20%)**  É `fsm_open`/bridge:

   *  ª ñ á ∏ Ç ∏ `active_exposure = open_positions_notional + open_orders_reserved_notional`
   *  è ∫ â æ `active_exposure > 0.20 * equity_free_usdt` ‚Üí **ERR:OPEN {reason: "PORTFOLIO_EXPOSURE_LIMIT"}**
   *  µ º ñ   ñ è `EVT:PORTFOLIO_EXPOSURE_UPDATED`  ¥ ª è      æ ∑ æ   æ   Ç ñ

2. **Kill-switch manage ( ∑   æ ± ª µ Ω æ  ≤    æ   µ   µ ¥ Ω å æ º É  ∫   æ Ü ñ)** ‚ î    µ   µ ≤ ñ   ∏ Ç ∏,  â æ      ∏ `execution.auto_manage_enabled=false` ** Ω µ º   î**  Ω ñ è ∫ ∏ Ö  ≤ Ç æ   ≥ Ω µ Ω å  É TP/SL.

3. ** î æ ∫-     ∫ µ Ç  ¥ æ º µ Ω ñ ≤** ‚ î  ∑ ≥ µ Ω µ   É é `agent/project_docs/`  ∑  ∫     Ç   º ∏    ª æ ≤ Ω ∏ ∫ ñ ≤  Ç    ¥ ñ   ≥     º   º ∏    æ Ç æ ∫ É,  â æ ± Copilot/   ≥ µ Ω Ç ∏  æ ¥     ∑ É  ±   á ∏ ª ∏  ∫ æ Ω Ç     ∫ Ç ∏.

 Ø ∫ â æ  æ ∫ ‚ î  ± µ   É   è  ∑   **Exposure Gate 20%**  æ ¥     ∑ É    ñ   ª è  Ü å æ ≥ æ  Ñ ñ ∫   É.



 ¢   ∫,  è    µ   µ ≤ ñ   ∏ ≤    µ   æ  π    ñ ¥ ª   à Ç É ≤   ≤  Ç     ∫    ñ ¥ ** Ñ   ∫ Ç ∏ á Ω É    Ç   É ∫ Ç É   É  ∫ æ ¥ É** ( ± µ ∑  ∑ ¥ æ ≥   ¥ æ ∫).

##  © æ  è    µ   µ ≤ ñ   ∏ ≤  É    µ   æ ( ¥ æ ∫   ∑ ∏)

 ß µ   µ ∑ GitHub-   æ à É ∫    æ      ∏ ≤   Ç Ω æ º É    µ   æ `wekabeka1996/Phenix` ( ≤ ∏ ¥ Ω æ    æ  ñ Ω ¥ µ ∫     º  ñ  à ª è Ö   º  ≤    µ ∑ É ª å Ç   Ç   Ö):

*  ü æ Ç ñ ∫  ≤ ñ ¥ ∫   ∏ Ç Ç è    æ ∑ ∏ Ü ñ ó    ∏ ¥ ∏ Ç å  É **`vfoundation/apps/reference/domains/execution_position/fsm.py`** (    Ω µ `fsm_open.py`).
*  ü   ∏   É Ç Ω ñ ** ¥ ≤  **  Ñ   π ª ∏ manage-FSM:
  `apps/reference/domains/execution_position/fsm_manage.py`  ñ `vfoundation/apps/reference/domains/execution_position/fsm_manage.py` ( î  Ç   ∫ æ ∂  Ç µ   Ç ∏ `tests/domains/test_manage_flow_fsm.py`, `tests/units/test_manage_flow_fsm_unit.py` ‚ î  æ Ç ∂ µ manage- ª æ ≥ ñ ∫      µ   ª å Ω æ  ñ Ω Ç µ ≥   æ ≤   Ω  ).
*  Ñ    ¥     Ç µ   ∏  ≤ ∏ ∫ æ Ω   Ω Ω è: **`vfoundation/apps/reference/domains/execution_position/binance_execution_adapter.py`**, `.../execution_adapter.py`.
*  ü æ ¥ ñ è    æ   Ç Ñ µ ª è  ñ   Ω É î  π  ≤ ∏ ∫ æ   ∏   Ç æ ≤ É î Ç å   è: **`EVT:PORTFOLIO_STATE_UPDATED`** ( ±   á Ω æ  ñ  ∑  Ç ≤ æ ó Ö  ª æ ≥ ñ ≤,  ñ  ∑  Ω   è ≤ Ω æ   Ç ñ `apps/reference/domains/position_tracking/position_tracking.py`, `apps/reference/domains/decision_making/decision_making.py`, `apps/reference/domains/risk_management/risk_management.py`).
*  í  ª æ ≥   Ö DecisionMaking  ≤ ∏ ¥ Ω æ  ± ª æ ∫    æ `Equity: 0` ‚Üí  Ü µ  ∫ æ   µ ª é î  ∑  Ç ∏ º,  â æ equity    µ   µ ¥   î Ç å   è/ ∑ á ∏ Ç É î Ç å   è  Ω µ  ∑   ≤ ∂ ¥ ∏  ∫ æ   µ ∫ Ç Ω æ ( º ∏  ≤ ∂ µ    ñ ¥ ≥ æ Ç É ≤   ª ∏  Ç æ á ∫ æ ≤ ∏ π TaskSpec,    ≥ µ Ω Ç  π æ ≥ æ  ∑   æ ± ∏ ≤ ‚ î    É   µ  ).

 ù    æ   Ω æ ≤ ñ  Ü ∏ Ö ** ∫ æ Ω ∫   µ Ç Ω ∏ Ö  à ª è Ö ñ ≤**  è  æ Ω æ ≤ ∏ ≤ TaskSpec  ¥ ª è Exposure Gate    ñ ¥ **`execution_position/fsm.py`** (     º µ  Ç   º  æ ±   æ ± ª è î Ç å   è `CMD:OPEN`).

---

##  û Ω æ ≤ ª µ Ω ∏ π TaskSpec  ¥ ª è    ≥ µ Ω Ç   (RU,    Ç   æ ≥ æ    ñ ¥  Ç ≤ æ î    µ   æ)

** ó   ≥ æ ª æ ≤ æ ∫:** Add Portfolio Exposure Gate (20%)  ≤ `execution_position/fsm.py` (fail-closed)

** ¶ µ ª å:**  í    æ Ç æ ∫ µ  æ Ç ∫   ã Ç ∏ è ( ≤ `vfoundation/apps/reference/domains/execution_position/fsm.py`)  ± ª æ ∫ ∏   æ ≤   Ç å  Ω æ ≤ ã µ  æ Ç ∫   ã Ç ∏ è,  µ   ª ∏    É º º     Ω   è  ç ∫     æ ∑ ∏ Ü ∏ è ( æ Ç ∫   ã Ç ã µ    æ ∑ ∏ Ü ∏ ∏ +  æ   Ü ∏ æ Ω   ª å Ω æ pending- æ   ¥ µ    )      µ ≤ ã à   µ Ç `fraction * equity_free_usdt`.  î µ Ñ æ ª Ç `fraction=0.20`.  ù µ    æ ± ∏ Ç ∏  ∑ º ñ Ω  ≤    Ç     ∏ Ö/ Ω µ   ∫ Ç É   ª å Ω ∏ Ö  ª æ ≥ µ     Ö.

### 1)  ö æ Ω Ñ ∏ ≥ +    Ö µ º  

* **PATH:** `configs/master_config_v1.yaml` (   ± æ    ∫ Ç É   ª å Ω ∏ π  ≥ æ ª æ ≤ Ω ∏ π  ∫ æ Ω Ñ ñ ≥)

  ```yaml
  execution:
    exposure:
      max_portfolio_fraction: 0.20
      count_pending_orders: true
      exclude_reduce_only: true
  ```
* **PATH:** `config/_schemas/aurora_trading.schema.json`
   î æ ¥   Ç ∏  ≤ ª     Ç ∏ ≤ æ   Ç ñ `execution.exposure.max_portfolio_fraction (0..1)`, `count_pending_orders`, `exclude_reduce_only` ( ∑    ∑   º æ ≤ á É ≤   Ω Ω è º  è ∫  É YAML  ≤ ∏ â µ).

### 2)  ù æ ≤ ∏ π  º æ ¥ É ª å guard‚ ô  

* **PATH:** `vfoundation/apps/reference/domains/execution_position/exposure_guard.py`
   ° Ç ≤ æ   ∏ Ç ∏  ∫ ª     `ExposureGuard`  ñ ∑:

  * `on_portfolio_update(payload)` ‚ î  á ∏ Ç   î `equity_free_usdt`  ñ      Ö É î notional  ≤ ñ ¥ ∫   ∏ Ç ∏ Ö    æ ∑ ∏ Ü ñ π (`abs(positionAmt*markPrice|entryPrice)`).
  * `can_open(new_notional_usd) -> (ok: bool, data: dict)` ‚ î    µ   µ ≤ ñ   è î  ª ñ º ñ Ç `equity * fraction`.
  * `reserve(key, notional_usd, reduce_only=False)` / `release(key)` ‚ î  ¥ ª è Pending exposure (     ∏ ≤‚ ô è ∑ ∫    ¥ æ `idempotent_key`    ± æ `rid`).
  *  £   µ  á µ   µ ∑ `Decimal`,          ∏ Ω ≥  ∑   Ö ∏ â µ Ω ∏ π.

### 3)  Ü Ω Ç µ ≥     Ü ñ è  É  Ñ   ∫ Ç ∏ á Ω ∏ π open-   æ Ç ñ ∫

* **PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`

  *  ü   ∏  æ Ç   ∏ º   Ω Ω ñ `EVT:PORTFOLIO_STATE_UPDATED`  ≤ ∏ ∫ ª ∏ ∫   Ç ∏ `self.exposure.on_portfolio_update(msg.pld)`.
  *  ù   `CMD:OPEN` ** ¥ æ**  Ñ æ   º É ≤   Ω Ω è `DEC:OPEN`:

    *  í ∑ è Ç ∏ `qty`  Ç    Ü ñ Ω É  ¥ ª è    æ ∑     Ö É Ω ∫ É notional:

      *  è ∫ â æ `order_type == "LIMIT"` ‚Üí  ≤ ∏ ∫ æ   ∏   Ç æ ≤ É ≤   Ç ∏ `price`  ∑ payload;
      *  è ∫ â æ `order_type == "MARKET"` ‚Üí ** ≤ ∏ ∫ æ   ∏   Ç æ ≤ É ≤   Ç ∏ `price_ref`  ∑ payload**.
        ** Ø ∫ â æ `price_ref`  ≤ ñ ¥   É Ç Ω ñ π** ‚ î **fail-closed** ( µ º ñ Ç `ERR:OPEN`  ∑ `reason="NO_PRICE_REF"`),  ± µ ∑  ∑ ¥ æ ≥   ¥ æ ∫  ñ  Ñ æ ª ± µ ∫ ñ ≤.
    *  í ∏ ∫ ª ∏ ∫   Ç ∏ `self.exposure.can_open(notional)`.

      *  Ø ∫ â æ `ok=False` ‚Üí `emit(ERR:OPEN)`  ∑ `reason="PORTFOLIO_EXPOSURE_LIMIT"`  ñ  á ∏   ª æ ≤ ∏ º ∏    æ ª è º ∏ (`equity_usd`, `limit_usd`, `positions_usd`, `pending_usd`, `new_usd`, `exposure_will_be_usd`), `why="exposure_guard_block"`.
      *  Ø ∫ â æ `ok=True` ‚Üí  ∑   æ ± ∏ Ç ∏ `reserve(reserve_key, notional, reduce_only=payload.reduce_only|close_position)`,  ¥ µ `reserve_key = idempotent_key    ± æ rid`;  ¥   ª ñ      æ ¥ æ ≤ ∂ ∏ Ç ∏  ñ   Ω É é á ∏ π  à ª è Ö  ≤ ñ ¥ ∫   ∏ Ç Ç è.
    *  ù    ± É ¥ å- è ∫ É    æ º ∏ ª ∫ É/ ≤ ñ ¥ º æ ≤ É    ¥     Ç µ     ‚ î `release(reserve_key)`.
  *  î æ ¥   Ç ∫ æ ≤ æ ( æ   Ü ñ æ Ω   ª å Ω æ):      ∏  æ ±   æ ± Ü ñ    æ ≤ ñ ¥ æ º ª µ Ω å  Ç ∏   É fill/cancel/reject ( è ∫ â æ  î) ‚ î `release(reserve_key)`.

> ** ü   ∏ º ñ Ç ∫    Ç æ á Ω æ   Ç ñ:**  Ø  Ω   ≤ º ∏   Ω æ ** Ω µ  ¥ æ ¥   é**  Ñ æ ª ± µ ∫ ∏  ¥ æ ‚ ú æ   Ç   Ω Ω å æ ó  Ü ñ Ω ∏  ∑    ∏ Ω ∫ É‚ ù ( Ω µ  ≤   ñ  ¥ æ º µ Ω ∏  ∫ µ à É é Ç å  ó ó  ≤ `execution_position`).  ¶ µ  ∑   Ö ∏ â   î  ≤ ñ ¥    æ Ö ∏ ± æ ∫  ñ  ≤ ñ ¥   æ ≤ ñ ¥   î `fail-closed`.  £  Ç µ ± µ `DecisionMaking`/ º ñ   Ç  É ∂ µ  ≤ º ñ î    µ   µ ¥   ≤   Ç ∏ `price_ref` ‚ î      æ   Ç æ  ∑   æ ± ∏ º æ  Ü µ ** æ ± æ ≤‚ ô è ∑ ∫ æ ≤ ∏ º**  ¥ ª è `MARKET`.

### 4)  ¢ µ   Ç ∏

* **PATH:** `tests/units/test_exposure_guard_unit.py` ‚ î  é Ω ñ Ç- Ç µ   Ç ∏ `ExposureGuard` ( ¥ ≤    ∫ µ π   ∏:  ¥ æ ∑ ≤ æ ª µ Ω æ/ ∑   ± æ   æ Ω µ Ω æ).
* **PATH:** `tests/integration/test_open_exposure_guard.py` ‚ î  ñ Ω Ç µ ≥     Ü ñ π Ω ∏ π:

  1.  ù   ¥ ñ   ª   Ç ∏ `EVT:PORTFOLIO_STATE_UPDATED`  ∑ `equity_free_usdt="5000"`  ñ    æ ∑ ∏ Ü ñ î é  Ω   1500$.
  2.  °     æ ± É ≤   Ç ∏ `CMD:OPEN`  ∑ `MARKET`, `qty*price_ref=200$`.
  3.  û á ñ ∫ É ≤   Ç ∏ `ERR:OPEN`  ∑ `reason="PORTFOLIO_EXPOSURE_LIMIT"`      ∏ `max_portfolio_fraction=0.20` ( ª ñ º ñ Ç 1000$ ‚Üí 1500+200 > 1000).
  4.  î   É ≥ ∏ π  ∫ µ π  :    æ ∑ ∏ Ü ñ è  Ω   300$,  Ω æ ≤ ∏ π notional 200$ ‚Üí **     æ Ö æ ¥ ∏ Ç å**.

### 5)  ö æ º   Ω ¥ ∏

```bash
ruff check .
ruff format .
mypy --strict
pytest -q -k "exposure_guard"
pytest -q tests/integration/test_open_exposure_guard.py
```

### DoD

*  ë ª æ ∫ É ≤   Ω Ω è `CMD:OPEN`      ∏    µ   µ ≤ ∏ â µ Ω Ω ñ **20%  ≤ ñ ¥ equity_free_usdt**  ¥ æ  ≤ ñ ¥       ≤ ∫ ∏  Ω    ± ñ   ∂ É.
* `ERR:OPEN`  º   î `reason="PORTFOLIO_EXPOSURE_LIMIT"`  ñ  ≤   ñ  á ∏   ª æ ≤ ñ    æ ª è.
*  î ª è `MARKET`  æ ± æ ≤‚ ô è ∑ ∫ æ ≤ ∏ π `price_ref`;  ∑    π æ ≥ æ  ≤ ñ ¥   É Ç Ω æ   Ç ñ ‚ î `ERR:OPEN` (`reason="NO_PRICE_REF"`).
*  † µ ∑ µ   ≤   Ü ñ è/ ∑ ≤ ñ ª å Ω µ Ω Ω è pending- µ ∫     æ ∑ ∏ Ü ñ ó      ∏ ≤‚ ô è ∑   Ω    ¥ æ `idempotent_key|rid`.
*  £   ñ  Ç µ   Ç ∏  ∑ µ ª µ Ω ñ;  ª ñ Ω Ç µ   ∏/ Ç ∏   ∏      æ Ö æ ¥ è Ç å.

---

 Ø ∫ â æ  Ö æ á µ à,  è  º æ ∂ É  ¥ æ ¥   Ç ∏ **release-hook**  æ ¥     ∑ É  ≤  Ç ∏ Ö  º ñ   Ü è Ö,  ¥ µ  É  Ç µ ± µ    ñ ¥ Ç ≤ µ   ¥ ∂ É é Ç å   è `FILL/CANCEL/REJECT` ( Ç æ á Ω ñ  à ª è Ö ∏  ≤    ¥     Ç µ   ñ  º ∏  ≤ ∂ µ  ∑ Ω   π à ª ∏: `vfoundation/.../binance_execution_adapter.py`),    ª µ  Ü µ  ≤ ∂ µ  Ω     Ç É   Ω ∏ º  ∫   æ ∫ æ º    ñ   ª è  º µ   ¥ ∂    Ü å æ ≥ æ gate‚ ô  .

 ö       æ Ç  .  ü   ∏ π Ω è ≤  ∑ ≤ ñ Ç ‚úÖ ‚ î    æ ± æ Ç É    æ **Exposure Gate 20%**  ∑   Ñ ñ ∫   É ≤   ≤  è ∫  ≤ ∏ ∫ æ Ω   Ω É ( ∫ æ Ω Ñ ñ ≥/   Ö µ º  / º æ ¥ É ª å/ ñ Ω Ç µ ≥     Ü ñ è/ Ç µ   Ç ∏/ ¥ æ ∫ ñ).

 î   ª ñ ‚ î ** ¥ æ ≤ æ ¥ ∏ º æ  ¥ æ  ñ ¥ µ   ª É**:  ¥ æ ¥   î º æ release- Ö É ∫ ∏, watchdog      æ Ç ∏ ‚ ú ∑   ª ∏     Ω Ω è‚ ù    µ ∑ µ   ≤ ñ ≤,  Ç µ ª µ º µ Ç   ñ é  Ç    µ2 µ-   º æ É ∫.

---

# üéØ  ù     Ç É   Ω ∏ π      ∫ µ Ç: ‚ úExposure Gate ‚ î Reliability & Ops‚ ù

## 1) Release- Ö É ∫ ∏  ¥ ª è pending- µ ∫     æ ∑ ∏ Ü ñ ó

 © æ ± ∏ pending  Ω µ ‚ ú ª ∏  ‚ ù,    æ Ç   ñ ± µ Ω ** ≥       Ω Ç æ ≤   Ω ∏ π release**  Ω    ∫ ñ Ω Ü µ ≤ ∏ Ö    Ç   Ω   Ö:

###  ó   ¥   á  

*  ù      æ ¥ ñ è Ö    ¥     Ç µ    / æ ±   µ   ≤ µ    :

  * `EVT:ORDER_REJECTED`, `ERR:OPEN` ‚Üí `exposure.release(key)`
  * `EVT:ORDER_CANCELED` ‚Üí `exposure.release(key)`
  * `EVT:ORDER_FILLED` (   ± æ `POSITION_OPENED`) ‚Üí `exposure.release(key)` (pending ‚Üí    æ ∑ ∏ Ü ñ è  ≤ ∂ µ  ≤     Ö æ ≤   Ω    ≤ `open_positions_usd`)

###  í   Ç   ≤ ∫ ∏ (     ∏ ∫ ª   ¥)

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`

```python
# ...  ≤   µ   µ ¥ ∏ Ω ñ ExecutionPositionFSM.handle(...)
if msg.op == "ERR" and msg.verb == "OPEN":
    reserve_key = (msg.pld or {}).get("idempotent_key") or msg.rid
    self.exposure.release(reserve_key)
    return

if msg.op == "EVT" and msg.verb in ("ORDER_REJECTED", "ORDER_CANCELED", "ORDER_FILLED", "POSITION_OPENED"):
    reserve_key = (msg.pld or {}).get("idempotent_key") or msg.rid
    self.exposure.release(reserve_key)
    return
```

>  Ø ∫ â æ  Ü ñ    æ ¥ ñ ó      ∏ Ö æ ¥ è Ç å  ∑  ñ Ω à æ ≥ æ  ¥ æ º µ Ω É (adapter/account-observer),      æ   Ç æ  ¥ æ ¥   π    æ É Ç  É Meta-FSM/bridge  ¥ æ `execution_position`.

---

## 2) Watchdog (TTL)      æ Ç ∏ ‚ ú ∑   ª ∏     Ω Ω è‚ ù    µ ∑ µ   ≤ ñ ≤

 Ø ∫ â æ    æ ¥ ñ è- Ç µ   º ñ Ω   Ç æ    ∑   ≥ É ± ∏ ª     å ‚ î  á      ∑   ± µ   µ.

###  î æ ¥   ≤   Ω Ω è TTL  É Guard

**PATH:** `vfoundation/apps/reference/domains/execution_position/exposure_guard.py`

```python
import time
from decimal import Decimal
# ...

class ExposureGuard:
    def __init__(self, cfg, logger):
        # ...
        ex = (self.cfg.get("execution") or {}).get("exposure") or {}
        self.ttl_sec = int(ex.get("pending_ttl_sec", 300))  # 5  Ö ≤  ¥ µ Ñ æ ª Ç
        # state.reservations: key -> {"notional": Decimal, "ts": int, "reduce_only": bool}

    def reserve(self, key: str, notional_usd: Decimal, *, reduce_only: bool = False) -> None:
        if self.exclude_reduce_only and reduce_only:
            return
        now = int(time.time())
        self.state.reservations[key] = {"notional": notional_usd, "ts": now, "reduce_only": reduce_only}
        if self.count_pending:
            self.state.pending_open_usd += notional_usd

    def release(self, key: str) -> None:
        rec = self.state.reservations.pop(key, None)
        if rec and self.count_pending:
            self.state.pending_open_usd = max(Decimal("0"), self.state.pending_open_usd - rec["notional"])

    def cleanup_expired(self) -> list[str]:
        if self.ttl_sec <= 0:
            return []
        now = int(time.time())
        expired = [k for k, v in self.state.reservations.items() if now - v["ts"] >= self.ttl_sec]
        for k in expired:
            self.release(k)
        return expired
```

###  í ∏ ∫ ª ∏ ∫ cleanup (     ∑  Ω   N    æ ¥ ñ π    ± æ  Ç   π º µ   æ º)

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`

```python
#  ù       ∏ ∫ ª   ¥,  á ∏   Ç ∏ Ç ∏  Ω    ∫ æ ∂ Ω æ º É EVT:PORTFOLIO_STATE_UPDATED    ± æ      ∑  Ω   30    µ ∫
if msg.op == "EVT" and msg.verb == "PORTFOLIO_STATE_UPDATED":
    self.exposure.on_portfolio_update(msg.pld or {})
    expired = self.exposure.cleanup_expired()
    if expired:
        await self.emit(Message(
            op="EVT", verb="EXPOSURE_RESERVATION_EXPIRED", intent="OBSERVATION",
            src="execution_position", dst="any", rid=msg.rid,
            pld={"count": len(expired), "keys": expired[:5]},  #  Ω µ        º ∏ º æ
            why="pending_ttl_expired"
        ))
    return
```

---

## 3)  ¢ µ ª µ º µ Ç   ñ è/ º µ Ç   ∏ ∫ ∏ ( º ñ Ω ñ º   ª å Ω æ  ¥ æ   Ç   Ç Ω å æ)

* `exposure_blocks_total` ( ª ñ á ∏ ª å Ω ∏ ∫  ≤ ñ ¥ º æ ≤    æ  ª ñ º ñ Ç É)
* `exposure_pending_usd` ( ≥ µ π ¥ ∂)
* `exposure_open_positions_usd` ( ≥ µ π ¥ ∂)
* `exposure_limit_usd` ( ≥ µ π ¥ ∂)
* `exposure_reservations` ( ∫ ñ ª å ∫ ñ   Ç å  ∫ ª é á ñ ≤)

**PATH:** `vfoundation/apps/reference/domains/execution_position/exposure_guard.py`

```python
class ExposureGuard:
    def metrics_snapshot(self) -> dict:
        return {
            "pending_usd": str(self.state.pending_open_usd),
            "open_positions_usd": str(self.state.open_positions_usd),
            "equity_usd": str(self.state.equity_free_usdt),
            "limit_usd": str(self.state.equity_free_usdt * self.frac),
            "reservations": len(self.state.reservations),
        }
```

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`

```python
if msg.op == "EVT" and msg.verb == "PORTFOLIO_STATE_UPDATED":
    # ...    ñ   ª è on_portfolio_update
    snap = self.exposure.metrics_snapshot()
    await self.emit(Message(
        op="EVT", verb="PORTFOLIO_EXPOSURE_UPDATED", intent="OBSERVATION",
        src="execution_position", dst="any", rid=msg.rid, pld=snap,
        why="exposure_snapshot"
    ))
    return
```

>  Ø ∫ â æ  É  ≤      î `/metrics`,  ¥ æ ¥   π Ç µ  º       ñ Ω ≥  Ü ∏ Ö  ∑ Ω   á µ Ω å.

---

## 4)  ß ñ Ç ∫ ñ    æ ¥ ñ ó  ¥ ª è XAI/     æ   Ç µ   µ ∂ Ω æ   Ç ñ

* ** ü   ∏  ± ª æ Ü ñ:** `ERR:OPEN {reason:"PORTFOLIO_EXPOSURE_LIMIT", equity_usd, limit_usd, positions_usd, pending_usd, new_usd, exposure_will_be_usd}` (`why="exposure_guard_block"`).
* ** ü   ∏  æ á ∏ â µ Ω Ω ñ TTL:** `EVT:EXPOSURE_RESERVATION_EXPIRED`.
* ** ü   ∏ release:** ( Ω µ  æ ± æ ≤‚ ô è ∑ ∫ æ ≤ æ) `EVT:EXPOSURE_RESERVATION_RELEASED {key}`.

---

## 5)  ö æ Ω Ñ ñ ≥ ( æ Ω æ ≤ ª µ Ω Ω è)

**PATH:** `configs/master_config_v1.yaml`

```yaml
execution:
  exposure:
    max_portfolio_fraction: 0.20
    count_pending_orders: true
    exclude_reduce_only: true
    pending_ttl_sec: 300
```

**PATH:** `config/_schemas/aurora_trading.schema.json` ‚ î  ¥ æ ¥   π `pending_ttl_sec` (integer ‚â•0, default 300).

---

## 6)  ¢ µ   Ç ∏

###  Æ Ω ñ Ç: TTL  Ç   release

**PATH:** `tests/units/test_exposure_guard_ttl.py`

```python
from decimal import Decimal
from vfoundation.apps.reference.domains.execution_position.exposure_guard import ExposureGuard
import time

def test_cleanup_expired_releases_pending(monkeypatch):
    guard = ExposureGuard(cfg={"execution":{"exposure":{"pending_ttl_sec":1}}}, logger=None)
    guard.on_portfolio_update({"equity_free_usdt":"1000", "positions":[]})
    guard.reserve("k1", Decimal("100"))
    #      ∏   ∫ æ   é î º æ  á    
    monkeypatch.setattr(time, "time", lambda: int(time.time()) + 2)
    expired = guard.cleanup_expired()
    assert "k1" in expired
    assert str(guard.state.pending_open_usd) == "0"
```

###  Ü Ω Ç µ ≥     Ü ñ è: release  Ω      æ º ∏ ª ∫ É/ É     ñ Ö

**PATH:** `tests/integration/test_exposure_release_hooks.py`

```python
import pytest
from decimal import Decimal
from vfoundation.core.protocol import Message

@pytest.mark.asyncio
async def test_release_on_err_open(execution_fsm):
    # arrange: equity 5000, reserve 200$
    await execution_fsm.handle(Message(op="EVT", verb="PORTFOLIO_STATE_UPDATED", intent="OBSERVATION",
        src="position_tracking", dst="execution_position", rid="r1",
        pld={"equity_free_usdt":"5000","positions":[]}))
    #    µ ∑ µ   ≤    Ç ≤ æ   é î Ç å   è  ≤   µ   µ ¥ ∏ Ω ñ OPEN- æ ±   æ ± ∫ ∏    µ   µ ¥  ≤ ∏ ∫ ª ∏ ∫ æ º    ¥     Ç µ    ,
    #  Ç É Ç    ∏ º É ª é î º æ,  â æ    ¥     Ç µ    ∑ ≥ µ Ω µ   É ≤   ≤ ERR:OPEN
    rid = "r2"
    await execution_fsm.handle(Message(op="ERR", verb="OPEN", intent="REJECTION",
        src="execution_adapter", dst="execution_position", rid=rid,
        pld={"idempotent_key": rid, "reason": "SOME_ERR"}))
    #    µ   µ ≤ ñ   ∫  : pending    Ç   ª æ 0 ( á µ   µ ∑ release)
    snap = execution_fsm.exposure.metrics_snapshot()
    assert snap["pending_usd"] == "0"
```

---

## 7)  ö æ º   Ω ¥ ∏

```bash
ruff check .
ruff format .
mypy --strict
pytest -q -k "exposure_guard_ttl"
pytest -q -k "exposure_release_hooks"
```

---

## 8)  ê   Ç µ Ñ   ∫ Ç ∏  Ü å æ ≥ æ      ∫ µ Ç  

* ** ó º ñ Ω µ Ω æ:**

  * `vfoundation/apps/reference/domains/execution_position/exposure_guard.py` ‚ î TTL,  º µ Ç   ∏ ∫ ∏,    æ ∑ à ∏   µ Ω ∏ π    µ ∑ µ   ≤.
  * `vfoundation/apps/reference/domains/execution_position/fsm.py` ‚ î release- Ö É ∫ ∏, snapshot EVT.
  * `configs/master_config_v1.yaml`, `config/_schemas/aurora_trading.schema.json` ‚ î `pending_ttl_sec`.
* ** î æ ¥   Ω æ:**

  * `tests/units/test_exposure_guard_ttl.py`
  * `tests/integration/test_exposure_release_hooks.py`

---

#  ï2 ï    º æ É ∫ ( Ω    ∂ ∏ ≤ æ º É      æ Ñ ñ ª ñ)

1.  ó     É   Ç ∏  ± æ Ç    ∑ `max_portfolio_fraction=0.20`, `pending_ttl_sec=300`.
2.  § æ     É π `CMD:OPEN`  ∑  ≤ ñ ¥   É Ç Ω ñ º `price_ref`  ¥ ª è MARKET ‚Üí  æ á ñ ∫ É π `ERR:OPEN (NO_PRICE_REF)`.
3.  § æ     É π `CMD:OPEN`  ∑  º   ª ∏ º notional ‚Üí `DEC:OPEN`      æ Ö æ ¥ ∏ Ç å, pending  ∑   æ   Ç   î;  Ω   `ORDER_FILLED`/`POSITION_OPENED` pending    æ ≤ µ   Ç   î Ç å   è  ¥ æ 0.
4.  § æ     É π    µ   ñ é  ≤ ñ ¥ ∫   ∏ Ç Ç ñ ≤  Ç   ∫,  â æ ± `exposure_will_be` >  ª ñ º ñ Ç É ‚Üí  æ á ñ ∫ É π `ERR:OPEN (PORTFOLIO_EXPOSURE_LIMIT)`.

---

 ì æ Ç æ ≤ æ  ¥ æ  ≤ ∏ ∫ æ Ω   Ω Ω è.  Ø ∫ â æ  Ö æ á µ à ‚ î    ñ   ª è  Ü å æ ≥ æ      ∫ µ Ç      ñ ¥ ∫ ª é á É **/metrics**  µ ∫     æ   Ç µ   ∏  ¥ ª è  Ü ∏ Ö    æ ∫   ∑ Ω ∏ ∫ ñ ≤  ñ  ¥ æ ¥   º  ∫ æ   æ Ç ∫ ñ  ¥   à ± æ   ¥ ∏ ( ª æ ∫   ª å Ω ñ).
 ¢ æ  !  ü   ∏ π º   é    µ ∑ É ª å Ç   Ç  è ∫ **DONE (EXP-1)**.  ü µ   µ Ö æ ¥ ∏ º æ  ¥ æ  Ω     Ç É   Ω æ ≥ æ  ± ª æ ∫ É    Ç   ± ñ ª ñ ∑   Ü ñ ó **Exposure**.

 ù ∏ ∂ á µ ‚ î  ≥ æ Ç æ ≤ ∏ π      ∫ µ Ç  Ω     Ç É   Ω ∏ Ö  ∑   ≤ ¥   Ω å,  â æ ±  ¥ æ ≤ µ   Ç ∏  ≥ µ π Ç  ¥ æ ‚ ú ± æ π æ ≤ æ ó‚ ù  Ω   ¥ ñ π Ω æ   Ç ñ:  Ö É ∫ ∏ release, TTL      æ Ç ∏ ‚ ú ∑   ª ∏     Ω Ω è‚ ù pending,  Ç µ ª µ º µ Ç   ñ è/ º µ Ç   ∏ ∫ ∏,  ∫ æ Ω Ç     ∫ Ç Ω ñ  Ç µ   Ç ∏,  ¥ æ ∫ É º µ Ω Ç   Ü ñ è.  § æ   º   Ç ‚ î  æ ¥     ∑ É    ñ ¥ Copilot/   ≥ µ Ω Ç ñ ≤ ( ñ ¥ µ º   æ Ç µ Ω Ç Ω æ,  ∑  á ñ Ç ∫ ∏ º ∏  à ª è Ö   º ∏,  ∫ æ ¥ æ º,  Ç µ   Ç   º ∏,  ∫ æ º   Ω ¥   º ∏).

---

# PACK EXP-2 ‚ î Release Hooks & TTL (no-stale pending)

##  ¶ ñ ª å

 ì       Ω Ç É ≤   Ç ∏,  â æ pending- µ ∫     æ ∑ ∏ Ü ñ è ** ∑   ≤ ∂ ¥ ∏  ∑ ≤ ñ ª å Ω è î Ç å   è**:      ∏ `FILL/CANCEL/REJECT/ERR`  ñ  ∑   TTL.  £ Ω µ º æ ∂ ª ∏ ≤ ∏ Ç ∏ ‚ ú ∑   ª ∏     Ω Ω è‚ ù    µ ∑ µ   ≤ ñ ≤,  è ∫ ñ  ∑  á     æ º  ± ª æ ∫ É é Ç å  Ω æ ≤ ñ  ≤ ñ ¥ ∫   ∏ Ç Ç è.

##  î ñ ó

### 1)  † æ ∑ à ∏   ∏ Ç ∏ ExposureGuard TTL‚ ô æ º

**PATH:** `vfoundation/apps/reference/domains/execution_position/exposure_guard.py`

```python
from time import time
from dataclasses import dataclass, field

@dataclass
class ExposureState:
    equity_free_usdt: Decimal = Decimal("0")
    open_positions_usd: Decimal = Decimal("0")
    pending_open_usd: Decimal = Decimal("0")
    reservations: Dict[str, Decimal] = field(default_factory=dict)
    reservations_ts: Dict[str, float] = field(default_factory=dict)  # key -> epoch_s

class ExposureGuard:
    def __init__(self, cfg: dict, logger):
        ...
        ex = (self.cfg.get("execution") or {}).get("exposure") or {}
        self.ttl_sec = int(ex.get("pending_reservation_ttl_sec", 90))  #  ¥ µ Ñ æ ª Ç 90  

    def reserve(self, key: str, notional_usd: Decimal, *, reduce_only: bool = False) -> None:
        if self.exclude_reduce_only and reduce_only:
            return
        self.state.reservations[key] = notional_usd
        self.state.reservations_ts[key] = time()
        if self.count_pending:
            self.state.pending_open_usd += notional_usd

    def release(self, key: str) -> None:
        val = self.state.reservations.pop(key, None)
        self.state.reservations_ts.pop(key, None)
        if val is not None and self.count_pending:
            self.state.pending_open_usd = max(Decimal("0"), self.state.pending_open_usd - val)

    def expire_stale(self) -> list[str]:
        """ ó ≤ ñ ª å Ω è î      æ   Ç   æ á µ Ω ñ    µ ∑ µ   ≤ ∏;    æ ≤ µ   Ç   î      ∏   æ ∫  ∫ ª é á ñ ≤,  è ∫ ñ  ∑ Ω è Ç æ."""
        now = time()
        expired = []
        for k, ts in list(self.state.reservations_ts.items()):
            if now - ts >= self.ttl_sec:
                self.release(k)
                expired.append(k)
        return expired
```

** ö æ Ω Ñ ñ ≥/   Ö µ º   ( ¥ æ ¥   Ç ∏    æ ª µ TTL):**

* **PATH:** `configs/master_config_v1.yaml`

  ```yaml
  execution:
    exposure:
      pending_reservation_ttl_sec: 90
  ```
* **PATH:** `config/_schemas/aurora_trading.schema.json`

  ```json
  "pending_reservation_ttl_sec": { "type": "integer", "minimum": 10, "maximum": 600, "default": 90 }
  ```

### 2)  í ∏ ∫ ª ∏ ∫   Ç ∏ expire  ≤ FSM

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`

*  í ∏ ∫ ª ∏ ∫   Ç ∏ `self.exposure.expire_stale()`  É  ∑   É á Ω ∏ Ö  Ç æ á ∫   Ö ( ± É ¥ å- è ∫    á     Ç      æ ¥ ñ è,  Ω       ∏ ∫ ª   ¥      ∏ `EVT:PORTFOLIO_STATE_UPDATED`    ± æ  Ω    ∫ æ ∂ Ω ∏ π `CMD:OPEN`    µ   µ ¥    µ   µ ≤ ñ   ∫   º ∏).
*  Ø ∫ â æ `expired`  Ω µ    æ   æ ∂ Ω ñ π ‚ î  ∑ ≥ µ Ω µ   É ≤   Ç ∏  Ç µ ª µ º µ Ç   ñ é.

```python
if msg.op == "EVT" and msg.verb == "PORTFOLIO_STATE_UPDATED":
    expired = self.exposure.expire_stale()
    if expired:
        await self.emit(Message(
            op="EVT", verb="PENDING_EXPOSURE_EXPIRED",
            intent="OBSERVATION", src="execution_position", dst="any", rid=msg.rid,
            pld={"expired_keys": expired, "why": "ttl_expired"}, why="expire_pending"
        ))
    self.exposure.on_portfolio_update(msg.pld or {})
    ...
```

### 3) Release- Ö É ∫ ∏  Ω    Ñ   ∫ Ç ∏ á Ω ñ  æ Ω æ ≤ ª µ Ω Ω è  æ   ¥ µ   ñ ≤

 ó ≤ ñ ª å Ω è Ç ∏    µ ∑ µ   ≤  ∑   ** Ç ∏ º      º ∏ º  ∫ ª é á µ º**,  â æ  ≤ ∏ ∫ æ   ∏   Ç æ ≤ É ≤   ≤   è  É `reserve_key = idempotent_key|rid`.

** ö É ¥ ∏    Ç   ≤ ∏ Ç ∏:**

*  Ç   º,  ¥ µ  ≤ ∏  æ Ç   ∏ º É î Ç µ    ñ ¥ Ç ≤ µ   ¥ ∂ µ Ω Ω è **REJECT/ERR:OPEN** ( ≤ ∂ µ  î),
*  Ç   º,  ¥ µ      ∏ Ö æ ¥ è Ç å ** ± ñ   ∂ æ ≤ ñ      ¥ µ π Ç ∏** (ACK/FILL/CANCELED/EXPIRED)  ¥ ª è entry- æ   ¥ µ    :

  * **PATH ( π º æ ≤ ñ   Ω æ):** `vfoundation/apps/reference/domains/execution_position/binance_execution_adapter.py` (   ± æ  æ ±   æ ± Ω ∏ ∫ user-stream    æ ¥ ñ π).
  *  ü   ∏  ≤ ∏ è ≤ ª µ Ω Ω ñ    Ç   Ç É   É    æ `clientOrderId` ‚Üí  ∑ Ω   π Ç ∏ `reserve_key` ( É  ≤      ≤ ñ Ω = `clientOrderId`    ± æ RID)  ñ `release(...)`.

* Ø ∫ â æ  ∑       ∑  Ω µ º   î  ∫   Ω æ Ω ñ á Ω æ ≥ æ `reserve_key -> clientOrderId`  º     ñ Ω ≥ É ‚ î  ¥ æ ¥   π Ç µ  ∫ æ   æ Ç ∫ ∏ π  ñ Ω- º µ º æ   ñ  º      É FSM  Ω      µ   ñ æ ¥ between `DEC:OPEN` ‚Üí `adapter.place_order(...)` ‚Üí `ACK`.*

### 4)  ¢ µ   Ç ∏

**PATH:** `tests/units/test_exposure_guard_ttl.py`

```python
def test_reservation_expires_by_ttl(monkeypatch):
    guard = ExposureGuard(cfg={"execution":{"exposure":{"pending_reservation_ttl_sec":1}}}, logger=None)
    guard.reserve("k1", Decimal("100"))
    #      ∏   ∫ æ   é î º æ  á    
    import time as _t
    monkeypatch.setattr("vfoundation.apps.reference.domains.execution_position.exposure_guard.time", lambda: _t.time()+2)
    expired = guard.expire_stale()
    assert "k1" in expired
    assert guard.state.pending_open_usd == Decimal("0")
```

**PATH:** `tests/integration/test_open_release_hooks.py`
 ° Ü µ Ω     ñ π: `CMD:OPEN` ‚Üí `reserve` ‚Üí  ñ º ñ Ç É î º æ `ERR:OPEN`/`REJECT` ‚Üí `release` ‚Üí `can_open`  ¥ æ ∑ ≤ æ ª è î  Ω æ ≤ ∏ π notional.

---

# PACK EXP-3 ‚ î Telemetry & Metrics

##  ¶ ñ ª å

 ú   Ç ∏      æ ∑ æ   ∏ π **   ª ñ ¥  µ ∫     æ ∑ ∏ Ü ñ ó** (     æ   Ç µ   µ ∂ É ≤   Ω ñ   Ç å),    ª é        æ º µ Ç µ π- º µ Ç   ∏ ∫ ∏.

##  î ñ ó

### 1)  ü æ ¥ ñ ó  Ç µ ª µ º µ Ç   ñ ó ( ≤ ∂ µ  á     Ç ∫ æ ≤ æ  ¥ æ ¥   ª ∏)

* `EVT:PORTFOLIO_EXPOSURE_UPDATED` ( ∑ equity, positions_usd, pending_usd, limit_usd)
* `EVT:PENDING_EXPOSURE_EXPIRED` (     ∏   æ ∫  ∫ ª é á ñ ≤)
* `ERR:OPEN`  ∑ `reason="PORTFOLIO_EXPOSURE_LIMIT"` ( ≤ ∂ µ  î)

### 2)  ú µ Ç   ∏ ∫ ∏

**PATH:** `apps/reference/api/main.py`    ± æ  º æ ¥ É ª å  º µ Ç   ∏ ∫,  è ∫ ∏ π  ≤ ∏  ≤ ∂ µ  ≤ ∏ ∫ æ   ∏   Ç æ ≤ É î Ç µ.

* `exposure_positions_usd` (gauge)
* `exposure_pending_usd` (gauge)
* `exposure_limit_usd` (gauge)
* `fsm_guard_rejects_total{guard="exposure"}` (counter)

 û Ω æ ≤ ª é ≤   Ç ∏  ∑ Ω   á µ Ω Ω è      ∏ `EVT:PORTFOLIO_EXPOSURE_UPDATED`  Ç      ñ   ª è `reserve/release`.

---

# PACK EXP-4 ‚ î Decision QoS ( â æ ±  Ω µ ‚ ú º æ ª æ Ç ∏ ≤‚ ù CMD:OPEN    ñ ¥  ± ª æ ∫ æ º)

##  ¶ ñ ª å

 ö æ ª ∏ exposure- ≥ µ π Ç  ± ª æ ∫ É î, ** Ω µ        º ∏ Ç ∏** `CMD:OPEN`  â æ   µ ∫ É Ω ¥ ∏/ â æ Ç ñ ∫ É.

##  î ñ ó

*  £ `DecisionMaking`    ± æ  ≤ bridge  ¥ æ ¥   Ç ∏      æ   Ç ∏ π **rate-limit**  Ω   `TRADE_INTENT_PROPOSED`      ∏  Ω   è ≤ Ω æ   Ç ñ `last_err_reason == PORTFOLIO_EXPOSURE_LIMIT` (cooldown,  Ω      ., 5‚ ì10    µ ∫  Ω      ∏ º ≤ æ ª).
*  ï º ñ Ç ∏ Ç ∏ `EVT:DECISION_RATE_LIMITED` (XAI).

---

# PACK EXP-5 ‚ î  ö æ Ω Ç     ∫ Ç ∏ &  î æ ∫ É º µ Ω Ç   Ü ñ è

##  ê   Ç µ Ñ   ∫ Ç ∏

* **PATH:** `agent/project_docs/exec/dictionary.md` ‚ î  ¥ æ ¥   Ç ∏:

  * `EVT:PORTFOLIO_EXPOSURE_UPDATED`, `EVT:PENDING_EXPOSURE_EXPIRED`
  * `ERR:OPEN.reason = { PORTFOLIO_EXPOSURE_LIMIT, NO_PRICE_REF }`
* **PATH:** `agent/project_docs/exec/api.md` ‚ î  æ   ∏     Ç ∏    æ ª è    æ ¥ ñ π/ERR ( á ∏   ª æ ≤ ñ    Ç   æ ∫ ∏).
* **PATH:** `JOURNAL.md` ‚ î  ∫ æ   æ Ç ∫ ∏ π  ∑     ∏   PACK EXP-2..5.
* **PATH:** `TODO.md` ‚ î    É Ω ∫ Ç ‚ úAdd release-hooks mapping clientOrderId ‚Üí reserve_key‚ ù.

---

#  ö æ º   Ω ¥ ∏

```bash
ruff check .
ruff format .
mypy --strict
pytest -q -k "exposure_guard_ttl or open_release_hooks"
```

---

# DoD ( ¥ ª è  ≤   ñ Ö PACK EXP-2..5)

* Pending-   µ ∑ µ   ≤ ** Ω ñ ∫ æ ª ∏  Ω µ  ∑   ª ∏     î** (hook‚ ô ∏ + TTL).
*  ü   ∏ FILL/CANCEL/REJECT/ERR  ≤ ñ ¥   æ ≤ ñ ¥ Ω ∏ π    µ ∑ µ   ≤ ** ∑ ≤ ñ ª å Ω è î Ç å   è**.
*  ú µ Ç   ∏ ∫ ∏  æ Ω æ ≤ ª é é Ç å   è;    æ ¥ ñ ó  Ç µ ª µ º µ Ç   ñ ó  ∑‚ ô è ≤ ª è é Ç å   è  ≤  ª æ ≥   Ö.
*  ü ñ ¥  ± ª æ ∫ æ º exposure ‚ î ** Ω µ º   î**        º É intent‚ ô ñ ≤ (rate-limit).
*  î æ ∫ É º µ Ω Ç   Ü ñ è/   ª æ ≤ Ω ∏ ∫ ∏  æ Ω æ ≤ ª µ Ω ñ.

 Ø ∫ â æ  æ ∫ ‚ î  ∑     É   ∫   π    ≥ µ Ω Ç      æ PACK EXP-2.  ü ñ   ª è merge  ¥   º  ∫ æ º     ∫ Ç Ω ∏ π PATCH  ¥ ª è clientOrderId‚Üíreserve_key  ∑ ≤‚ ô è ∑ É ≤   Ω Ω è  É  Ç ≤ æ î º É    ¥     Ç µ   ñ,  è ∫ â æ  π æ ≥ æ  â µ  Ω µ º   î.

 ö         ≤ á ∏ ∫ üôå PACK EXP-2      ∏ π Ω è ≤  è ∫ DONE.  ô ¥ µ º æ  ¥   ª ñ ‚ î  ≥ æ Ç É é **PACK EXP-3: Telemetry & Metrics**  Ç   ∫,  â æ ±    ≥ µ Ω Ç  ∑   æ ± ∏ ≤  ± µ ∑  ¥ æ ≥   ¥ æ ∫.

---

# PACK EXP-3 ‚ î Telemetry & Metrics (Prometheus + FSM hooks)

 ù ∏ ∂ á µ **TaskSpec  ¥ ª è Copilot (RU,  ∏ ¥ µ º   æ Ç µ Ω Ç Ω æ)** +    æ ≤ Ω ñ  à ª è Ö ∏  Ñ   π ª ñ ≤,  ∫ æ ¥,  Ç µ   Ç ∏,  ∫ æ º   Ω ¥ ∏,      Ç µ Ñ   ∫ Ç ∏.

---

## üéØ  ¶ µ ª å

*  í ≤ µ   Ç ∏ **     æ ∑ æ   É  Ç µ ª µ º µ Ç   ñ é  µ ∫     æ ∑ ∏ Ü ñ ó** (equity/positions/pending/limit)  ñ **     Ö É ≤   Ç ∏  ≤ ñ ¥ º æ ≤ ∏  ≥ µ π Ç  **.
*  î   Ç ∏ `/metrics`  ¥ ª è Prometheus.
*  ü ñ ¥ ≤‚ ô è ∑   Ç ∏  º µ Ç   ∏ ∫ ∏  ¥ æ    µ   ª å Ω ∏ Ö FSM-   æ ¥ ñ π (`EVT:PORTFOLIO_EXPOSURE_UPDATED`, `PENDING_EXPOSURE_EXPIRED`, `ERR:OPEN{PORTFOLIO_EXPOSURE_LIMIT}`, `MANAGE_SKIPPED`, * æ   Ü ñ æ Ω   ª å Ω æ* ‚ î `ORDER_PLACED/ORDER_FILLED`).

---

## 0)  ó   ª µ ∂ Ω ñ   Ç å

 î æ ¥   π ( è ∫ â æ  â µ  Ω µ º   î)  ¥ æ `requirements.txt`:

```
prometheus_client>=0.20.0
```

---

## 1)  ú æ ¥ É ª å  º µ Ç   ∏ ∫

**PATH:** `vfoundation/apps/reference/telemetry/metrics.py`

```python
from __future__ import annotations
from decimal import Decimal, InvalidOperation
from typing import Any

try:
    from prometheus_client import Counter, Gauge, generate_latest, CONTENT_TYPE_LATEST
except Exception:  # fail-closed:  ∑   ≥ ª É à ∫ ∏  è ∫ â æ  ª ∏ ±  Ω µ º   î
    class _D:
        def __getattr__(self, _): return self
        def labels(self, *_, **__): return self
        def set(self, *_): pass
        def inc(self, *_): pass
    Gauge = Counter = _D  # type: ignore
    def generate_latest(): return b""
    CONTENT_TYPE_LATEST = "text/plain; version=0.0.4"

def _d(x: Any) -> float:
    try:
        from decimal import Decimal as D
        return float(D(str(x)))
    except (InvalidOperation, ValueError, TypeError):
        return 0.0

# Gauges (   æ Ç æ á Ω ñ  ∑ Ω   á µ Ω Ω è)
g_exposure_equity_usd   = Gauge("exposure_equity_usd", "Free equity (USDT-M) used for sizing")
g_exposure_positions_usd= Gauge("exposure_positions_usd", "Open positions notional in USD")
g_exposure_pending_usd  = Gauge("exposure_pending_usd", "Pending (reserved) notional in USD")
g_exposure_limit_usd    = Gauge("exposure_limit_usd", "Exposure limit in USD (equity * fraction)")

# Counters (   æ ¥ ñ ó)
c_guard_rejects_total   = Counter("fsm_guard_rejects_total", "FSM guard rejects", ["guard"])
c_pending_expired_total = Counter("pending_exposure_expired_total", "Expired pending reservations by TTL")
c_manage_skipped_total  = Counter("manage_skipped_total", "Auto-manage skipped due to bracket mode")
c_orders_placed_total   = Counter("orders_placed_total", "Orders placed (entry & brackets)")
c_orders_filled_total   = Counter("orders_filled_total", "Orders filled")
c_decision_rate_limited = Counter("decision_rate_limited_total", "Decision intents rate-limited", ["symbol"])

def update_exposure(equity_usd: Any, positions_usd: Any, pending_usd: Any, fraction: float) -> None:
    eq  = _d(equity_usd)
    pos = _d(positions_usd)
    pen = _d(pending_usd)
    lim = eq * float(fraction)
    g_exposure_equity_usd.set(eq)
    g_exposure_positions_usd.set(pos)
    g_exposure_pending_usd.set(pen)
    g_exposure_limit_usd.set(lim)

def inc_exposure_guard_block() -> None:
    c_guard_rejects_total.labels(guard="exposure").inc()

def inc_pending_expired(n: int) -> None:
    if n > 0:
        c_pending_expired_total.inc(n)

def inc_manage_skipped() -> None:
    c_manage_skipped_total.inc()

def inc_order_placed() -> None:
    c_orders_placed_total.inc()

def inc_order_filled() -> None:
    c_orders_filled_total.inc()

def inc_decision_rl(symbol: str) -> None:
    c_decision_rate_limited.labels(symbol=symbol).inc()
```

---

## 2) `/metrics`  É API

**PATH:** `apps/reference/api/main.py`  *( ¥ æ ¥   π  º     à   É Ç,  Ω µ  ª   º   π  ñ   Ω É é á ñ)*

```python
from fastapi import FastAPI, Response
from vfoundation.apps.reference.telemetry.metrics import generate_latest, CONTENT_TYPE_LATEST

app = FastAPI(title="Aurora API")

# ...  Ç ≤ æ ó  ñ   Ω É é á ñ  µ Ω ¥   æ ñ Ω Ç ∏ ...

@app.get("/metrics")
def metrics():
    data = generate_latest()
    return Response(content=data, media_type=CONTENT_TYPE_LATEST)
```

---

## 3) FSM-hooks  É ExecutionPositionFSM

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`
( ¥ æ   æ ≤ Ω ∏  æ ±   æ ± ∫ É    æ ¥ ñ π,  ¥ µ  º ∏  ¥ æ ¥   ≤   ª ∏ ExposureGuard)

```python
from vfoundation.apps.reference.telemetry.metrics import (
    update_exposure, inc_exposure_guard_block, inc_pending_expired
)

class ExecutionPositionFSM(DomainFSM):
    def __init__(...):
        ...
        ex = (self.cfg.get("execution") or {}).get("exposure") or {}
        self._exposure_fraction = float(ex.get("max_portfolio_fraction", 0.20))

    async def handle(self, msg: Message):
        if msg.op == "EVT" and msg.verb == "PORTFOLIO_STATE_UPDATED":
            # 1)  ∑ Ω è Ç ∏      æ   Ç   æ á µ Ω ñ    µ ∑ µ   ≤ ∏
            expired = self.exposure.expire_stale()
            if expired:
                inc_pending_expired(len(expired))
                await self.emit(Message(
                    op="EVT", verb="PENDING_EXPOSURE_EXPIRED", intent="OBSERVATION",
                    src="execution_position", dst="any", rid=msg.rid,
                    pld={"expired_keys": expired}, why="ttl_expired"
                ))
            # 2)  æ Ω æ ≤ ∏ Ç ∏    Ç   Ω  ñ  º µ Ç   ∏ ∫ ∏
            self.exposure.on_portfolio_update(msg.pld or {})
            update_exposure(
                equity_usd=(msg.pld or {}).get("equity_free_usdt"),
                positions_usd=self.exposure.state.open_positions_usd,
                pending_usd=self.exposure.state.pending_open_usd,
                fraction=self._exposure_fraction,
            )
            return

        if msg.op == "ERR" and msg.verb == "OPEN" and (msg.pld or {}).get("reason") == "PORTFOLIO_EXPOSURE_LIMIT":
            inc_exposure_guard_block()
            return

        #  ñ Ω à    ª æ ≥ ñ ∫    æ ±   æ ± ∫ ∏...
```

---

## 4) Manage-FSM hook (kill-switch  Ç µ ª µ º µ Ç   ñ è)

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm_manage.py`
( É  ≥ ñ ª Ü ñ,  ¥ µ  º ∏  µ º ñ Ç ∏ º æ `EVT:MANAGE_SKIPPED` ‚ î  ¥ æ ¥   π  ª ñ á ∏ ª å Ω ∏ ∫)

```python
from vfoundation.apps.reference.telemetry.metrics import inc_manage_skipped

if not self._auto_manage_enabled:
    inc_manage_skipped()
    await self.emit(... "MANAGE_SKIPPED" ...)
    return
```

---

## 5)  ê ¥     Ç µ    ≤ ∏ ∫ æ Ω   Ω Ω è ( ª ñ á ∏ ª å Ω ∏ ∫ ∏  æ   ¥ µ   ñ ≤)

**PATH:** `vfoundation/apps/reference/domains/execution_position/binance_execution_adapter.py`
(   ± æ  Ç ≤ ñ π  Ñ   ∫ Ç ∏ á Ω ∏ π    ¥     Ç µ  ;  ¥ æ ¥   π    ñ   ª è  É     ñ à Ω æ ≥ æ    æ ∑ º ñ â µ Ω Ω è/ Ñ ñ ª  )

```python
from vfoundation.apps.reference.telemetry.metrics import inc_order_placed, inc_order_filled

#    ñ   ª è  É     ñ à Ω æ ≥ æ place (200 OK)
inc_order_placed()

#  É  ≥ ñ ª Ü ñ  æ ±   æ ± ∫ ∏ FILL
inc_order_filled()
```

>  Ø ∫ â æ FILL  æ ±   æ ± ª è î Ç å   è  ≤  ñ Ω à æ º É  º æ ¥ É ª ñ (user-stream),  ¥ æ ¥   π  Ç   º.

---

## 6) ( û   Ü ñ π Ω æ) QoS  ¥ ª è DecisionMaking

 Ø ∫ â æ  É  Ç µ ± µ  ≤ ∂ µ  î    æ ¥ ñ è `EVT:DECISION_RATE_LIMITED` ( ∫ æ ª ∏ exposure  ± ª æ ∫ É î  ñ  º ∏  Ω µ  Ö æ á µ º æ        º ∏ Ç ∏ intent),  ¥ æ ¥   π  ª ñ á ∏ ª å Ω ∏ ∫:

**PATH:** `apps/reference/domains/decision_making/decision_making.py`

```python
from vfoundation.apps.reference.telemetry.metrics import inc_decision_rl

#  É  º ñ   Ü ñ rate-limit:
inc_decision_rl(symbol)
self.fsm.emit("EVT:DECISION_RATE_LIMITED", {"symbol": symbol, ...})
```

---

## 7)  ¢ µ   Ç ∏

### A)  Æ Ω ñ Ç:  æ Ω æ ≤ ª µ Ω Ω è  º µ Ç   ∏ ∫  µ ∫     æ ∑ ∏ Ü ñ ó

**PATH:** `tests/units/test_metrics_update.py`

```python
from vfoundation.apps.reference.telemetry.metrics import update_exposure, generate_latest

def test_update_exposure_exports_values():
    update_exposure(5000, 300, 200, 0.2)
    data = generate_latest().decode("utf-8")
    assert "exposure_equity_usd" in data
    assert "exposure_positions_usd" in data
    assert "exposure_pending_usd" in data
    assert "exposure_limit_usd" in data
```

### B)  Ü Ω Ç µ ≥     Ü ñ π Ω ∏ π: /metrics endpoint

**PATH:** `tests/integration/test_metrics_endpoint.py`

```python
from fastapi.testclient import TestClient
from apps.reference.api.main import app

def test_metrics_endpoint_ok():
    c = TestClient(app)
    r = c.get("/metrics")
    assert r.status_code == 200
    body = r.text
    assert "exposure_equity_usd" in body
    assert "fsm_guard_rejects_total" in body
```

### C)  Ü Ω Ç µ ≥     Ü ñ π Ω ∏ π:    ñ ¥     Ö É Ω æ ∫  ≤ ñ ¥ º æ ≤  ≥ µ π Ç  

**PATH:** `tests/integration/test_exposure_reject_metrics.py`

```python
import pytest
from vfoundation.core.protocol import Message
from vfoundation.apps.reference.domains.execution_position.fsm import ExecutionPositionFSM
from vfoundation.apps.reference.telemetry.metrics import generate_latest

@pytest.mark.asyncio
async def test_exposure_guard_reject_counter(execution_fsm: ExecutionPositionFSM):
    #    ñ ¥ ∂ µ Ω ∏    Ç   Ω  Ç   ∫,  â æ ± OPEN  ∑   ± ª æ ∫ É ≤   ª æ ( µ ∫ ≤ ñ ≤   ª µ Ω Ç  Ç ≤ æ ó Ö  ñ   Ω É é á ∏ Ö  ñ Ω Ç µ ≥     Ü ñ π Ω ∏ Ö  Ç µ   Ç ñ ≤)
    #    æ Ç ñ º    µ   µ ≤ ñ  ,  â æ  Ñ     ∑    ∑  ª ñ á ∏ ª å Ω ∏ ∫ æ º  ∑' è ≤ ∏ ª     è
    data = generate_latest().decode("utf-8")
    assert "fsm_guard_rejects_total{guard=\"exposure\"}" in data
```

>  ü ñ ¥ ª   à Ç É π    ñ ¥  Ç ≤ æ ó  Ñ ñ ∫   Ç É   ∏ (`execution_fsm`).  Ø ∫ â æ    ∫ ª   ¥ Ω æ ‚ î  ∑   ª ∏ à  ¥ ≤      µ   à ∏ Ö  Ç µ   Ç ∏;  Ü µ  ≤ ∂ µ  ¥   î    æ ∫   ∏ Ç Ç è.

---

## 8)  ö æ º   Ω ¥ ∏

```bash
ruff check .
ruff format .
mypy --strict
pytest -q -k "metrics_"
pytest -q tests/integration/test_metrics_endpoint.py
```

---

## 9)  ê   Ç µ Ñ   ∫ Ç ∏

* ** ° Ç ≤ æ   µ Ω æ:**
  `vfoundation/apps/reference/telemetry/metrics.py`
  `tests/units/test_metrics_update.py`
  `tests/integration/test_metrics_endpoint.py`
  *( æ   Ü ñ π Ω æ)* `tests/integration/test_exposure_reject_metrics.py`
* ** ó º ñ Ω µ Ω æ:**
  `apps/reference/api/main.py` ( ¥ æ ¥   Ω æ `/metrics`)
  `vfoundation/apps/reference/domains/execution_position/fsm.py` (hooks)
  `vfoundation/apps/reference/domains/execution_position/fsm_manage.py` (manage skipped counter)
  `vfoundation/apps/reference/domains/execution_position/binance_execution_adapter.py` (orders counters)
* ** ö æ Ω Ñ ñ ≥:** `requirements.txt` (prometheus_client)

---

## DoD ( ≥ æ Ç æ ≤ Ω ñ   Ç å)

* `/metrics`    æ ≤ µ   Ç   î Prometheus- Ñ æ   º   Ç  ∑  º µ Ç   ∏ ∫   º ∏:

  * `exposure_equity_usd`, `exposure_positions_usd`, `exposure_pending_usd`, `exposure_limit_usd`
  * `fsm_guard_rejects_total{guard="exposure"}`
  * `pending_exposure_expired_total`
  * `manage_skipped_total`
  * *( æ   Ü ñ π Ω æ)* `orders_placed_total`, `orders_filled_total`, `decision_rate_limited_total{symbol=‚ ¶}`
*  Æ Ω ñ Ç/ ñ Ω Ç µ ≥     Ü ñ π Ω ñ  Ç µ   Ç ∏ ‚ î **green**, `ruff/mypy` ‚ î **OK**.

---

 • æ á µ à ‚ î    ñ   ª è merge EXP-3,  ∑ ± µ   É **Dashboard- à   ± ª æ Ω (Grafana JSON)**    ñ ¥  Ü ñ  º µ Ç   ∏ ∫ ∏  Ç    ¥ æ ¥   º  ª µ ≥ ∫ ∏ π `/statdump` snapshot,  â æ ±  º   Ç ∏ human-readable  ∑   µ ∑ é º æ ≤   Ω ∏ π    Ç   Ω  µ ∫     æ ∑ ∏ Ü ñ ó  ≤ API.

 ë ª ∏   ∫ É á µ,  ö ª     ∫. **EXP-3 ‚ î DONE.**  ô ¥ µ º æ  ¥   ª ñ  ∑      ª   Ω æ º    Ç   ± ñ ª ñ ∑   Ü ñ ó.  ù ∏ ∂ á µ ‚ î  ¥ ≤    Ω     Ç É   Ω ñ      ∫ µ Ç ∏,  º   ∫   ∏ º   ª å Ω æ      ∏ ∑ µ º ª µ Ω ñ    ñ ¥  Ç ≤ æ é  ∫ æ ¥ æ ≤ É  ±   ∑ É ( à ª è Ö ∏  Ç æ á Ω ñ,  ± µ ∑  ∑ ¥ æ ≥   ¥ æ ∫),  ñ ∑  ≥ æ Ç æ ≤ ∏ º ∏  Ñ     ≥ º µ Ω Ç   º ∏  ∫ æ ¥ É,  Ç µ   Ç   º ∏  ñ  ∫ æ º   Ω ¥   º ∏.

---

# PACK EXP-4 ‚ î Decision QoS & Anti-Intent Spam

##  ú µ Ç  

 ó Ω ∏ ∑ ∏ Ç ∏  à É º  ñ  Ω   ≤   Ω Ç   ∂ µ Ω Ω è,  ∫ æ ª ∏  ≤ ñ ¥ ∫   ∏ Ç Ç è  ± ª æ ∫ É î Ç å   è (exposure gate),  Ç    ∑   ≥   ª æ º  æ ± º µ ∂ ∏ Ç ∏  á     Ç æ Ç É  Ω   º ñ   ñ ≤  Ω      ∏ º ≤ æ ª.

## 1)  ö æ Ω Ñ ñ ≥ +    Ö µ º  

**PATH:** `configs/master_config_v1.yaml`

```yaml
decision:
  qos:
    exposure_block_cooldown_sec: 10   #      É ∑      ñ   ª è  ± ª æ ∫ É ≤   Ω Ω è exposure- ≥ µ π Ç æ º
    symbol_cooldown_sec: 3            #  º ñ Ω ñ º   ª å Ω ∏ π  ñ Ω Ç µ   ≤   ª  º ñ ∂ intent    æ    ∏ º ≤ æ ª É
    max_intents_per_minute_per_symbol: 6
```

**PATH:** `config/_schemas/aurora_trading.schema.json`

```json
{
  "type": "object",
  "properties": {
    "decision": {
      "type": "object",
      "properties": {
        "qos": {
          "type": "object",
          "properties": {
            "exposure_block_cooldown_sec": {"type": "integer", "minimum": 1, "maximum": 300, "default": 10},
            "symbol_cooldown_sec": {"type": "integer", "minimum": 1, "maximum": 60, "default": 3},
            "max_intents_per_minute_per_symbol": {"type": "integer", "minimum": 1, "maximum": 60, "default": 6}
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
```

## 2)  † µ   ª ñ ∑   Ü ñ è QoS  É DecisionMaking

**PATH:** `apps/reference/domains/decision_making/decision_making.py`

```python
import time
from collections import defaultdict, deque
from decimal import Decimal, InvalidOperation
from vfoundation.core.protocol import Message
from vfoundation.apps.reference.telemetry.metrics import inc_decision_rl

def _d(x):
    try:
        return Decimal(str(x))
    except (InvalidOperation, ValueError, TypeError):
        return Decimal("0")

class DecisionMaking:
    def __init__(self, fsm, cfg, logger):
        self.fsm = fsm
        self.cfg = cfg or {}
        self.logger = logger
        qos = ((self.cfg.get("decision") or {}).get("qos") or {})
        self._cooldown_after_exposure_block = int(qos.get("exposure_block_cooldown_sec", 10))
        self._symbol_cooldown_sec = int(qos.get("symbol_cooldown_sec", 3))
        self._max_ipm = int(qos.get("max_intents_per_minute_per_symbol", 6))
        # QoS state
        self._last_block_ts = defaultdict(lambda: 0.0)     # symbol -> ts (block by exposure)
        self._last_intent_ts = defaultdict(lambda: 0.0)    # symbol -> ts (last propose)
        self._minute_window = defaultdict(lambda: deque(maxlen=60))  # symbol -> deque[timestamps]

        # portfolio/equity cache
        self._equity_usdt = Decimal("0")
        self._positions_cache = []

    def on_portfolio(self, msg: Message):
        pld = msg.pld or {}
        eq = _d(pld.get("equity_free_usdt"))
        if eq > 0 or getattr(self, "_equity_usdt", Decimal("0")) <= 0:
            self._equity_usdt = eq
        self._positions_cache = pld.get("positions") or []
        self.logger.info("   Equity (free): %s, Positions: %d", str(self._equity_usdt), len(self._positions_cache))

    def on_err_open(self, msg: Message):
        #  ≤ ∏ ∫ ª ∏ ∫   Ç ∏  ∑ bridge,  ∫ æ ª ∏      ∏ Ö æ ¥ ∏ Ç å ERR:OPEN
        pld = msg.pld or {}
        if pld.get("reason") == "PORTFOLIO_EXPOSURE_LIMIT":
            sym = (pld.get("symbol") or pld.get("instrument") or "").upper()
            if sym:
                self._last_block_ts[sym] = time.time()

    def _qos_allow(self, symbol: str) -> tuple[bool, str]:
        now = time.time()
        # 1) cooldown    ñ   ª è exposure block
        block_ts = self._last_block_ts[symbol]
        if block_ts and (now - block_ts) < self._cooldown_after_exposure_block:
            return False, "qos_exposure_block_cooldown"

        # 2) symbol cooldown
        last_ts = self._last_intent_ts[symbol]
        if last_ts and (now - last_ts) < self._symbol_cooldown_sec:
            return False, "qos_symbol_cooldown"

        # 3) intents per minute
        dq = self._minute_window[symbol]
        #  æ á ∏   Ç ∏ Ç ∏    Ç     à ñ  ∑   60  
        while dq and (now - dq[0]) > 60.0:
            dq.popleft()
        if len(dq) >= self._max_ipm:
            return False, "qos_rate_limited"

        return True, "ok"

    async def maybe_propose(self, symbol: str, build_intent_cb):
        allowed, reason = self._qos_allow(symbol)
        if not allowed:
            self.logger.warning("[%s] Decision QoS: %s", symbol, reason)
            inc_decision_rl(symbol)
            await self.fsm.emit(Message(
                op="EVT", verb="DECISION_RATE_LIMITED", intent="OBSERVATION",
                src="decision_making", dst="any", rid=None,
                pld={"symbol": symbol, "reason": reason}, why="decision_qos"
            ))
            return

        #  ∑ ≥ µ Ω µ   É ≤   Ç ∏ intent
        intent = build_intent_cb()
        if not intent:
            return
        self._last_intent_ts[symbol] = time.time()
        self._minute_window[symbol].append(self._last_intent_ts[symbol])
        await self.fsm.emit(Message(
            op="EVT", verb="TRADE_INTENT_PROPOSED", intent="INTENT",
            src="decision_making", dst="execution_position", rid=intent["rid"],
            pld=intent, why="intent_ready"
        ))
```

>  ü   ∏ º ñ Ç ∫  :  É  Ç ≤ æ î º É  ñ   Ω É é á æ º É  ∫ æ ¥ ñ  ∑   º ñ Ω ∏  º ñ   Ü µ      è º æ ≥ æ  µ º ∏ Ç É `TRADE_INTENT_PROPOSED`  Ω    ≤ ∏ ∫ ª ∏ ∫ `await self.maybe_propose(symbol, build_intent_cb)`  ¥ µ `build_intent_cb`    æ ≤ µ   Ç   î payload ( ≤ ∫ ª é á Ω æ  ∑ `rid`, `symbol`, `side`, `qty`, `price_ref`  Ç æ â æ).

## 3)  ¢ µ   Ç ∏

**PATH:** `tests/units/test_decision_qos.py`

```python
import time
import pytest
from types import SimpleNamespace
from vfoundation.core.protocol import Message

class DummyBus:
    def __init__(self): self.emitted=[]
    async def emit(self, msg): self.emitted.append(msg)

@pytest.mark.asyncio
async def test_qos_symbol_cooldown(monkeypatch):
    bus = DummyBus()
    dm = __import__("apps.reference.domains.decision_making.decision_making",
                    fromlist=["DecisionMaking"]).decision_making.DecisionMaking(bus, {"decision":{"qos":{"symbol_cooldown_sec": 5,"max_intents_per_minute_per_symbol": 100}}}, logger=SimpleNamespace(info=lambda *a,**k:None, warning=lambda *a,**k:None))
    symbol="ETHUSDT"
    def mk(): return {"rid":"r1","symbol":symbol,"side":"BUY","qty":"0.01","price_ref":"3000"}

    #    µ   à ∏ π allowed
    await dm.maybe_propose(symbol, mk)
    assert any(m.verb=="TRADE_INTENT_PROPOSED" for m in bus.emitted)

    #  ¥   É ≥ ∏ π  æ ¥     ∑ É  º   î  ± É Ç ∏ rate-limited
    bus.emitted.clear()
    await dm.maybe_propose(symbol, mk)
    assert any(m.verb=="DECISION_RATE_LIMITED" for m in bus.emitted)
```

## 4)  ö æ º   Ω ¥ ∏

```bash
ruff check .
ruff format .
mypy --strict
pytest -q -k "decision_qos"
```

## 5)  ê   Ç µ Ñ   ∫ Ç ∏

*  ó º ñ Ω µ Ω æ: `apps/reference/domains/decision_making/decision_making.py`, `configs/master_config_v1.yaml`, `config/_schemas/aurora_trading.schema.json`
*  î æ ¥   Ω æ: `tests/units/test_decision_qos.py`

---

# PACK EXP-5 ‚ î Normalized Reject Reasons (AUR-003)

##  ú µ Ç  

 Ñ ¥ ∏ Ω ∏ π    ª æ ≤ Ω ∏ ∫      ∏ á ∏ Ω  ≤ ñ ¥ Ö ∏ ª µ Ω å  æ   ¥ µ   ñ ≤/ ≤ ñ ¥ º æ ≤    ¥     Ç µ     ‚Üí    Ç   ± ñ ª å Ω ñ `ERR:*`  ∑  Ω æ   º   ª ñ ∑ æ ≤   Ω ∏ º ∏  ∫ æ ¥   º ∏  ¥ ª è    Ω   ª ñ Ç ∏ ∫ ∏,  ª æ ≥ ñ ≤  ñ UI.  ü   ∏ ∫ ª   ¥ ∏: `NRR_IMMEDIATE_TRIGGER`, `NRR_MIN_NOTIONAL`, `NRR_INVALID_SIGNATURE`, `NRR_RATE_LIMIT`, `NRR_REDUCE_ONLY_RULE`  Ç æ â æ.

## 1)  ° ª æ ≤ Ω ∏ ∫  Ω æ   º   ª ñ ∑   Ü ñ ó

**PATH:** `vfoundation/apps/reference/common/reject_reasons.py`

```python
import re
from typing import Tuple, Dict

# canonical reasons
NRR = {
    "IMMEDIATE_TRIGGER": "NRR_IMMEDIATE_TRIGGER",
    "MIN_NOTIONAL": "NRR_MIN_NOTIONAL",
    "MIN_QTY": "NRR_MIN_QTY",
    "PRICE_TICK": "NRR_PRICE_TICK",
    "RATE_LIMIT": "NRR_RATE_LIMIT",
    "SIGNATURE_INVALID": "NRR_INVALID_SIGNATURE",
    "REDUCE_ONLY_RULE": "NRR_REDUCE_ONLY_RULE",
    "POSITION_SIDE_CONFLICT": "NRR_POSITION_SIDE_CONFLICT",
    "UNKNOWN": "NRR_UNKNOWN",
}

_PATTERNS: Dict[str, re.Pattern] = {
    NRR["IMMEDIATE_TRIGGER"]: re.compile(r"would\s+immediately\s+trigger", re.I),
    NRR["MIN_NOTIONAL"]: re.compile(r"min[_\s]?notional", re.I),
    NRR["MIN_QTY"]: re.compile(r"min[_\s]?qty|LOT_SIZE", re.I),
    NRR["PRICE_TICK"]: re.compile(r"tick[_\s]?size|min[_\s]?price|max[_\s]?price", re.I),
    NRR["RATE_LIMIT"]: re.compile(r"rate\s*limit|too many requests|429", re.I),
    NRR["SIGNATURE_INVALID"]: re.compile(r"-?1022|signature\s+is\s+not\s+valid", re.I),
    NRR["REDUCE_ONLY_RULE"]: re.compile(r"reduceOnly.*(rejected|not.*allowed)", re.I),
    NRR["POSITION_SIDE_CONFLICT"]: re.compile(r"positionSide", re.I),
}

def normalize_reject_reason(msg: str) -> Tuple[str, str]:
    if not msg: return (NRR["UNKNOWN"], "empty")
    for code, pat in _PATTERNS.items():
        if pat.search(msg):
            return (code, msg)
    return (NRR["UNKNOWN"], msg)
```

## 2)  í ∏ ∫ æ   ∏   Ç   Ω Ω è  É FSM/Adapter

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`
(   µ   µ ¥  µ º ñ   ñ î é `ERR:OPEN`  ¥ æ   æ ≤ Ω ∏ Ç ∏ payload  Ω æ   º   ª ñ ∑ æ ≤   Ω æ é      ∏ á ∏ Ω æ é)

```python
from vfoundation.apps.reference.common.reject_reasons import normalize_reject_reason

#      ∏ ∫ ª   ¥  É  ≥ ñ ª Ü ñ exposure guard ‚ î  ≤ ∂ µ  î reason;  ¥ ª è    æ º ∏ ª æ ∫    ¥     Ç µ    :
#  É  º ñ   Ü ñ,  ¥ µ  ª æ ≤ ∏ Ç µ Exception/adapter error e:
reason_raw = getattr(e, "message", None) or str(e)
code, _ = normalize_reject_reason(reason_raw)
await self.emit(Message(
    op="ERR", verb="OPEN", intent="REJECTION", src="execution_position", dst=msg.src, rid=msg.rid,
    pld={"reason": code, "reason_raw": reason_raw}, why="adapter_error"
))
```

**PATH:** `vfoundation/apps/reference/domains/execution_position/binance_execution_adapter.py`
( è ∫ â æ  Ç É Ç  Ñ æ   º É î à ERR ‚ î  Ç µ ∂  ¥ æ ¥   π  Ω æ   º   ª ñ ∑   Ü ñ é    µ   µ ¥    æ ≤ µ   Ω µ Ω Ω è º/   µ- µ º ñ Ç æ º)

## 3)  ¢ µ   Ç ∏    ª æ ≤ Ω ∏ ∫  

**PATH:** `tests/units/test_reject_reason_normalize.py`

```python
from vfoundation.apps.reference.common.reject_reasons import normalize_reject_reason, NRR

def test_immediate_trigger():
    code, raw = normalize_reject_reason("Order would immediately trigger.")
    assert code == NRR["IMMEDIATE_TRIGGER"]

def test_signature_invalid():
    code, raw = normalize_reject_reason("Binance API Error -1022: Signature for this request is not valid.")
    assert code == NRR["SIGNATURE_INVALID"]

def test_min_notional():
    code, raw = normalize_reject_reason("Filter failure: MIN_NOTIONAL")
    assert code == NRR["MIN_NOTIONAL"]
```

## 4)  ö æ º   Ω ¥ ∏

```bash
ruff check .
ruff format .
mypy --strict
pytest -q -k "reject_reason_normalize"
```

## 5)  ê   Ç µ Ñ   ∫ Ç ∏

*  î æ ¥   Ω æ: `vfoundation/apps/reference/common/reject_reasons.py`, `tests/units/test_reject_reason_normalize.py`
*  ó º ñ Ω µ Ω æ: `vfoundation/apps/reference/domains/execution_position/fsm.py` (+  æ   Ü. `binance_execution_adapter.py`)

---

##  ü ñ   ª è  Ü ∏ Ö  ¥ ≤ æ Ö      ∫ µ Ç ñ ≤

*  ° ∏   Ç µ º   ** Ω µ        º ∏ Ç å intent‚ ô   º ∏**      ∏  ± ª æ ∫   Ö,  ñ ** ≤   ñ ERR  Ω æ   º   ª ñ ∑ æ ≤   Ω ñ**,  â æ    æ ª µ ≥ à É î    Ω   ª ñ Ç ∏ ∫ É,    ª µ   Ç ∏  ñ  ¥ µ ±   ≥.

 ü   ∏ π Ω è Ç æ. **EXP-4  ñ EXP-5 ‚ î DONE.**  † É Ö   î º æ   å  ¥   ª ñ  ¥ æ **AUR-004: Order Lifecycle Correlation** ( ∫ æ   µ ª è Ü ñ è  ∂ ∏ Ç Ç î ≤ æ ≥ æ  Ü ∏ ∫ ª É  æ   ¥ µ    ).

 ù ∏ ∂ á µ ‚ î  ≥ æ Ç æ ≤ ∏ π **TaskSpec  ¥ ª è    ≥ µ Ω Ç   (   æ   ñ π   å ∫ æ é,  ñ ¥ µ º   æ Ç µ Ω Ç Ω æ)**,  ± µ ∑  ∑ ¥ æ ≥   ¥ æ ∫,  ∑  Ç æ á Ω ∏ º ∏  à ª è Ö   º ∏,  ∫ æ Ω Ç     ∫ Ç   º ∏    æ ¥ ñ π,  Ç µ   Ç   º ∏  π  ∫ æ º   Ω ¥   º ∏.

---

## AUR-004 ‚ î Order Lifecycle Correlation (rid ‚Üî idem ‚Üî clientOrderId ‚Üî exchangeOrderId)

###  ¶ µ ª å

 î   Ç ∏    æ ≤ Ω É  Ç       É    Ç   Ω ñ ≤  æ   ¥ µ      ≤ ñ ¥  Ω   º ñ   É  ¥ æ  Ç µ   º ñ Ω   ª å Ω æ ≥ æ    Ç   Ω É:

*  ∫ æ   µ ª è Ü ñ è: `rid ‚Üî idempotent_key ‚Üî clientOrderId ‚Üî exchangeOrderId`;
*  É Ω ñ Ñ ñ ∫ æ ≤   Ω ñ    æ ¥ ñ ó `EVT:ORDER_STATE_CHANGED`  ¥ ª è    Ç   Ω ñ ≤: `NEW`, `PARTIALLY_FILLED`, `FILLED`, `CANCELED`, `REJECTED`, `EXPIRED`;
* JSONL-   É ¥ ∏ Ç  ñ  º µ Ç   ∏ ∫ ∏, fail-closed, XAI (`why ‚â§ 80`),  ± µ ∑      è º ∏ Ö  ñ º   æ   Ç ñ ≤  º ñ ∂  ¥ æ º µ Ω   º ∏.

---

### 1)  ö æ Ω Ç     ∫ Ç ∏    æ ¥ ñ π

**EVT:ORDER_STATE_CHANGED** ( ¥ ∂ µ   µ ª æ: execution_position)

```json
{
  "op": "EVT",
  "verb": "ORDER_STATE_CHANGED",
  "intent": "OBSERVATION",
  "pld": {
    "symbol": "ETHUSDT",
    "status": "PARTIALLY_FILLED",
    "rid": "a1b2...",
    "idempotent_key": "open:ETHUSDT:...:ms",
    "clientOrderId": "C0P1D-abc123",
    "exchangeOrderId": "1234567890",
    "side": "BUY",
    "order_type": "MARKET|LIMIT",
    "tif": "GTC|IOC|FOK",
    "qty": "0.050",
    "filled_qty": "0.020",
    "avg_fill_price": "3920.15",
    "last_fill_price": "3920.20",
    "cum_quote": "78.40",
    "reduce_only": false,
    "ts_ms": 1761770867206
  },
  "why": "order_update"
}
```

**ERR:OPEN** ( ¥ æ   æ ≤ Ω µ Ω Ω è): `pld.clientOrderId`, `pld.exchangeOrderId` ( è ∫ â æ  ¥ æ   Ç É   Ω æ), `pld.reason` ‚ î  Ω æ   º   ª ñ ∑ æ ≤   Ω ∏ π NRR- ∫ æ ¥, `pld.reason_raw`.

---

### 2)  Ü Ω ¥ µ ∫    æ   ¥ µ   ñ ≤ (in-mem  ∑ TTL)

**PATH:** `vfoundation/apps/reference/domains/execution_position/order_index.py`

```python
from __future__ import annotations
from dataclasses import dataclass, field
from decimal import Decimal
from time import time
from typing import Optional, Dict

@dataclass
class OrderRef:
    rid: str
    idempotent_key: Optional[str]
    clientOrderId: Optional[str] = None
    exchangeOrderId: Optional[str] = None
    symbol: Optional[str] = None
    side: Optional[str] = None
    order_type: Optional[str] = None
    created_ts: float = field(default_factory=time)
    terminal: bool = False

class OrderIndex:
    def __init__(self, ttl_sec: int = 3600):
        self.ttl = ttl_sec
        self._by_rid: Dict[str, OrderRef] = {}
        self._by_client: Dict[str, OrderRef] = {}
        self._by_exchange: Dict[str, OrderRef] = {}

    def upsert_from_open(self, *, rid: str, idempotent_key: str, clientOrderId: Optional[str], symbol: str, side: str, order_type: str) -> OrderRef:
        ref = self._by_rid.get(rid) or OrderRef(rid=rid, idempotent_key=idempotent_key)
        ref.clientOrderId = clientOrderId or ref.clientOrderId
        ref.symbol, ref.side, ref.order_type = symbol, side, order_type
        self._by_rid[rid] = ref
        if ref.clientOrderId:
            self._by_client[ref.clientOrderId] = ref
        return ref

    def attach_exchange_id(self, *, clientOrderId: Optional[str], exchangeOrderId: Optional[str]) -> Optional[OrderRef]:
        ref = (clientOrderId and self._by_client.get(clientOrderId)) or None
        if ref and exchangeOrderId:
            ref.exchangeOrderId = exchangeOrderId
            self._by_exchange[exchangeOrderId] = ref
        return ref

    def get(self, *, rid: str=None, clientOrderId: str=None, exchangeOrderId: str=None) -> Optional[OrderRef]:
        if rid: return self._by_rid.get(rid)
        if clientOrderId: return self._by_client.get(clientOrderId)
        if exchangeOrderId: return self._by_exchange.get(exchangeOrderId)
        return None

    def mark_terminal(self, ref: OrderRef) -> None:
        ref.terminal = True

    def expire(self) -> int:
        now = time(); removed = 0
        def _kill(ref: OrderRef):
            nonlocal removed
            self._by_rid.pop(ref.rid, None); removed += 1
            if ref.clientOrderId: self._by_client.pop(ref.clientOrderId, None)
            if ref.exchangeOrderId: self._by_exchange.pop(ref.exchangeOrderId, None)

        for ref in list(self._by_rid.values()):
            if ref.terminal or (now - ref.created_ts) > self.ttl:
                _kill(ref)
        return removed
```

---

### 3)  Ü Ω Ç µ ≥     Ü ñ è  ≤ FSM  ≤ ñ ¥ ∫   ∏ Ç Ç è/ æ Ω æ ≤ ª µ Ω å

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`

*  ü ñ ¥  á      É     ñ à Ω æ ≥ æ `DEC:OPEN`/place: `order_index.upsert_from_open(...)`,  µ º ñ   ñ è `EVT:ORDER_STATE_CHANGED(status="NEW")`.
*  ù    æ Ç   ∏ º   Ω Ω è  ± ñ   ∂ æ ≤ ∏ Ö      ¥ µ π Ç ñ ≤ ( á µ   µ ∑  Ç ≤ ñ π  æ ±   æ ± Ω ∏ ∫  É  Ü å æ º É  ∂  ¥ æ º µ Ω ñ    ± æ  á µ   µ ∑    ¥     Ç µ  ) ‚ î `order_index.attach_exchange_id(...)`,  µ º ñ   ñ è `ORDER_STATE_CHANGED`  ∑  Ñ   ∫ Ç ∏ á Ω ∏ º ∏    æ ª è º ∏ (`filled_qty`, `avg_fill_price`,  Ç æ â æ).
*  ù    Ç µ   º ñ Ω   ª å Ω ∏ Ö    Ç   Ω   Ö (`FILLED`, `CANCELED`, `REJECTED`, `EXPIRED`) ‚ î `order_index.mark_terminal(...)`.
*  † µ ≥ É ª è   Ω ∏ π `expire()`  Ω    á     Ç ∏ Ö    æ ¥ ñ è Ö (tick/portfolio/update) +  Ç µ ª µ º µ Ç   ñ è.

>  ü   ∏ º ñ Ç ∫  :  è ∫ â æ  æ Ω æ ≤ ª µ Ω Ω è      ∏ Ö æ ¥ è Ç å  ≤    ¥     Ç µ   ñ ‚ î  ¥ ∏ ≤.   .4  Ω ∏ ∂ á µ:  Ç   º  Ñ æ   º É î º æ    æ ¥ ñ é/ ∫ æ ª ± µ ∫  É FSM.

---

### 4)  ê ¥     Ç µ   Binance ‚ î  º     ñ Ω ≥  æ Ω æ ≤ ª µ Ω å

**PATH:** `vfoundation/apps/reference/domains/execution_position/binance_execution_adapter.py`

*  ü ñ   ª è ** É     ñ à Ω æ ≥ æ place** (entry/TP/SL):  Ω   ¥   ∏ ª   π  É FSM    æ ≤ ñ ¥ æ º ª µ Ω Ω è/ ≤ ∏ ∫ ª ∏ ∫  ¥ ª è:

  * `upsert_from_open(...)` + `EVT:ORDER_STATE_CHANGED(status="NEW")`;
  *  è ∫ â æ  ≤ ACK      ∏ Ö æ ¥ ∏ Ç å `orderId` ‚ î  æ ¥     ∑ É `attach_exchange_id(...)`.
*  £ **WebSocket user-stream**/poll-     ¥ µ π Ç   Ö:

  *  ∑ Ω   Ö æ ¥ å `clientOrderId`/`orderId` ‚Üí `attach_exchange_id(...)`;
  *  µ º ∏ Ç å  É FSM `EVT:ORDER_STATE_CHANGED`  ∑: `status`, `filled_qty`, `cum_quote`, `last_fill_price`, `avg_fill_price` ( è ∫ â æ  î) +  æ ± æ ≤‚ ô è ∑ ∫ æ ≤ æ `rid`, `idempotent_key`, `clientOrderId`, `exchangeOrderId`.

>  ü   ∏ ≤‚ ô è ∑ ∫ É `rid`/`idempotent_key`    æ ≤ µ   Ç   î `OrderIndex.get(...)`    æ `clientOrderId|orderId`.

---

### 5) JSONL    É ¥ ∏ Ç    æ ¥ ñ π

**PATH:** `logs/aurora_events.jsonl` (   Ç ≤ æ   é î Ç å   è    ¥     Ç µ   æ º  ¥ æ º µ Ω É    ± æ  ∑   ≥   ª å Ω ∏ º  ª æ ≥ µ   æ º    æ ¥ ñ π)

*  § æ   º   Ç: ** æ ¥ ∏ Ω JSON    è ¥ æ ∫  Ω      æ ¥ ñ é**,  ∫ ª é á ñ:

  * `ts_ms`, `event="ORDER_STATE_CHANGED"`, `rid`, `idempotent_key`, `clientOrderId`, `exchangeOrderId`, `symbol`, `status`, `qty`, `filled_qty`, `avg_fill_price`, `why`.
*  ü ∏     Ç ∏  Ω    ∫ æ ∂ µ Ω `ORDER_STATE_CHANGED`  Ç    Ç µ   º ñ Ω   ª å Ω ∏ π `ERR:OPEN`.

>  í ∏ ∫ æ   ∏   Ç   π  ñ   Ω É é á ∏ π  É Ç ∏ ª ñ Ç     Ω ∏ π writer ( è ∫ â æ  î);  ñ Ω   ∫ à µ ‚ î  ∫ æ º     ∫ Ç Ω ∏ π file-writer  ∑ `RotatingFileHandler`    ± æ `append + flush`.

---

### 6)  ú µ Ç   ∏ ∫ ∏

 † æ ∑ à ∏   ∏ Ç ∏ **metrics.py** ( è ∫ â æ  â µ  Ω µ  ∑   æ ± ª µ Ω æ):

* `order_state_total{status}` (Counter) ‚ î  ñ Ω ∫   µ º µ Ω Ç  Ω    ∫ æ ∂ µ Ω `ORDER_STATE_CHANGED`.
* `order_lifecycle_seconds` (Histogram,  ∑    ±   ∂   Ω Ω è º) ‚ î  ≤ ñ ¥ `NEW`  ¥ æ  Ç µ   º ñ Ω   ª å Ω æ ≥ æ    Ç   Ω É ( è ∫ â æ  ∑ ± µ   ñ ≥   î à `created_ts`  É `OrderRef`).

**FSM hook (fsm.py)**:

*  Ω   `ORDER_STATE_CHANGED` ‚ î `order_state_total.labels(status=...)++`
*  è ∫ â æ `status`  Ç µ   º ñ Ω   ª å Ω ∏ π ‚ î `observe(now - ref.created_ts)`.

---

### 7)  ¢ µ   Ç ∏

**PATH:** `tests/units/test_order_index.py`

```python
from vfoundation.apps.reference.domains.execution_position.order_index import OrderIndex

def test_upsert_and_attach_and_get():
    idx = OrderIndex(ttl_sec=60)
    ref = idx.upsert_from_open(rid="r1", idempotent_key="idem1", clientOrderId="cid1", symbol="ETHUSDT", side="BUY", order_type="MARKET")
    assert idx.get(rid="r1") is ref
    idx.attach_exchange_id(clientOrderId="cid1", exchangeOrderId="ex1")
    ref2 = idx.get(exchangeOrderId="ex1")
    assert ref2.exchangeOrderId == "ex1"
```

**PATH:** `tests/integration/test_order_lifecycle_flow.py`

*  ° Ü µ Ω     ñ π:

  1. `CMD:OPEN` ‚Üí place ‚Üí `ORDER_STATE_CHANGED:NEW`
  2. partial fill ‚Üí `PARTIALLY_FILLED`  ∑  ≤   ª ñ ¥   Ü ñ î é `filled_qty`
  3. full fill ‚Üí `FILLED`  ñ `mark_terminal`
  4.    µ   µ ≤ ñ   ∏ Ç ∏ JSONL    è ¥ ∫ ∏  ñ  º µ Ç   ∏ ∫ ∏.

---

### 8)  ö æ º   Ω ¥ ∏

```bash
ruff check .
ruff format .
mypy --strict
pytest -q -k "order_index or order_lifecycle_flow"
```

---

### 9)  ê   Ç µ Ñ   ∫ Ç ∏

* ** î æ ¥   Ω æ:**
  `vfoundation/apps/reference/domains/execution_position/order_index.py`
  `tests/units/test_order_index.py`
  `tests/integration/test_order_lifecycle_flow.py`
* ** ó º ñ Ω µ Ω æ:**
  `vfoundation/apps/reference/domains/execution_position/fsm.py` ( µ º ñ   ñ è `ORDER_STATE_CHANGED`,  ñ Ω Ç µ ≥     Ü ñ è  ñ Ω ¥ µ ∫   É)
  `vfoundation/apps/reference/domains/execution_position/binance_execution_adapter.py` ( º     ñ Ω ≥ ACK/WS  æ Ω æ ≤ ª µ Ω å)
  `vfoundation/apps/reference/telemetry/metrics.py` ( ª ñ á ∏ ª å Ω ∏ ∫ ∏    Ç   Ω ñ ≤,  æ   Ü ñ π Ω æ  ≥ ñ   Ç æ ≥     º  )
  *( ∑      æ Ç   µ ± ∏)* writer  É  ∑   ≥   ª å Ω æ º É  ª æ ≥ µ   ñ    æ ¥ ñ π
* ** õ æ ≥ ∏:** `logs/aurora_events.jsonl` (append-only)

---

## DoD

*  î ª è  ∫ æ ∂ Ω æ ≥ æ  æ   ¥ µ      É  Ω      î    Ç   ± ñ ª å Ω ∏ π  ª   Ω Ü é ≥ `rid‚Üîidem‚ÜîclientOrderId‚ÜîexchangeOrderId`.
*  ï º ñ   ñ è `EVT:ORDER_STATE_CHANGED`  Ω    ∫ æ ∂ µ Ω    Ç   Ω; JSONL  ñ  º µ Ç   ∏ ∫ ∏  æ Ω æ ≤ ª é é Ç å   è.
*  ¢ µ   º ñ Ω   ª å Ω ñ    Ç   Ω ∏  ∑ ≤ ñ ª å Ω è é Ç å  ñ Ω ¥ µ ∫  ; `expire()`  á ∏   Ç ∏ Ç å    Ç     µ.
*  Æ Ω ñ Ç/ ñ Ω Ç µ ≥     Ü ñ π Ω ñ  Ç µ   Ç ∏ ‚ î  ∑ µ ª µ Ω ñ; `ruff/mypy` ‚ î  æ ∫.

 • æ á µ à ‚ î    ñ   ª è AUR-004    ñ ¥ ≥ æ Ç É é **L1-ORDER-LOGGER ( ∑ ≤ µ ¥ µ Ω ∏ π JSONL +  ∫ æ   æ Ç ∫      Ω   ª ñ Ç ∏ ∫   /statdump)**  Ç   **L3-METRICS-SUMMARY** (   ≤ Ç æ º   Ç ∏ á Ω ∏ π  ¥   π ¥ ∂ µ   Ç  É `reports/summary_gate_status.json`).


 ü   ∏ π Ω è ≤.  ó     É   ∫   î º æ **PROD-1**  ñ **PROD-2**.  ù ∏ ∂ á µ ‚ î  ≥ æ Ç æ ≤ ñ TaskSpec‚ ô ∏  ¥ ª è    ≥ µ Ω Ç   ( ñ ¥ µ º   æ Ç µ Ω Ç Ω æ,  ∑  Ç æ á Ω ∏ º ∏  à ª è Ö   º ∏,  ∫ æ ¥ æ º,  Ç µ   Ç   º ∏,  ∫ æ º   Ω ¥   º ∏,      Ç µ Ñ   ∫ Ç   º ∏).  ú ∏  Ω µ  á ñ     î º æ `fsm_manage`  ñ  Ω µ  º ñ Ω è î º æ `fsm_close` ‚ î        Ü é î º æ  ª ∏ à µ  É  ≤ ñ ¥ ∫   ∏ Ç Ç ñ (OPEN-flow),  è ∫  ñ  ¥ æ º æ ≤ ª è ª ∏   å.

---

# PACK PROD-1 ‚ î Daily Loss / Drawdown Gate (A3)

##  ¶ ñ ª å

 ë ª æ ∫ É ≤   Ç ∏  Ω æ ≤ ñ `CMD:OPEN`,  è ∫ â æ  ≤ ∏ ∫ æ Ω   Ω æ  ¥ µ Ω Ω ∏ π  ª ñ º ñ Ç  ∑ ± ∏ Ç ∫ É (USD)    ± æ intraday-drawdown  ≤ ñ ¥ `equity_open`. Fail-closed:  ± µ ∑  ¥   Ω ∏ Ö ‚ î  ± ª æ ∫ É î º æ.

##  ö æ Ω Ñ ñ ≥ +    Ö µ º  

**PATH:** `configs/master_config_v1.yaml`

```yaml
risk:
  daily:
    max_realized_loss_usd: 250.0     #    Ç æ  - ¥ µ Ω å    æ    µ   ª ñ ∑ æ ≤   Ω æ º É PnL
    max_drawdown_pct: 8.0            #    Ç æ  - ¥ µ Ω å    æ intra-day DD (%  ≤ ñ ¥ equity_open)
    reset_time_utc: "00:00"          #  á      ¥ æ ± æ ≤ æ ≥ æ    µ   µ Ç É
```

**PATH:** `config/_schemas/aurora_trading.schema.json`

```json
{
  "type": "object",
  "properties": {
    "risk": {
      "type": "object",
      "properties": {
        "daily": {
          "type": "object",
          "properties": {
            "max_realized_loss_usd": { "type": "number", "minimum": 0, "default": 250.0 },
            "max_drawdown_pct": { "type": "number", "minimum": 0, "maximum": 100, "default": 8.0 },
            "reset_time_utc": { "type": "string", "pattern": "^[0-2][0-9]:[0-5][0-9]$", "default": "00:00" }
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
```

##  ö æ ¥

### 1) DailyGate (   ≥   µ ≥ É î PnL  ñ DD)

**PATH:** `vfoundation/apps/reference/domains/risk_management/daily_gate.py`

```python
from __future__ import annotations
from dataclasses import dataclass
from datetime import datetime, timezone
from decimal import Decimal, InvalidOperation
from typing import Optional, Dict, Any, Tuple

def _d(x) -> Decimal:
    from decimal import Decimal as D
    try: return D(str(x))
    except (InvalidOperation, ValueError, TypeError): return D("0")

def _now_utc() -> datetime:
    return datetime.now(timezone.utc)

@dataclass
class DailyConfig:
    max_realized_loss_usd: Decimal
    max_drawdown_pct: Decimal
    reset_h: int
    reset_m: int

class DailyRiskState:
    """
     ¢   ∏ º   î  ¥ µ Ω Ω æ-   ≥   µ ≥ æ ≤   Ω ñ  º µ Ç   ∏ ∫ ∏:
      - equity_open_usd (snapshot  Ω    ¥ æ ± æ ≤ æ º É    µ   µ Ç ñ),
      - equity_now_usd ( æ Ω æ ≤ ª é î Ç å   è  ≤ ñ ¥    æ   Ç Ñ µ ª è),
      - realized_pnl_usd ( Ω   ∫ æ   ∏ á É î º æ  ∑ FILLED/ ∑   ∫   ∏ Ç Ç ñ ≤).
    """
    def __init__(self, cfg: dict, logger=None):
        self.log = logger
        rcfg = ((cfg.get("risk") or {}).get("daily") or {})
        self.cfg = DailyConfig(
            max_realized_loss_usd=_d(rcfg.get("max_realized_loss_usd", 250)),
            max_drawdown_pct=_d(rcfg.get("max_drawdown_pct", 8)),
            reset_h=int(str(rcfg.get("reset_time_utc", "00:00")).split(":")[0]),
            reset_m=int(str(rcfg.get("reset_time_utc", "00:00")).split(":")[1]),
        )
        self._equity_open = Decimal("0")
        self._equity_now = Decimal("0")
        self._realized_pnl = Decimal("0")
        self._last_reset_date = None  # YYYY-MM-DD

    def _maybe_reset(self, now: Optional[datetime] = None) -> None:
        now = now or _now_utc()
        reset_date = now.date()
        if self._last_reset_date != reset_date and now.hour >= self.cfg.reset_h and now.minute >= self.cfg.reset_m:
            #  ≤ ñ ¥ ∫   ∏ ≤   î º æ  Ω æ ≤ ∏ π  ¥ µ Ω å
            self._equity_open = self._equity_now  #  Ñ ñ ∫   É î º æ    Ç     Ç
            self._realized_pnl = Decimal("0")
            self._last_reset_date = reset_date

    def on_portfolio(self, pld: Dict[str, Any]) -> None:
        self._equity_now = _d(pld.get("equity_free_usdt"))
        self._maybe_reset()

    def on_order_filled(self, pld: Dict[str, Any]) -> None:
        #  æ á ñ ∫ É î º æ pld: {"realized_pnl_usd": "..."}    ± æ    æ ∑     Ö É Ω æ ∫  ∑  ∫ É º É ª è Ç ∏ ≤ Ω ∏ Ö  ∫ æ Ç ∏   É ≤   Ω å
        realized = _d(pld.get("realized_pnl_usd"))
        self._realized_pnl += realized

    def can_open(self) -> Tuple[bool, Dict[str, Any]]:
        # Fail-closed:  ± µ ∑  ∫ æ   µ ∫ Ç Ω ∏ Ö equity ‚ î  ± ª æ ∫ É î º æ
        if self._equity_open <= 0 or self._equity_now <= 0:
            return False, {
                "reason": "DAILY_RISK_LIMIT",
                "detail": "NO_EQUITY",
                "equity_open_usd": str(self._equity_open),
                "equity_now_usd": str(self._equity_now),
            }
        dd = Decimal("0")
        if self._equity_open > 0:
            dd = (Decimal("1") - (self._equity_now / self._equity_open)) * Decimal("100")

        if (-self._realized_pnl) >= self.cfg.max_realized_loss_usd:
            return False, {
                "reason": "DAILY_RISK_LIMIT",
                "detail": "MAX_REALIZED_LOSS",
                "realized_pnl_usd": str(self._realized_pnl),
                "limit_usd": str(self.cfg.max_realized_loss_usd),
            }

        if dd >= self.cfg.max_drawdown_pct:
            return False, {
                "reason": "DAILY_RISK_LIMIT",
                "detail": "MAX_DRAWDOWN",
                "drawdown_pct": str(dd),
                "limit_pct": str(self.cfg.max_drawdown_pct),
                "equity_open_usd": str(self._equity_open),
                "equity_now_usd": str(self._equity_now),
            }

        return True, {
            "equity_open_usd": str(self._equity_open),
            "equity_now_usd": str(self._equity_now),
            "realized_pnl_usd": str(self._realized_pnl),
            "drawdown_pct": str(dd),
        }
```

### 2)  Ü Ω Ç µ ≥     Ü ñ è  É OPEN-flow

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`
( Ñ     ≥ º µ Ω Ç ∏;  ≤   Ç   ≤  É  Ç ≤ ñ π FSM    ñ   ª è exposure- ≥ µ π Ç  ,  ¥ æ `DEC:OPEN`)

```python
from vfoundation.apps.reference.domains.risk_management.daily_gate import DailyRiskState, _d
# ...  É __init__:
self.daily = DailyRiskState(cfg=self.cfg, logger=self.logger)

# ...  É handle():
if msg.op == "EVT" and msg.verb == "PORTFOLIO_STATE_UPDATED":
    self.daily.on_portfolio(msg.pld or {})
    # ( ∑   ª ∏ à  ñ Ω à ñ  Ç ≤ æ ó  æ Ω æ ≤ ª µ Ω Ω è)

# ( è ∫ â æ  º   î à    æ ¥ ñ ó FILLED  ñ ∑ realized_pnl_usd)
if msg.op == "EVT" and msg.verb == "ORDER_STATE_CHANGED":
    st = (msg.pld or {}).get("status", "").upper()
    if st == "FILLED":
        self.daily.on_order_filled(msg.pld or {})

if msg.op == "CMD" and msg.verb == "OPEN":
    # ...  ü Ü ° õ Ø exposure.can_open() OK ...
    ok, data = self.daily.can_open()
    if not ok:
        await self.emit(Message(
            op="ERR", verb="OPEN", intent="REJECTION",
            src="execution_position", dst=msg.src, rid=msg.rid,
            pld=data, why="daily_gate_block"
        ))
        return

    #      æ ¥ æ ≤ ∂ É π  Ç ≤ ñ π      π   ª   π Ω OPEN ...
```

##  ¢ µ   Ç ∏

**PATH:** `tests/units/test_daily_gate_unit.py`

```python
from vfoundation.apps.reference.domains.risk_management.daily_gate import DailyRiskState

def test_daily_gate_blocks_on_drawdown():
    st = DailyRiskState(cfg={"risk":{"daily":{"max_drawdown_pct": 5}}})
    st.on_portfolio({"equity_free_usdt":"1000"})
    #  µ º É ª é î º æ    Ç     Ç  ¥ Ω è: equity_open  Ñ ñ ∫   É î Ç å   è  Ω      µ   à æ º É reset;        æ   Ç ∏ º æ ‚ î  ≤   É á Ω É:
    st._equity_open = st._equity_now  #  Ç ñ ª å ∫ ∏  ¥ ª è  Ç µ   Ç É
    #      ¥   î º æ  Ω   10%:
    st.on_portfolio({"equity_free_usdt":"900"})
    ok, data = st.can_open()
    assert ok is False and data["detail"] == "MAX_DRAWDOWN"
```

**PATH:** `tests/integration/test_daily_gate_block_open.py`

```python
#    æ ± É ¥ É π  ≤ ñ ¥ ∫   ∏ Ç Ç è,  ¥ µ daily.can_open()    æ ≤ µ   Ç   î False ‚Üí  æ á ñ ∫ É π ERR:OPEN  ∑ reason=DAILY_RISK_LIMIT
```

##  ö æ º   Ω ¥ ∏

```bash
ruff check . && ruff format .
mypy --strict
pytest -q -k "daily_gate"
```

##  ê   Ç µ Ñ   ∫ Ç ∏

* ** î æ ¥   Ω æ:** `vfoundation/apps/reference/domains/risk_management/daily_gate.py`, `tests/units/test_daily_gate_unit.py`
* ** ó º ñ Ω µ Ω æ:** `vfoundation/apps/reference/domains/execution_position/fsm.py`, `config/_schemas/aurora_trading.schema.json`, `configs/master_config_v1.yaml`

---

# PACK PROD-2 ‚ î Panic Kill-Switch & Quiet Hours (A3)

##  ¶ ñ ª å

 î   Ç ∏  Ç æ ± ñ  ¥ ≤   ‚ ú ± æ π æ ≤ ñ‚ ù    µ   µ º ∏ ∫   á ñ:

* **Panic Kill-Switch:**  º ∏ Ç Ç î ≤ æ  ± ª æ ∫ É î  ≤   ñ  Ω æ ≤ ñ `CMD:OPEN`.
* **Quiet Hours (UTC):**    ª   Ω æ ≤ æ  ± ª æ ∫ É î  ≤ ñ ¥ ∫   ∏ Ç Ç è  É  ∑   ¥   Ω ñ  á     æ ≤ ñ  ≤ ñ ∫ Ω  .

-  æ   Ü ñ æ Ω   ª å Ω ∏ π **allowlist_symbols**.

##  ö æ Ω Ñ ñ ≥ +    Ö µ º  

**PATH:** `configs/master_config_v1.yaml`

```yaml
ops:
  panic_killswitch: false
  quiet_hours_utc: ["22:00-06:00"]
  allowlist_symbols: []    #    æ   æ ∂ Ω ñ π =  ± µ ∑  æ ± º µ ∂ µ Ω å
```

**PATH:** `config/_schemas/aurora_trading.schema.json`

```json
{
  "type": "object",
  "properties": {
    "ops": {
      "type": "object",
      "properties": {
        "panic_killswitch": { "type": "boolean", "default": false },
        "quiet_hours_utc": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[0-2][0-9]:[0-5][0-9]-[0-2][0-9]:[0-5][0-9]$" },
          "default": ["22:00-06:00"]
        },
        "allowlist_symbols": {
          "type": "array",
          "items": { "type": "string" },
          "default": []
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
```

##  ö æ ¥

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm.py`
( ¥ æ ¥   π    µ   à ∏ π  à     guard‚ ô ñ ≤  É `CMD:OPEN` ‚ î    µ   µ ¥ exposure/daily)

```python
from datetime import datetime, timezone

def _utc_hm() -> tuple[int,int]:
    now = datetime.now(timezone.utc)
    return now.hour, now.minute

def _in_quiet(quiet: list[str]) -> bool:
    h, m = _utc_hm()
    cur = h*60 + m
    for win in quiet or []:
        a,b = win.split("-")
        ah,am = map(int, a.split(":"))
        bh,bm = map(int, b.split(":"))
        start = ah*60+am
        end = bh*60+bm
        if start <= end:
            #  ∑ ≤ ∏ á   π Ω ∏ π  ¥ ñ       ∑ æ Ω
            if start <= cur <= end: return True
        else:
            #  á µ   µ ∑    ñ ≤ Ω ñ á
            if cur >= start or cur <= end: return True
    return False

# ...  É handle()      ∏ CMD:OPEN:
if msg.op == "CMD" and msg.verb == "OPEN":
    ops_cfg = (self.cfg.get("ops") or {})
    # 1) PANIC
    if bool(ops_cfg.get("panic_killswitch", False)):
        await self.emit(Message(
            op="ERR", verb="OPEN", intent="REJECTION",
            src="execution_position", dst=msg.src, rid=msg.rid,
            pld={"reason":"PANIC_ON"}, why="panic_kill"
        ))
        return
    # 2) QUIET HOURS
    if _in_quiet(ops_cfg.get("quiet_hours_utc") or []):
        await self.emit(Message(
            op="ERR", verb="OPEN", intent="REJECTION",
            src="execution_position", dst=msg.src, rid=msg.rid,
            pld={"reason":"QUIET_HOURS"}, why="quiet_window"
        ))
        return
    # 3) ALLOWLIST
    allow = ops_cfg.get("allowlist_symbols") or []
    sym = (msg.pld or {}).get("symbol", "").upper()
    if allow and sym not in set(x.upper() for x in allow):
        await self.emit(Message(
            op="ERR", verb="OPEN", intent="REJECTION",
            src="execution_position", dst=msg.src, rid=msg.rid,
            pld={"reason":"SYMBOL_NOT_ALLOWED", "symbol": sym, "allow": allow}, why="symbol_not_allowlisted"
        ))
        return

    # ...  ¥   ª ñ  Ç ≤ ñ π exposure/daily guards  ñ  ≤ ñ ¥ ∫   ∏ Ç Ç è ...
```

>  î ∏ Ω   º ñ á Ω µ  ≤ º ∏ ∫   Ω Ω è/ ≤ ∏ º ∏ ∫   Ω Ω è:  Ω      µ   à æ º É  µ Ç     ñ  ¥ æ   Ç   Ç Ω å æ  ∫ æ Ω Ñ ñ ≥  .  ü ñ ∑ Ω ñ à µ  º æ ∂ Ω    ¥ æ ¥   Ç ∏ `/ops/panic/on|off`  ∑  µ º ñ   ñ î é `EVT:OPS_PANIC_SET`.

##  ¢ µ   Ç ∏

**PATH:** `tests/units/test_quiet_hours.py`

```python
from vfoundation.apps.reference.domains.execution_position.fsm import _in_quiet

def test_in_quiet_simple():
    assert _in_quiet(["00:00-23:59"]) is True
    assert _in_quiet(["23:00-01:00"]) in (True, False)  #  ∑   ª µ ∂ ∏ Ç å  ≤ ñ ¥    æ Ç æ á Ω æ ≥ æ UTC  á     É
```

**PATH:** `tests/integration/test_panic_killswitch.py`

```python
#  ° ∫ æ Ω   Ç   É é π FSM  ∑ cfg.ops.panic_killswitch=true  ñ    µ   µ ≤ ñ  ,  â æ CMD:OPEN ‚Üí ERR:OPEN{PANIC_ON}
```

##  ö æ º   Ω ¥ ∏

```bash
ruff check . && ruff format .
mypy --strict
pytest -q -k "quiet_hours or panic_killswitch"
```

##  ê   Ç µ Ñ   ∫ Ç ∏

* ** ó º ñ Ω µ Ω æ:** `vfoundation/apps/reference/domains/execution_position/fsm.py`, `config/_schemas/aurora_trading.schema.json`, `configs/master_config_v1.yaml`
* ** î æ ¥   Ω æ:** `tests/units/test_quiet_hours.py`, `tests/integration/test_panic_killswitch.py`

---

# Go / No-Go

 ü ñ   ª è  º µ   ¥ ∂ É PROD-1  ñ PROD-2  Ç ∏  æ Ç   ∏ º   î à:

*  ê ≤ Ç æ º   Ç ∏ á Ω ∏ π **   Ç æ  - ¥ µ Ω å** (loss/DD) +    ª   Ω æ ≤ ñ ‚ ú Ç ∏ Ö ñ  ≥ æ ¥ ∏ Ω ∏‚ ù +    É á Ω ∏ π **panic-switch**.
*  ñ æ ¥ Ω ∏ Ö    É á Ω ∏ Ö  ≤ ∏ º ∏ ∫   Ω å  á µ   µ ∑  ª ñ º ñ Ç  ± ñ   ∂ ñ  á ∏  ∑   ª ∏     Ω Ω è pending ‚ î  Ü µ  ≤ ∂ µ  ∑   ∫   ∏ Ç æ    æ   µ   µ ¥ Ω ñ º ∏      ∫ µ Ç   º ∏.

 Ø ∫ â æ  ≤   µ  æ ∫ ‚ î  ¥   ≤   π    ≥ µ Ω Ç É  ≤ ∏ ∫ æ Ω É ≤   Ç ∏  Ü ñ      ∫ µ Ç ∏    æ  á µ   ∑ ñ (**     æ á   Ç ∫ É PROD-1**,    æ Ç ñ º **PROD-2**).

 ë æ º ± µ ∑ Ω æ.  § ñ ∫   É é: **PROD-1  ñ PROD-2 ‚ î DONE**.  ü µ   µ Ö æ ¥ ∏ º æ  ¥ æ  Ñ ñ Ω   ª å Ω æ ≥ æ  ± æ π æ ≤ æ ≥ æ    æ ª ñ   É ≤   Ω Ω è: **L3-METRICS-SUMMARY** + **/statdump** ( ∫ æ   æ Ç ∫ ∏ π  ∑ ≤ µ ¥ µ Ω ∏ π    Ç   Ω  ¥ ª è Ops).  ù ∏ ∂ á µ ‚ î  ≥ æ Ç æ ≤ ñ TaskSpec‚ ô ∏  ¥ ª è    ≥ µ Ω Ç   ( ñ ¥ µ º   æ Ç µ Ω Ç Ω æ),  ∑  Ç æ á Ω ∏ º ∏  à ª è Ö   º ∏,  ∫ æ ¥ æ º,  Ç µ   Ç   º ∏,  ∫ æ º   Ω ¥   º ∏  Ç        Ç µ Ñ   ∫ Ç   º ∏.

---

# PACK L3 ‚ î Metrics Summary Generator ‚Üí `reports/summary_gate_status.json`

##  ¶ ñ ª å

 ó   æ ± ∏ Ç ∏  ª µ ≥ ∫ É  É Ç ∏ ª ñ Ç É,  è ∫    ∑ Ω ñ º   î  ∑   ñ ∑  ∫ ª é á æ ≤ ∏ Ö  º µ Ç   ∏ ∫ (exposure/daily/ops)  ñ  Ñ æ   º É î  æ ¥ ∏ Ω JSON- ∑ ≤ ñ ¥  ¥ ª è Ops/   Ω   ª ñ Ç ∏ ∫ ∏.

##  ö æ Ω Ñ ñ ≥

**PATH:** `configs/master_config_v1.yaml`

```yaml
ops:
  metrics_url: "http://127.0.0.1:8000/metrics"   #  è ∫ â æ  ≤   à API  Ω    ñ Ω à æ º É    æ   Ç É ‚ î  æ Ω æ ≤ ñ Ç å  Ç É Ç
  reports_dir: "reports"
```

##  ö æ ¥

**PATH:** `tools/metrics_summary.py`

```python
#!/usr/bin/env python3
import os, re, json, sys, time
from urllib.request import urlopen, Request
from urllib.error import URLError

def _get_cfg():
    #      æ   Ç µ  á ∏ Ç   Ω Ω è env  ∑  ¥ µ Ñ æ ª Ç   º ∏;  º æ ∂ Ω    ∑   º ñ Ω ∏ Ç ∏  Ω    ≤   à ConfigLoader
    url = os.environ.get("OPS_METRICS_URL", "http://127.0.0.1:8000/metrics")
    out_dir = os.environ.get("OPS_REPORTS_DIR", "reports")
    return url, out_dir

def _scrape_metrics_text(url: str) -> str:
    try:
        req = Request(url, headers={"User-Agent": "metrics-summary/1.0"})
        with urlopen(req, timeout=3) as r:
            return r.read().decode("utf-8", "replace")
    except URLError as e:
        return ""

def _mget(txt: str, name: str, label_filter: str | None = None, default: float = 0.0) -> float:
    """
     í ∏ Ç è ≥   î  ∑ Ω   á µ Ω Ω è  º µ Ç   ∏ ∫ ∏  ∑ Prometheus- Ç µ ∫   Ç É.
     ù      ., name='exposure_equity_usd'
          name='order_state_total' + label_filter='status="FILLED"'
    """
    if not txt: return default
    if label_filter:
        pat = rf'^{re.escape(name)}\{{[^}}]*{label_filter}[^}}]*\}}\s+([0-9eE\.\+\-]+)$'
    else:
        pat = rf'^{re.escape(name)}\s+([0-9eE\.\+\-]+)$'
    for line in txt.splitlines():
        m = re.match(pat, line)
        if m:
            try: return float(m.group(1))
            except Exception: return default
    return default

def main():
    url, out_dir = _get_cfg()
    txt = _scrape_metrics_text(url)
    # exposure
    equity   = _mget(txt, "exposure_equity_usd")
    pos_usd  = _mget(txt, "exposure_positions_usd")
    pend_usd = _mget(txt, "exposure_pending_usd")
    limit_usd= _mget(txt, "exposure_limit_usd")
    # guards
    exp_rej  = _mget(txt, "fsm_guard_rejects_total", 'guard="exposure"')
    ttl_exp  = _mget(txt, "pending_exposure_expired_total")
    daily_blk= _mget(txt, "fsm_guard_rejects_total", 'guard="daily"')  #  è ∫ â æ  ¥ æ ¥     Ç µ  ª ñ á ∏ ª å Ω ∏ ∫  ¥   ª ñ
    # orders
    placed   = _mget(txt, "orders_placed_total")
    filled   = _mget(txt, "orders_filled_total")
    # decisions
    #      ∏ ∫ ª   ¥    ∏ º ≤ æ ª É ‚ î  è ∫ â æ  µ ∫     æ   Ç É î Ç µ  ª µ π ± ª ∏    æ    ∏ º ≤ æ ª É ( Ω µ æ ± æ ≤' è ∑ ∫ æ ≤ æ)
    # rl_eth   = _mget(txt, "decision_rate_limited_total", 'symbol="ETHUSDT"')

    summary = {
        "ts_ms": int(time.time()*1000),
        "exposure": {
            "equity_usd": equity,
            "positions_usd": pos_usd,
            "pending_usd": pend_usd,
            "limit_usd": limit_usd,
            "utilization_pct": ( (pos_usd + pend_usd)/limit_usd*100.0 ) if limit_usd>0 else 0.0
        },
        "guards": {
            "exposure_rejects_total": exp_rej,
            "pending_expired_total": ttl_exp,
            "daily_rejects_total": daily_blk
        },
        "orders": {
            "placed_total": placed,
            "filled_total": filled
        }
    }
    os.makedirs(out_dir, exist_ok=True)
    out_path = os.path.join(out_dir, "summary_gate_status.json")
    with open(out_path, "w", encoding="utf-8") as f:
        json.dump(summary, f, ensure_ascii=False, indent=2)
    print(out_path)

if __name__ == "__main__":
    sys.exit(main())
```

###  ¢ µ   Ç ∏

**PATH:** `tests/units/test_metrics_summary_parse.py`

```python
from tools.metrics_summary import _mget

def test_mget_simple_gauge():
    txt = "exposure_equity_usd 5121.05\n"
    assert _mget(txt, "exposure_equity_usd") == 5121.05

def test_mget_counter_with_label():
    txt = 'fsm_guard_rejects_total{guard="exposure"} 7\n'
    assert _mget(txt, "fsm_guard_rejects_total", 'guard="exposure"') == 7.0
```

###  ö æ º   Ω ¥ ∏

```bash
ruff check . && ruff format .
mypy --strict
pytest -q -k "metrics_summary_parse"
python tools/metrics_summary.py
```

###  ê   Ç µ Ñ   ∫ Ç ∏

* ** î æ ¥   Ω æ:** `tools/metrics_summary.py`, `tests/units/test_metrics_summary_parse.py`
* ** í ∏ Ö ñ ¥:** `reports/summary_gate_status.json`

---

# PACK A4 ‚ î `/statdump` ( î ¥ ∏ Ω ∏ π  ∑ Ω ñ º æ ∫    Ç   Ω É  ¥ ª è Ops)

##  ¶ ñ ª å

 ® ≤ ∏ ¥ ∫ ∏ π JSON- ∑   ñ ∑  ∫ ª é á æ ≤ ∏ Ö  ≤ É ∑ ª ñ ≤ (exposure, daily, ops)  á µ   µ ∑ API.  õ µ ≥ ∫ æ  á ∏ Ç   Ç ∏  ∑ TG- ± æ Ç  /   ∫   ∏   Ç ñ ≤.

##  ö æ ¥ (API)

**PATH:** `apps/reference/api/main.py`
( ¥ æ   æ ≤ Ω µ Ω Ω è  ¥ æ  ≤ ∂ µ  ñ   Ω É é á æ ≥ æ FastAPI  ∑ `/metrics`)

```python
from fastapi import FastAPI
from fastapi.responses import JSONResponse
from vfoundation.apps.reference.telemetry.metrics import generate_latest
import os, re

app = FastAPI(title="Aurora API")

# ...  ≤   à ñ  ñ   Ω É é á ñ  µ Ω ¥   æ ó Ω Ç ∏ (health, metrics) ...

def _mget(txt: str, name: str, label_filter: str | None = None, default: float = 0.0) -> float:
    import re
    if not txt: return default
    if label_filter:
        pat = rf'^{re.escape(name)}\{{[^}}]*{label_filter}[^}}]*\}}\s+([0-9eE\.\+\-]+)$'
    else:
        pat = rf'^{re.escape(name)}\s+([0-9eE\.\+\-]+)$'
    for line in txt.splitlines():
        m = re.match(pat, line)
        if m:
            try: return float(m.group(1))
            except Exception: return default
    return default

@app.get("/statdump")
def statdump():
    #  á ∏ Ç   î º æ  Ç µ      º µ,  â æ  ≤ ñ ¥ ¥   î /metrics,  ∑  ≤ Ω É Ç   ñ à Ω å æ ≥ æ    µ î   Ç   É
    txt = generate_latest().decode("utf-8", "replace")

    equity   = _mget(txt, "exposure_equity_usd")
    pos_usd  = _mget(txt, "exposure_positions_usd")
    pend_usd = _mget(txt, "exposure_pending_usd")
    limit_usd= _mget(txt, "exposure_limit_usd")
    exp_rej  = _mget(txt, "fsm_guard_rejects_total", 'guard="exposure"')
    ttl_exp  = _mget(txt, "pending_exposure_expired_total")
    placed   = _mget(txt, "orders_placed_total")
    filled   = _mget(txt, "orders_filled_total")

    #  ó á ∏ Ç É î º æ  á     Ç ∏ Ω É ops- ∫ æ Ω Ñ ñ ≥   ( á µ   µ ∑      æ   Ç ∏ π env    ± æ  ≤   à ConfigLoader)
    panic = os.environ.get("OPS_PANIC", "false").lower() == "true"

    data = {
        "exposure": {
            "equity_usd": equity,
            "positions_usd": pos_usd,
            "pending_usd": pend_usd,
            "limit_usd": limit_usd,
            "utilization_pct": ((pos_usd+pend_usd)/limit_usd*100.0) if limit_usd>0 else 0.0
        },
        "guards": {
            "exposure_rejects_total": exp_rej,
            "pending_expired_total": ttl_exp
        },
        "orders": {
            "placed_total": placed,
            "filled_total": filled
        },
        "ops": {
            "panic_killswitch": panic
        }
    }
    return JSONResponse(data)
```

>  Ø ∫ â æ  É  ≤      É ∂ µ  î  Ü µ Ω Ç     ª ñ ∑ æ ≤   Ω ∏ π `ConfigLoader`,  º æ ∂ Ω    ∑   º ñ Ω ∏ Ç ∏  á ∏ Ç   Ω Ω è `panic`  Ω      µ   ª å Ω µ  ¥ ∂ µ   µ ª æ  ∫ æ Ω Ñ ñ ≥ ñ ≤.  ú     à   É Ç  Ω µ  ª   º   î  ñ   Ω É é á ñ  µ Ω ¥   æ ó Ω Ç ∏.

###  ¢ µ   Ç ∏

**PATH:** `tests/integration/test_statdump_endpoint.py`

```python
from fastapi.testclient import TestClient
from apps.reference.api.main import app

def test_statdump_ok():
    c = TestClient(app)
    r = c.get("/statdump")
    assert r.status_code == 200
    body = r.json()
    assert "exposure" in body and "orders" in body and "guards" in body
```

###  ö æ º   Ω ¥ ∏

```bash
ruff check . && ruff format .
mypy --strict
pytest -q tests/integration/test_statdump_endpoint.py
```

###  ê   Ç µ Ñ   ∫ Ç ∏

* ** ó º ñ Ω µ Ω æ:** `apps/reference/api/main.py`
* ** î æ ¥   Ω æ:** `tests/integration/test_statdump_endpoint.py`

---

##  û Ω æ ≤ ª µ Ω Ω è JOURNAL/TODO

*  î æ ¥   Ç ∏  ∑     ∏    É `JOURNAL.md`: **L3-METRICS-SUMMARY (reports/summary_gate_status.json)**  Ç   **A4-OPS-/statdump** ‚ î  ≤ ∏ ∫ æ Ω   Ω ñ/ ≤      æ Ü µ   ñ.
*  £ `TODO.md`:    É Ω ∫ Ç  Ω   **Grafana dashboard JSON** ( æ   Ü ñ π Ω æ),    ª é   ‚ ú   µ   µ ≤ µ   Ç ∏ panic/quiet/allowlist  ñ ∑ YAML  É runtime API (/ops/panic on|off)‚ ù  É  Ω     Ç É   Ω ñ π  ñ Ç µ     Ü ñ ó.

---

##  ö æ º   Ω ¥ ∏  ∑   ≥   ª æ º

```bash
ruff check . && ruff format .
mypy --strict
pytest -q
python tools/metrics_summary.py
```

 Ø ∫  ∑ º µ   ¥ ∂ ∏ Ç µ  Ü ñ  ¥ ≤        ∫ µ Ç ∏ ‚ î  É  Ω      ± É ¥ µ **   æ ≤ Ω ∏ π      æ ¥- ∫ æ Ω Ç É  **:  ± µ ∑   µ á Ω ∏ π open-flow,    Ç æ  - ¥ µ Ω å,      Ω ñ ∫  / Ç ∏ à  ,  º µ Ç   ∏ ∫ ∏,  ∑ ≤ µ ¥ µ Ω ∏ π  ∑ ≤ ñ Ç  ñ    Ç   Ç ¥   º  .  ì æ Ç æ ≤ ∏ π    µ   µ Ö æ ¥ ∏ Ç ∏  ¥ æ  ª µ ≥ ∫ æ ≥ æ Grafana- ¥   à ± æ   ¥ É    ± æ  ¥ æ  º ñ Ω ñ º   ª å Ω æ ó Telegram- ñ Ω Ç µ ≥     Ü ñ ó (status/statdump/panic).


 í æ Ç  á ë Ç ∫ ∏ π **TaskSpec  ¥ ª è Copilot** (RU).  í ã   æ ª Ω ∏ ** ∏ ¥ µ º   æ Ç µ Ω Ç Ω æ**,  ± µ ∑  ¥ æ ≥   ¥ æ ∫.  ¶ µ ª å ‚ î  ≥       Ω Ç ∏   æ ≤   Ω Ω æ ** æ Ç ∫ ª é á ∏ Ç å    ≤ Ç æ- º µ Ω µ ¥ ∂ µ      æ ∑ ∏ Ü ∏ π** (bracket-only),  æ ± Ω æ ≤ ∏ Ç å  ∫ æ Ω Ñ ∏ ≥/   Ö µ º É/ ∫ æ ¥,      æ ≤   ª ∏ ¥ ∏   æ ≤   Ç å  ∏    æ ¥ Ç ≤ µ   ¥ ∏ Ç å  ≤  ª æ ≥   Ö/ º µ Ç   ∏ ∫   Ö.

---

##  ó   ¥   á  : Disable Auto-Manage (Bracket-Only) +  í   ª ∏ ¥   Ü ∏ è

###  ö æ Ω Ç µ ∫   Ç /  ò Ω ≤     ∏   Ω Ç ã

*  ê ∫ Ç ∏ ≤ Ω ã π  ∫ æ Ω Ñ ∏ ≥  á ∏ Ç   µ Ç   è  ∏ ∑ `.env`  ∫ ª é á æ º `AURORA_CONFIG_NAME`.  ï   ª ∏  ∫ ª é á  æ Ç   É Ç   Ç ≤ É µ Ç ‚Üí  ∏     æ ª å ∑ É µ º `configs/master_config_v1.yaml`.
*  í bracket-   µ ∂ ∏ º µ **Manage FSM  ¥ æ ª ∂ µ Ω      Ω Ω µ  ≤ ã Ö æ ¥ ∏ Ç å**  ∏  ù ï  ≤ ã   æ ª Ω è Ç å  Ç   µ π ª/ ±   µ π ∫- ∏ ≤ µ Ω/ Ç   π º-   Ç æ  .
*  ¢ µ ª µ º µ Ç   ∏ è:      ∏  æ Ç ∫ ª é á ë Ω Ω æ º  º µ Ω µ ¥ ∂ µ   µ  ∏ Ω ∫   µ º µ Ω Ç ∏ º `manage_skipped_total`  ∏  ç º ∏ Ç ∏ º `EVT:MANAGE_SKIPPED`.

---

##  ®   ≥ ∏ (    Ñ   π ª   º ∏  ∏  ∫ æ ¥ æ º)

### 1)  ù   π Ç ∏    ∫ Ç ∏ ≤ Ω ã π  ∫ æ Ω Ñ ∏ ≥

*  û Ç ∫   æ π `.env`  ≤  ∫ æ   Ω µ    µ   æ.

  *  ï   ª ∏  µ   Ç å `AURORA_CONFIG_NAME=...` ‚ î  ∑     æ º Ω ∏    É Ç å ( ¥   ª µ µ: `<ACTIVE_CFG>`).
  *  ï   ª ∏  ∫ ª é á    Ω µ Ç ‚ î    á ∏ Ç   Ç å `<ACTIVE_CFG>` = `configs/master_config_v1.yaml`.

### 2)  ü     ≤ ∫      ∫ Ç ∏ ≤ Ω æ ≥ æ YAML

**PATH:** `<ACTIVE_CFG>`
 î æ ±   ≤ å ( ∏ ª ∏  æ ± Ω æ ≤ ∏)    µ ∫ Ü ∏ é:

```yaml
execution:
  manage:
    auto: false
```

>  ¢   µ ± æ ≤   Ω ∏ µ:  Ω µ  º µ Ω è Ç å  ¥   É ≥ ∏ µ  ∑ Ω   á µ Ω ∏ è;  µ   ª ∏ `execution`/`manage`  æ Ç   É Ç   Ç ≤ É é Ç ‚ î    æ ∑ ¥   Ç å.

### 3)  û ± Ω æ ≤ ∏ Ç å JSON-   Ö µ º É

**PATH:** `config/_schemas/aurora_trading.schema.json`
 í Ω É Ç   ∏ `"execution"`  ¥ æ ±   ≤ å `"manage.auto"`:

```json
"execution": {
  "type": "object",
  "properties": {
    "manage": {
      "type": "object",
      "properties": {
        "auto": { "type": "boolean", "default": false }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
```

### 4)  ü   ∏ ≤ è ∑   Ç å  Ñ ª   ≥  ≤ Manage FSM

**PATH:** `vfoundation/apps/reference/domains/execution_position/fsm_manage.py`

*  í `__init__`      æ á ∏ Ç   π  Ñ ª   ≥:

```python
cfg_exec = (self.cfg.get("execution") or {})
manage_cfg = (cfg_exec.get("manage") or {})
self._auto_manage_enabled = bool(manage_cfg.get("auto", False))
```

*  í  Ω   á   ª µ  æ   Ω æ ≤ Ω æ ≥ æ  æ ±     ± æ Ç á ∏ ∫   ( ∏ ª ∏  ≤  º µ   Ç µ  ≤ Ö æ ¥    ª æ ≥ ∏ ∫ ∏)  ¥ æ ±   ≤ å      Ω Ω ∏ π  ≤ ã Ö æ ¥:

```python
from vfoundation.apps.reference.telemetry.metrics import inc_manage_skipped
from vfoundation.core.protocol import Message

if not self._auto_manage_enabled:
    #  Ç µ ª µ º µ Ç   ∏ è +    æ ± ã Ç ∏ µ  ¥ ª è  Ω   ± ª é ¥   µ º æ   Ç ∏
    inc_manage_skipped()
    await self.emit(Message(
        op="EVT", verb="MANAGE_SKIPPED", intent="OBSERVATION",
        src="execution_position", dst="any", rid=getattr(msg, "rid", None),
        pld={"symbol": (msg.pld or {}).get("symbol"), "why": "manage_disabled"},
        why="manage_disabled"
    ))
    return
```

>  ù µ  ∏ ∑ º µ Ω è π  æ   Ç   ª å Ω É é  ª æ ≥ ∏ ∫ É  º µ Ω µ ¥ ∂ µ    .

### 5)  í   ª ∏ ¥   Ç æ    ∫ æ Ω Ñ ∏ ≥    ∏    Ç   Ç ∏ á µ   ∫   è      æ ≤ µ   ∫  

 í ã   æ ª Ω ∏:

```bash
ruff check .
ruff format .
mypy --strict
python tools/auroractl.py config-validate
```

### 6)  ú ∏ Ω ∏- Ç µ   Ç ( µ   ª ∏  Ω µ Ç ‚ î    æ ∑ ¥   Ç å)

**PATH:** `tests/units/test_manage_disabled.py`

```python
import pytest
from types import SimpleNamespace
from vfoundation.core.protocol import Message

@pytest.mark.asyncio
async def test_manage_disabled_emits_skipped(monkeypatch):
    #  ó   ≥ ª É à ∫   FSM- æ ± æ ª æ á ∫ ∏: capture emit()
    emitted = []
    class DummyFSM:
        async def emit(self, m): emitted.append(m)

    #  ò º   æ   Ç ∏   É µ º  ∫ ª       Manage FSM  ∏ ∑      æ µ ∫ Ç  
    from vfoundation.apps.reference.domains.execution_position import fsm_manage
    fsm = fsm_manage.ManageFlowFSM(
        fsm=DummyFSM(),
        cfg={"execution":{"manage":{"auto": False}}},
        logger=SimpleNamespace(info=lambda *a,**k:None, warning=lambda *a,**k:None)
    )
    msg = Message(op="EVT", verb="TICK", intent="OBSERVATION", src="x", dst="y", rid="r1", pld={"symbol":"ETHUSDT"}, why="test")
    #  ≤ ã ∑ æ ≤ ∏  æ   Ω æ ≤ Ω æ π  æ ±     ± æ Ç á ∏ ∫ ( É ∫   ∂ ∏  ∫ æ     µ ∫ Ç Ω ã π  º µ Ç æ ¥  ≤   à µ ≥ æ FSM)
    await fsm.handle(msg)  #      ∏  Ω µ æ ± Ö æ ¥ ∏ º æ   Ç ∏    æ ¥   Ç   æ π  Ç æ á Ω æ µ  ∏ º è  º µ Ç æ ¥  

    assert any(m.verb == "MANAGE_SKIPPED" for m in emitted)
```

>  ï   ª ∏    ∏ ≥ Ω   Ç É     `ManageFlowFSM`  ∏  º µ Ç æ ¥    æ Ç ª ∏ á   µ Ç   è ‚ î    æ ¥   Ç   æ π  ≤ ã ∑ æ ≤,  Ω æ    É Ç å:      ∏ `auto:false`  ¥ æ ª ∂ µ Ω  ç º ∏ Ç ∏ Ç å   è `MANAGE_SKIPPED`  ∏  Ω µ      æ ∏   Ö æ ¥ ∏ Ç å  Ω ∏ ∫   ∫ æ π  É       ≤ ª µ Ω á µ   ∫ æ π  ª æ ≥ ∏ ∫ ∏.

### 7)  ü µ   µ ∑     É   ∫  ∏      æ ≤ µ   ∫  

*  ü µ   µ ∑     É   Ç ∏    µ   ≤ ∏    ∫   ∫      ∏ Ω è Ç æ  ≤      æ µ ∫ Ç µ.
*  £ ± µ ¥ ∏   å:

  *  í `/metrics`        Ç ë Ç `manage_skipped_total`.
  *  í  ª æ ≥   Ö    æ ± ã Ç ∏ π    æ è ≤ ª è µ Ç   è `EVT:MANAGE_SKIPPED`.
  *  ù µ Ç  Ç   µ π ª/ ±   µ π ∫- ∏ ≤ µ Ω/ Ç   π º-   Ç æ    ¥ µ π   Ç ≤ ∏ π ‚ î  æ   Ç   ë Ç   è **bracket-only** (TP/SL  Ω    ± ∏   ∂ µ).

---

##  ö æ º º ∏ Ç ã /      Ç µ Ñ   ∫ Ç ã

* ** ò ∑ º µ Ω ë Ω Ω ã µ  Ñ   π ª ã:**

  * `<ACTIVE_CFG>` ( ¥ æ ±   ≤ ª µ Ω `execution.manage.auto: false`)
  * `config/_schemas/aurora_trading.schema.json`
  * `vfoundation/apps/reference/domains/execution_position/fsm_manage.py`

* ** î æ ±   ≤ ª µ Ω Ω ã µ  Ñ   π ª ã:**

  * `tests/units/test_manage_disabled.py` ( µ   ª ∏  Ω µ  ± ã ª æ)

* ** ö æ º º ∏ Ç-   æ æ ± â µ Ω ∏ µ:**

```
feat(exec-manage): disable auto-manage via config flag, early exit + telemetry [A3-MANAGE-OFF]
```

* **DoD:**

  *  ü   ∏ `execution.manage.auto:false` Manage FSM ** Ω µ  ≤ ã   æ ª Ω è µ Ç**  ¥ ∏ Ω   º ∏ á µ   ∫ æ µ  ≤ µ ¥ µ Ω ∏ µ.
  *  ≠ º ∏ Ç ∏ Ç   è `EVT:MANAGE_SKIPPED`,  º µ Ç   ∏ ∫   `manage_skipped_total`  ∏ Ω ∫   µ º µ Ω Ç ∏ Ç   è.
  * `config-validate`, `ruff`, `mypy --strict`,  Ç µ   Ç ã ‚ î  ∑ µ ª ë Ω ã µ.
  *  í runtime ‚ î ** Ç æ ª å ∫ æ**  ± ∏   ∂ µ ≤ ã µ TP/SL;  É       ≤ ª µ Ω ∏ µ    æ ∑ ∏ Ü ∏ µ π  º µ Ω µ ¥ ∂ µ   æ º  ≤ ã ∫ ª é á µ Ω æ.

---

** ° ¥ µ ª   π    æ ≤ Ω æ    æ    É Ω ∫ Ç   º.**  ï   ª ∏  ≥ ¥ µ- Ç æ    Ç   É ∫ Ç É    /   ∏ ≥ Ω   Ç É   ã  æ Ç ª ∏ á   é Ç   è ‚ î    æ ¥   Ç   æ π  ≤ ã ∑ æ ≤ ã,  Ω æ  ∏ Ω ≤     ∏   Ω Ç ã ( Ñ ª   ≥  ≤ YAML ‚Üí    Ö µ º   ‚Üí      Ω Ω ∏ π  ≤ ã Ö æ ¥ FSM ‚Üí  Ç µ ª µ º µ Ç   ∏ è ‚Üí  Ç µ   Ç ‚Üí      æ ≤ µ   ∫  )  æ ± è ∑   Ω ã    æ Ö     Ω ∏ Ç å   è.
