# План Інтеграції LLA та vFoundation

**Вер� ія:** 1.0
**Дата:** 2025-10-26
**Автор:** Gemini AI Agent

## 1. Мета проєкту

Головна мета — інтегрувати � амонавчальний орке� тратор LLA (Living Latent Agent) з і� нуючою доменною архітектурою vFoundation. Це перетворить � и� тему зі � татичної, керованої правилами федерації FSM, на динамічну, адаптивну � и� тему, яка � амо� тійно оптимізує по� лідовні� ть взаємодії між � воїми компонентами. Кінцевою ціллю є автоматична мак� имізація прибутково� ті (`+PnL`) при дотриманні заданих обмежень ризику (`-CVaR`), � табільно� ті та продуктивно� ті.

## 2. Вимоги до функціоналу

### Функціональні вимоги:
-   **Збір даних:** LLA має отримувати події (`events`) та метрики (`metrics`) з у� іх ключових доменів vFoundation (execution, risk, data_provider) через � тандартизовані "мо� ти" (Bridges).
-   **Орке� трація:** LLA (в ролі Meta-FSM/CA-FSM) має керувати потоком виконання, динамічно вирішуючи, який домен і коли активувати.
-   **Навчання та Адаптація:** Си� тема повинна аналізувати результати � воїх дій через "О� ьовий Градієнт" (Axial Gradient) і по� тупово змінювати параметри � воєї логіки (ваги, пороги), щоб покращити показник AG.
-   **Безпека Адаптацій:** `Proof Kernel` повинен валідувати кожну запропоновану адаптацію на відповідні� ть фундаментальним інваріантам безпеки (LTL Invariants). Жодна зміна, що порушує безпеку, не повинна бути за� то� ована.
-   **Управління:** LLA має над� илати команди (наприклад, зміна параметрів ризику, ініціація дій) назад у домени vFoundation через `CommandBridge`.
-   **"Тіньовий" режим (Shadow Mode):** Си� тема повинна підтримувати режим, у якому LLA навчаєть� я на реальних даних, але його команди лише логують� я, а не виконують� я.

### Нефункціональні вимоги:
-   **Надійні� ть:** Си� тема повинна бути відмово� тійкою. Збій одного з компонентів не повинен призводити до ка� кадного збою. Механізми відновлення � тану (DR/Hydration) мають бути збережені та розширені для LLA.
-   **Продуктивні� ть:** Критичний "гарячий шлях" (hot path) в� ередині доменів vFoundation має відповідати і� нуючим SLO (p95 ≤ 50ms). Цикл адаптації LLA є "холодним шляхом" (cold path) і може виконувати� я з меншою ча� тотою (наприклад, раз на кілька � екунд/хвилин).
-   **Прозорі� ть (Observability):** Кожне рішення LLA та кожна адаптація повинні мати чіткий "причинний � лід" (`causal_trace`) та запи� увати� я в лог для подальшого аналізу.
-   **Підтримувані� ть:** Інтерфей� и між LLA та vFoundation (мо� ти) мають бути чітко визначені, � лабко зв'язані та задокументовані, щоб � про� тити подальшу розробку.

## 3. Технологічний � тек

-   **Мова програмування:** Python 3.11
-   **О� новний фреймворк:** vFoundation (і� нуюча кодова база)
-   **Орке� тратор:** LLA (на о� нові кодової бази з `LLA-r2-ci-harden-cvar-dod`)
-   **Контракти даних:** YAML (� ловники) та JSON Schema 2020-12
-   **Валідація даних (Runtime):** Pydantic
-   **Те� тування:** Pytest, `unittest.mock`
-   **Інфра� труктура:** Локальна файлова � и� тема для логів, WAL, � танів. Архітектура має бути готова до майбутньої інтеграції з хмарними � ховищами (напр., S3).

## 4. Етапи розробки (з термінами)

План базуєть� я на інкрементальному підході, опи� аному в `vFoundation_LLA.md`.

-   **Sprint 0: Підготовка та закриття боргів (3-4 дні)**
    -   **Завдання:** Напи� ати від� утні юніт- та інтеграційні те� ти для і� нуючої бази vFoundation, виявлені під ча�  аудиту (ідемпотентні� ть, гідратація, portfolio risk, E2E).
    -   **DoD:** Покриття те� тами критичних компонентів > 95%. В� і те� ти "зелені".

-   **Sprint A: Інфра� труктура та Мо� ти (Bridges) (2-3 дні)**
    -   **Завдання:** Створити � труктуру директорій (`foundation/adapters/`, `foundation/meta_fsm/`). Реалізувати базові `EventBridge`, `MetricBridge`, `CommandBridge`, які поки що лише логують дані у файли. Інтегрувати виклики `EventBridge` у ключові точки доменів vFoundation.
    -   **DoD:** Файл `logs/causal_events.jsonl` наповнюєть� я подіями з доменів під ча�  те� тового прогону.

-   **Sprint B: Карка�  CA-FSM (3-4 дні)**
    -   **Завдання:** Інтегрувати базову логіку `CAFSM` з `run_r0.py`. Налаштувати головний цикл, який реагує на події з мо� тів і приймає рішення на о� нові *� татичної* (поки що) конфігурації переходів з `ca_fsm.yaml`.
    -   **DoD:** Си� тема здатна прове� ти один повний "епізод" (від � игналу до умовного закриття позиції) під керуванням CA-FSM.

-   **Sprint C: О� ьовий Градієнт (AG) та Оцінювач (3-4 дні)**
    -   **Завдання:** Реалізувати `axial_gradient.py` та `evaluator.py`. Підключити їх до `CAFSM` для розрахунку та логування значення AG для кожного епізоду. **Тюнер адаптацій вимкнено (log-only)**.
    -   **DoD:** Файл `logs/ag_eval.jsonl` наповнюєть� я реалі� тичними значеннями AG.

-   **Sprint D: Proof Kernel (4-6 днів)**
    -   **Завдання:** Реалізувати мінімальний `Proof Kernel` з базовими LTL-інваріантами (напр., "не відкривати дублюючі позиції"). Інтегрувати його як "запобіжник", який перевіряє команди від LLA.
    -   **DoD:** Спроба LLA згенерувати небезпечну команду (в те� ті) блокуєть� я, і цей факт логуєть� я в `logs/proof_record.jsonl`.

-   **Sprint E-G: Інтеграція, CI та 72-годинний "тіньовий" прогін (5-7 днів)**
    -   **Завдання:** Повні� тю інтегрувати в� і компоненти. Налаштувати CI для запу� ку в� іх те� тів. Запу� тити � и� тему в режимі "Shadow Mode" на 72 години. Активувати базовий тюнер адаптацій.
    -   **DoD:** Си� тема � табільно працює 72 години без критичних помилок. AG демон� трує позитивну динаміку. `R2_72H_READY=YES`.

**Орієнтовний загальний ча� :** 4-6 тижнів.

## 5. Критерії у� піху (KPI)

-   **Технічні KPI:**
    -   У� пішне завершення 72-годинного "тіньового" прогону без критичних помилок.
    -   Покриття те� тами нового коду (мо� ти, LLA-адаптери) > 95%.
    -   Кількі� ть � працювань `Proof Kernel` (блокувань небезпечних дій) дорівнює 0 у � табільному режимі.
-   **Бізне� -KPI (в режимі � имуляції):**
    -   Позитивна динаміка � ереднього значення О� ьового Градієнта (`AG`) протягом "тіньового" прогону.
    -   Перевершення показників (Sharpe, Profit Factor) � татичної вер� ії архітектури на тих � амих і� торичних даних.

## 6. Ризики та їх мінімізація

-   **Ризик 1: Технічний борг у те� тах.**
    -   *Опи� :* Початок інтеграції LLA на недо� татньо проте� тованій базі vFoundation може призве� ти до важкодіагно� тованих помилок.
    -   *Мінімізація:* **Виконати Sprint 0 (закриття боргів по те� тах) перед початком активної інтеграції LLA.**
-   **Ризик 2: Складні� ть інтеграції.**
    -   *Опи� :* Неправильне проєктування "мо� тів" може призве� ти до � ильної зв'язано� ті (tight coupling) між LLA та vFoundation.
    -   *Мінімізація:* Суворо дотримувати� я "contract-first" підходу для мо� тів. Інтерфей� и мають бути мінімалі� тичними та а� инхронними.
-   **Ризик 3: Не� табільне навчання LLA.**
    -   *Опи� :* На початкових етапах LLA може генерувати хаотичні або неоптимальні по� лідовно� ті команд, що призведе до "тремтіння" � и� теми.
    -   *Мінімізація:* Запу� кати LLA з вимкненим тюнером (log-only) на перших етапах. Викори� товувати `Proof Kernel` як жор� ткий запобіжник. Проводити тривалі прогони в "shadow mode".

## 7. План те� тування

1.  **Юніт-те� ти:** Кожен новий модуль (`EventBridge`, `AxialGradient`, `ProofKernel` і т.д.) буде покрито вла� ним набором юніт-те� тів.
2.  **Інтеграційні те� ти:**
    -   Те� тування кожного "мо� та" окремо (напр., чи правильно `MetricBridge` агрегує дані з `PositionTracking`).
    -   Те� тування повного циклу LLA з імітованими (mocked) доменами vFoundation.
3.  **E2E (End-to-End) Те� тування:**
    -   Головним E2E те� том є **72-годинний "тіньовий" прогін**, який валідує поведінку в� ієї � и� теми на реальних даних.
4.  **Регре� ійне те� тування:** Повний набір те� тів (`pytest`) буде запу� кати� я в CI пі� ля кожної значної зміни.

## 8. План розгортання

Розгортання буде відбувати� я поетапно, з по� туповим підвищенням рівня ризику.

1.  **Етап 1: Shadow Mode (Тіньовий режим).** LLA працює з реальними ринковими даними, приймає рішення, але його команди лише логують� я. Си� тема не виконує реальних угод.
2.  **Етап 2: Paper Trading (Паперова торгівля).** `CommandBridge` налаштовуєть� я на відправку ордерів на те� товий (паперовий) акаунт біржі.
3.  **Етап 3: Live Limited (Обмежений реальний режим).** Си� тема запу� каєть� я на реальному акаунті з мінімальним капіталом та для одного, найбільш � табільного ін� трументу.
4.  **Етап 4: Live Full (Повний реальний режим).** По� тупове збільшення капіталу та кілько� ті торгових ін� трументів під по� тійним моніторингом KPI.

## 9. План підтримки

-   **Моніторинг:** Налаштування дашбордів для від� теження ключових KPI (� ередній AG, PnL, drawdown, кількі� ть LTL-порушень, � тан "мо� тів"). Налаштування автоматичних � повіщень (alerts) на критичні помилки та порушення інваріантів.
-   **Логування:** У� і артефакти LLA (`causal_events.jsonl`, `ag_eval.jsonl`, `proof_record.jsonl`) мають централізовано збирати� я, архівувати� я та бути до� тупними для аналізу.
-   **План відкату (Rollback Plan):** Має і� нувати чітка процедура для негайного відключення адаптивного механізму LLA. Це можна реалізувати через "feature flag", який перемикає � и� тему назад до � татичної логіки орке� трації, минаючи `CAFSM`.
