# Файл: tests/config/aurora/system.yaml — Тестове системне налаштування
# Мета: мінімальні системні параметри, які потрібні під час тестового запуску.
#
# Пояснення:
# - trading_mode: вказує середовище запуску для тестів (testnet/live)
# - logging.level: рівень логування під час тестів

# --- Оригінальний вміст файлу ---
trading_mode: "testnet"
logging:
  level: "INFO"
