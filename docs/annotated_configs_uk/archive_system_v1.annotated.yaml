# Файл: config/archive/v1/system.yaml — Архівна системна конфігурація (v1)
# Мета: історичні налаштування середовища та логування для відтворення старих запусків.

# --- Оригінальний вміст файлу ---
trading_mode: "testnet"
logging:
  level: "INFO"
