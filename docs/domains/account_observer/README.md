# Account Observer Domain (Домен Спо� терігача Рахунку)

## Огляд

Домен `account_observer` є критичним компонентом � и� теми Aurora, що забезпечує па� ивний моніторинг торгової активно� ті на Binance Futures. Він викори� товує read-only API ключі для періодичного опитування біржі та емі� ії подій про виконані торгові операції.

## Архітектурна роль

### Відповідальні� ть
- **Моніторинг трейдів:** Па� ивне � по� тереження за виконаними угодами через Binance API
- **Дедуплікація:** Від� теження оброблених трейдів для уникнення дублікатів подій
- **Стандартизація даних:** Перетворення � ирих API даних в � труктуровані FSM події
- **Надійні� ть:** Забезпечення � и� теми актуальною інформацією про торгову активні� ть

### Критичні� ть для � и� теми
Домен є фундаментальним для:
- **Position Tracking** - � инхронізація � тану позицій з реальними трейдами
- **Risk Management** - перерахунок ризиків пі� ля виконання операцій
- **Audit Trail** - запи�  в� іх операцій для аудиту та аналізу

## Структура домену

```
account_observer/
├── README.md              # Цей файл - огляд домену
├── documentation.md       # Детальна документація FSM подій та взаємодій
├── architecture.md        # Архітектурна � хема та робочі проце� и
├── dictionaries.yaml      # Словники даних та конфігурації
├── functions.md           # Реалізовані функції та методи
├── examples/              # Приклади коду та конфігурації
│   ├── config.yaml        # Приклади конфігурації
│   ├── usage.py           # Приклад викори� тання
│   └── tests/             # Те� тові приклади
└── schema.json           # JSON Schema для валідації подій
```

## О� новні компоненти

### AccountObserver
Головний кла�  домену, що реалізує моніторинг трейдів через Binance API.

**Ключові можливо� ті:**
- Автоматичний вибір API залежно від режиму роботи (live/testnet/hybrid)
- Фоновий моніторинг з конфігурованим інтервалом
- Дедуплікація трейдів за ID для уникнення дублікатів
- Структурована емі� ія FSM подій про виконані операції

## FSM події

### Генеровані події
- `EVT:TRADE_EXECUTED` - повідомлення про виконання нової торгової операції

### Споживані події
Домен працює автономно і не � поживає зовнішні події.

## Взаємодії з доменами

### Синхронні зв'язки
- **Position Tracking** ← `EVT:TRADE_EXECUTED`
- **Risk Management** ← `EVT:TRADE_EXECUTED`
- **Audit Trail** ← `EVT:TRADE_EXECUTED`

### А� инхронні залежно� ті
Домен працює незалежно, але його події критичні для в� іх компонентів, що від� тежують позиції.

## Конфігурація

### О� новні параметри
```yaml
# Загальна конфігурація � и� теми
trading_mode: "testnet"

# API конфігурація
binance_api:
  testnet:
    api_key: "${BINANCE_TESTNET_API_KEY}"
    api_secret: "${BINANCE_TESTNET_API_SECRET}"

# Конфігурація домену account_observer
account_observer:
  poll_interval: 5        # � екунди між опитуваннями
  symbols:                # � имволи для моніторингу
    - BTCUSDT
    - ETHUSDT
    - BNBUSDT
  trade_limit: 50         # кількі� ть трейдів за запит
```
  trade_limit: 50        # кількі� ть трейдів для перевірки
```

## Моніторинг

### Метрики
- `account_observer.trades_processed` - Кількі� ть оброблених трейдів
- `account_observer.new_trades_detected` - Кількі� ть нових трейдів
- `account_observer.api_errors` - Кількі� ть помилок API
- `account_observer.poll_interval_actual` - Фактичний інтервал опитування

### Логування
- INFO: Виявлення нових трейдів, емі� ія подій
- DEBUG: Деталі опитування кожного � имволу
- ERROR: Критичні помилки API або пар� ингу

## Запу� к та викори� тання

### Ініціалізація
```python
from apps.reference.domains.account_observer import AccountObserver

observer = AccountObserver(fsm, config)
observer.start()
```

### Моніторинг активно� ті
```python
# Домен працює автономно, події емітують� я автоматично
# EVT:TRADE_EXECUTED генеруєть� я при виявленні нових трейдів
```

## Те� тування

### Інтеграційні те� ти
```bash
pytest tests/domains/account_observer/ -v
```

### Модульні те� ти
```bash
pytest tests/unit/domains/account_observer/ -v
```

## Безпека

### Read-only до� туп
- Викори� тання тільки read-only API ключів
- Неможливі� ть виконання торгових операцій
- Мінімальні дозволи для моніторингу трейдів

### Аудит
- В� і виявлені трейди логують� я для аудиту
- Детальне логування в� іх операцій
- Від� теження оброблених трейдів

## Розробка

### Додавання нових функцій
1. Оновіть `dictionaries.yaml` з новими � труктурами даних
2. Додайте методи в `AccountObserver`
3. Оновіть `schema.json` для нових подій
4. Напишіть те� ти
5. Оновіть документацію

### Контрибуція
- Дотримуйте� ь конвенцій кодування проекту
- Додавайте те� ти для нових функцій
- Оновлюйте документацію при змінах

## Дивіть� я також

- [Детальна документація](documentation.md) - FSM події та взаємодії
- [Архітектурна � хема](architecture.md) - Робочі проце� и та діаграми
- [Словники даних](dictionaries.yaml) - Структури даних
- [Реалізовані функції](functions.md) - API та методи</content>
<parameter name="filePath">c:\Users\job11\Music\Olimp_v1\docs\domains\account_observer\README.md