#  ü   ∏ ∫ ª   ¥  ∫ æ Ω Ñ ñ ≥ É     Ü ñ ó  ¥ æ º µ Ω É Account Observer

##  ü   æ ¥ É ∫ Ç ∏ ≤ Ω    ∫ æ Ω Ñ ñ ≥ É     Ü ñ è (production.yaml)

```yaml
#  ü æ ≤ Ω    ∫ æ Ω Ñ ñ ≥ É     Ü ñ è    ∏   Ç µ º ∏  ∑ account_observer  ¥ æ º µ Ω æ º
trading_mode: live

binance_api:
  live:
    api_key: "${BINANCE_LIVE_API_KEY}"
    api_secret: "${BINANCE_LIVE_API_SECRET}"

account_observer:
  #  ü       º µ Ç   ∏  º æ Ω ñ Ç æ   ∏ Ω ≥ É
  poll_interval: 5          #  û   ∏ Ç É ≤   Ω Ω è  ∫ æ ∂ Ω ñ 5    µ ∫ É Ω ¥
  trade_limit: 50          #  û   Ç   Ω Ω ñ 50  Ç   µ π ¥ ñ ≤  ∑    ∑     ∏ Ç

  #  °   ∏   æ ∫    ∏ º ≤ æ ª ñ ≤  ¥ ª è  º æ Ω ñ Ç æ   ∏ Ω ≥ É
  symbols:
    - BTCUSDT
    - ETHUSDT
    - BNBUSDT
    - ADAUSDT
    - SOLUSDT
    - DOTUSDT
    - LINKUSDT
    - LTCUSDT
```

##  ¢ µ   Ç æ ≤    ∫ æ Ω Ñ ñ ≥ É     Ü ñ è (testnet.yaml)

```yaml
trading_mode: testnet

binance_api:
  testnet:
    api_key: "${BINANCE_TESTNET_API_KEY}"
    api_secret: "${BINANCE_TESTNET_API_SECRET}"

account_observer:
  poll_interval: 10
  trade_limit: 20
  symbols:
    - BTCUSDT
    - ETHUSDT
```

##  ú ñ Ω ñ º   ª å Ω    ∫ æ Ω Ñ ñ ≥ É     Ü ñ è (minimal.yaml)

```yaml
trading_mode: live

binance_api:
  live:
    api_key: "your_api_key"
    api_secret: "your_api_secret"

account_observer:
  symbols: [BTCUSDT]
```

##  ö æ Ω Ñ ñ ≥ É     Ü ñ è  ∑  Ñ ñ ª å Ç     º ∏ (filtered.yaml)

```yaml
trading_mode: live

binance_api:
  live:
    api_key: "${BINANCE_LIVE_API_KEY}"
    api_secret: "${BINANCE_LIVE_API_SECRET}"

account_observer:
  poll_interval: 3
  trade_limit: 100

  #  † æ ∑ à ∏   µ Ω ∏ π      ∏   æ ∫    ∏ º ≤ æ ª ñ ≤
  symbols:
    - BTCUSDT
    - ETHUSDT
    - BNBUSDT
    - ADAUSDT
    - SOLUSDT
    - MATICUSDT
    - AVAXUSDT
    - LINKUSDT
    - DOTUSDT
    - ALGOUSDT
```

##  ö æ Ω Ñ ñ ≥ É     Ü ñ è  ¥ ª è  ≤ ∏   æ ∫ æ ó  á     Ç æ Ç ∏ (high_frequency.yaml)

```yaml
trading_mode: live

binance_api:
  live:
    api_key: "${BINANCE_LIVE_API_KEY}"
    api_secret: "${BINANCE_LIVE_API_SECRET}"

account_observer:
  poll_interval: 1           #  ê ≥   µ   ∏ ≤ Ω µ  æ   ∏ Ç É ≤   Ω Ω è
  trade_limit: 100          #  ë ñ ª å à µ  Ç   µ π ¥ ñ ≤  ∑        ∑

  #  û ± º µ ∂ µ Ω ∏ π  Ω   ± ñ    ≤ ∏   æ ∫ æ ª ñ ∫ ≤ ñ ¥ Ω ∏ Ö    ∏ º ≤ æ ª ñ ≤
  symbols:
    - BTCUSDT
    - ETHUSDT
```

##  ö æ Ω Ñ ñ ≥ É     Ü ñ è  ¥ ª è debugging (debug.yaml)

```yaml
trading_mode: testnet

binance_api:
  testnet:
    api_key: "${BINANCE_TESTNET_API_KEY}"
    api_secret: "${BINANCE_TESTNET_API_SECRET}"

account_observer:
  poll_interval: 30          #  ü æ ≤ ñ ª å Ω µ  æ   ∏ Ç É ≤   Ω Ω è  ¥ ª è    Ω   ª ñ ∑ É
  trade_limit: 10           #  ú ñ Ω ñ º É º  Ç   µ π ¥ ñ ≤

  symbols:
    - BTCUSDT
```</content>
<parameter name="filePath">c:\Users\job11\Music\Olimp_v1\docs\domains\account_observer\examples\config_examples.yaml