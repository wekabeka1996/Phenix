# Приклад конфігурації домену Account Observer

## Продуктивна конфігурація (production.yaml)

```yaml
# Повна конфігурація системи з account_observer доменом
trading_mode: live

binance_api:
  live:
    api_key: "${BINANCE_LIVE_API_KEY}"
    api_secret: "${BINANCE_LIVE_API_SECRET}"

account_observer:
  # Параметри моніторингу
  poll_interval: 5          # Опитування кожні 5 секунд
  trade_limit: 50          # Останні 50 трейдів за запит

  # Список символів для моніторингу
  symbols:
    - BTCUSDT
    - ETHUSDT
    - BNBUSDT
    - ADAUSDT
    - SOLUSDT
    - DOTUSDT
    - LINKUSDT
    - LTCUSDT
```

## Тестова конфігурація (testnet.yaml)

```yaml
trading_mode: testnet

binance_api:
  testnet:
    api_key: "${BINANCE_TESTNET_API_KEY}"
    api_secret: "${BINANCE_TESTNET_API_SECRET}"

account_observer:
  poll_interval: 10
  trade_limit: 20
  symbols:
    - BTCUSDT
    - ETHUSDT
```

## Мінімальна конфігурація (minimal.yaml)

```yaml
trading_mode: live

binance_api:
  live:
    api_key: "your_api_key"
    api_secret: "your_api_secret"

account_observer:
  symbols: [BTCUSDT]
```

## Конфігурація з фільтрами (filtered.yaml)

```yaml
trading_mode: live

binance_api:
  live:
    api_key: "${BINANCE_LIVE_API_KEY}"
    api_secret: "${BINANCE_LIVE_API_SECRET}"

account_observer:
  poll_interval: 3
  trade_limit: 100

  # Розширений список символів
  symbols:
    - BTCUSDT
    - ETHUSDT
    - BNBUSDT
    - ADAUSDT
    - SOLUSDT
    - MATICUSDT
    - AVAXUSDT
    - LINKUSDT
    - DOTUSDT
    - ALGOUSDT
```

## Конфігурація для високої частоти (high_frequency.yaml)

```yaml
trading_mode: live

binance_api:
  live:
    api_key: "${BINANCE_LIVE_API_KEY}"
    api_secret: "${BINANCE_LIVE_API_SECRET}"

account_observer:
  poll_interval: 1           # Агресивне опитування
  trade_limit: 100          # Більше трейдів за раз

  # Обмежений набір високоліквідних символів
  symbols:
    - BTCUSDT
    - ETHUSDT
```

## Конфігурація для debugging (debug.yaml)

```yaml
trading_mode: testnet

binance_api:
  testnet:
    api_key: "${BINANCE_TESTNET_API_KEY}"
    api_secret: "${BINANCE_TESTNET_API_SECRET}"

account_observer:
  poll_interval: 30          # Повільне опитування для аналізу
  trade_limit: 10           # Мінімум трейдів

  symbols:
    - BTCUSDT
```</content>
<parameter name="filePath">c:\Users\job11\Music\Olimp_v1\docs\domains\account_observer\examples\config_examples.yaml