# Домен Account Observer (Спо� терігач Рахунку)

## Загальна інформація

**Ідентифікатор домену:** `account_observer`  
**Роль в � и� темі:** Моніторинг виконаних угод та емі� ія подій про трейди

## Архітектурна роль

Домен `account_observer` виконує функцію па� ивного моніторингу торгової активно� ті на біржі. Він періодично опитує API біржі для отримання інформації про о� танні виконані угоди та перетворює їх в � тандартизовані події FSM � и� теми.

### Відповідальні� ть
- Моніторинг нових трейдів через read-only API
- Від� теження ID трейдів для уникнення дублікатів
- Перетворення � ирих даних трейдів в � труктуровані події
- Забезпечення � и� теми актуальною інформацією про виконані операції

## Структура домену

### О� новні компоненти

#### AccountObserver
Головний кла�  домену, що реалізує моніторинг трейдів.

**Ініціалізація:**
- Налаштування Binance Client на о� нові `trading_mode`
- Вибір API ключів залежно від режиму роботи
- Налаштування � пи� ку � имволів для моніторингу
- Конфігурація інтервалу опитування

**Методи життєвого циклу:**
- `start()` - запу� к фонового опитування
- `stop()` - зупинка моніторингу

### Внутрішня архітектура

#### Цикл опитування
```
_poll_loop() -> _poll_trades()
    ├── Опитування кожного � имволу з конфігурації
    ├── Отримання о� танніх трейдів (get_my_trades)
    ├── Фільтрація нових трейдів
    └── Емі� ія подій EVT:TRADE_EXECUTED
```

#### Управління дублікатами
- Від� теження оброблених trade_id в Set
- Пропу� к вже оброблених трейдів
- Автоматичне очищення при перезапу� ку

## FSM події

### Генеровані події

#### EVT:TRADE_EXECUTED
**Ча� тота:** При виявленні нових трейдів (зазвичай кожні 5 � екунд опитування)  
**Направлення:** Position Tracking, Risk Management, Audit Trail  

**Payload � труктура:**
```json
{
  "symbol": "BTCUSDT",
  "side": "buy",
  "price": "50000.00",
  "quantity": "0.001",
  "ts": 1640995200000,
  "fees": "0.0001",
  "venue": "binance"
}
```

**Опи� :** Повідомляє � и� тему про виконання нової торгової операції.

### Споживані події
Домен не � поживає зовнішні події - працює автономно.

## Взаємодія з іншими доменами

### Синхронні зв'язки

#### Position Tracking
- **Вхід:** EVT:TRADE_EXECUTED
- **Викори� тання:** Оновлення � тану позицій на о� нові реальних трейдів
- **Ча� тота:** Подієво (при нових трейдах)

#### Risk Management
- **Вхід:** EVT:TRADE_EXECUTED
- **Викори� тання:** Перерахунок ризиків пі� ля виконання операцій
- **Ча� тота:** Подієво (при нових трейдах)

#### Audit Trail
- **Вхід:** EVT:TRADE_EXECUTED
- **Викори� тання:** Запи�  операцій в аудит лог
- **Ча� тота:** Подієво (при в� іх трейдах)

### А� инхронні залежно� ті
Домен працює незалежно, але його дані критичні для коректного від� теження позицій.

## Конфігурація

### О� новні параметри
```yaml
trading_mode: "hybrid_live_data_testnet_exec"

binance_api:
  testnet:
    api_key: "${BINANCE_TESTNET_API_KEY}"
    api_secret: "${BINANCE_TESTNET_API_SECRET}"

account_observer:
  poll_interval: 5          # Інтервал опитування в � екундах
  symbols:                  # Спи� ок � имволів для моніторингу
    - BTCUSDT
    - ETHUSDT
    - BNBUSDT
  trade_limit: 50          # Кількі� ть о� танніх трейдів для перевірки
```

### Режими роботи
- **live:** Моніторинг бойових трейдів
- **testnet:** Моніторинг те� тових трейдів
- **hybrid:** Автоматичний вибір залежно від конфігурації

## Моніторинг та діагно� тика

### Логування
- **Інформаційні повідомлення:** Виявлення нових трейдів, емі� ія подій
- **Debug повідомлення:** Деталі опитування кожного � имволу
- **Помилки:** Проблеми з API, мережеві помилки

### Метрики
- Кількі� ть оброблених трейдів
- Ча� тота виявлення нових трейдів
- Середній ча�  між опитуваннями

## Обробка помилок

### Стратегії відновлення
1. **API помилки:** Продовження опитування інших � имволів
2. **Мережеві проблеми:** Повторні � проби при на� тупному циклі
3. **Пар� инг помилки:** Логування та пропу� к проблемних трейдів

### Graceful degradation
При неможливо� ті опитування окремих � имволів продовжує моніторинг інших.

## Те� тування

### Інтеграційні те� ти
- Валідація коректно� ті тран� формації трейдів в події
- Перевірка фільтрації дублікатів
- Те� тування роботи в різних режимах

### Модульні те� ти
- Перевірка пар� ингу трейдів Binance
- Валідація формату payload
- Те� тування логіки дедуплікації

## Безпека

### Read-only до� туп
- Викори� тання тільки read-only API ключів
- Неможливі� ть виконання торгових операцій
- Мінімальні дозволи для моніторингу

### Аудит
В� і виявлені трейди логують� я для аудиту та debugging.