# Реалізовані функції домену Account Balance

## Огляд

Домен `account_balance` реалізує кла�  `AccountConnector` з набором методів для моніторингу � тану рахунку Binance Futures. В� і методи викори� товують а� инхронне програмування та обробку помилок для забезпечення надійно� ті.

## О� новний кла� : AccountConnector

### Кон� труктор та ініціалізація

#### `__init__(fsm: FSMCore, config: Dict[str, Any]) -> None`
**Опи� :** Ініціалізує з'єднання з Binance API через BinanceAdapter.

**Параметри:**
- `fsm`: Екземпляр FSM core для емі� ії подій
- `config`: Конфігураційний � ловник з API credentials та налаштуваннями

**Логіка ініціалізації:**
1. Збереження по� илань на FSM та конфігурацію
2. Отримання інтервалу опитування з `account_observer.poll_interval`
3. Вибір API конфігурації залежно від `trading_mode`:
   - `live`: Викори� тання бойових API ключів
   - `testnet`/`hybrid`: Викори� тання те� тових API ключів
4. Валідація наявно� ті в� іх необхідних API параметрів
5. Створення екземпляра `BinanceAdapter`

**Викидає:** `ValueError` при неповній API конфігурації

### Управління життєвим циклом

#### `start() -> None`
**Опи� :** Запу� кає моніторинг рахунку у фоновому потоці.

**Логіка:**
1. Перевірка, чи вже запущено
2. В� тановлення прапора `running = True`
3. Створення daemon потоку з методом `_monitor_loop`
4. Запу� к потоку
5. Логування у� пішного запу� ку

#### `stop() -> None`
**Опи� :** Зупиняє моніторинг та закриває з'єднання.

**Логіка:**
1. В� тановлення `running = False`
2. Очікування завершення потоку (thread.join())
3. Закриття HTTP � е� ії адаптера
4. Логування зупинки

### О� новний цикл моніторингу

#### `_monitor_loop() -> None`
**Опи� :** Головний цикл моніторингу, що працює у фоновому потоці.

**Логіка:**
1. Створення нового event loop для а� инхронних операцій
2. Цикл поки `running == True`:
   - Виклик `_fetch_and_emit_account_data()`
   - Очікування `update_interval` � екунд
3. Закриття event loop при завершенні

**Обробка помилок:** Логування завершення циклу

### Отримання та обробка даних

#### `_fetch_and_emit_account_data() -> None`
**Опи� :** А� инхронно отримує дані з Binance API та емітує FSM події.

**Логіка для балан� у:**
1. Виклик `adapter.get_account_balance()`
2. Валідація типу відповіді (має бути list)
3. Збереження даних у `_latest_balance_data`
4. Детальне логування USDT балан� у
5. Виклик `_emit_balance_update()`

**Логіка для позицій:**
1. Виклик `adapter.get_open_positions()`
2. Валідація типу відповіді (має бути list)
3. Виклик `_emit_positions_update()`

**Обробка помилок:** Окрема обробка для балан� у та позицій з детальним логуванням

### Емі� ія FSM подій

#### `_emit_balance_update(balance_data: List[Dict[str, Any]]) -> None`
**Опи� :** Емітує подію `EVT:BALANCE_UPDATE_RECEIVED` з даними балан� у.

**Тран� формація даних:**
1. Фільтрація активів з `balance > 0`
2. Конвертація чи� лових значень у `Decimal` для точно� ті
3. Створення � пи� ку assets з полями:
   - `asset`: назва активу
   - `balance`: загальний балан� 
   - `crossUnPnl`: нереалізований P&L
   - `crossWalletBalance`: балан�  cross margin
   - `updateTime`: ча�  оновлення

**Payload � труктури:**
```python
{
    'assets': [...],  # � пи� ок активів
    'updateTime': int  # мак� имальний updateTime з активів
}
```

**Емі� ія події:**
- Event name: `"EVT:BALANCE_UPDATE_RECEIVED"`
- Why: `"Balance data updated from Binance API."`
- Логування кілько� ті активів з балан� ом > 0

#### `_emit_positions_update(positions_data: List[Dict[str, Any]]) -> None`
**Опи� :** Емітує подію `EVT:ACCOUNT_UPDATE_RECEIVED` з даними позицій та загального � тану.

**Тран� формація позицій:**
1. Фільтрація позицій з `positionAmt != 0`
2. Конвертація у Decimal для точно� ті
3. Створення � пи� ку відкритих позицій з полями:
   - `symbol`: торгова пара
   - `positionAmt`: розмір позиції
   - `entryPrice`: ціна входу
   - `unRealizedProfit`: нереалізований прибуток
   - `leverage`: плече
   - `marginType`: тип маржі
   - `markPrice`: позначена ціна
   - `liquidationPrice`: ціна ліквідації

**Розрахунок загального � тану:**
1. Викори� тання `_latest_balance_data` для USDT активу
2. Отримання `totalWalletBalance` з поля `balance`
3. Отримання `totalUnrealizedProfit` з поля `crossUnPnl`
4. Отримання `totalCrossWalletBalance` з відповідного поля

**Payload � труктури:**
```python
{
    'totalWalletBalance': str,
    'totalUnrealizedProfit': str,
    'totalCrossWalletBalance': str,
    'positions': [...],  # � пи� ок відкритих позицій
    'updateTime': int    # поточний timestamp в ms
}
```

**Емі� ія події:**
- Event name: `"EVT:ACCOUNT_UPDATE_RECEIVED"`
- Why: `"Open positions data updated from Binance API."`
- Логування кілько� ті відкритих позицій та wallet balance

## Внутрішні атрибути кла� у

### Конфігураційні атрибути
- `fsm`: По� илання на FSM core
- `config`: Конфігураційний � ловник
- `update_interval`: Інтервал опитування в � екундах (за замовчуванням 30)

### Стан виконання
- `thread`: Фоновий потік виконання
- `running`: Прапор активно� ті моніторингу
- `adapter`: Екземпляр BinanceAdapter

### Кешовані дані
- `_latest_balance_data`: О� танні дані балан� у з API `/fapi/v2/balance`

## Викори� товувані зовнішні залежно� ті

### vfoundation.adapters.binance_adapter.BinanceAdapter
**Методи:**
- `get_account_balance()`: Отримання балан� у активів
- `get_open_positions()`: Отримання відкритих позицій
- `close_session()`: Закриття HTTP � е� ії

### vfoundation.core.FSMCore
**Методи:**
- `emit(event_name, payload, why)`: Емі� ія FSM події

## Обробка помилок

### Стратегії
1. **API помилки:** Логування з `exc_info=True` для детального traceback
2. **Невалідні дані:** Перевірка типів та пропу� к некоректних запи� ів
3. **Мережеві проблеми:** Graceful degradation з о� танніми відомими даними

### Логування
- **INFO:** У� пішні операції, кількі� ть даних
- **WARNING:** Від� утні� ть USDT, проблеми з даними
- **ERROR:** Критичні помилки з повним traceback

## Продуктивні� ть

### Оптимізації
- **Фільтрація:** Тільки активи з балан� ом > 0 та позиції з amount != 0
- **Кешування:** Збереження балан� у для викори� тання в позиціях
- **А� инхронні� ть:** Неблокуючі API виклики
- **Background execution:** Окремий потік не блокує о� новний проце� 

### Метрики викори� тання
- **CPU:** Мінімальне навантаження (тільки періодичні API виклики)
- **Memory:** Фік� ований розмір для кешування о� танніх даних
- **Network:** 2 API виклики кожні 30 � екунд
- **Storage:** Не викори� товує по� тійне � ховище

## Те� тування

### Модульні те� ти
- **Ініціалізація:** Валідація конфігурації та � творення адаптера
- **API моки:** Симуляція відповідей Binance API
- **Тран� формація:** Перевірка конвертації даних у події
- **Емі� ія:** Валідація � труктури payload подій

### Інтеграційні те� ти
- **FSM інтеграція:** Перевірка емі� ії та � поживання подій
- **API інтеграція:** Те� тування з реальними credentials (testnet)
- **Error handling:** Симуляція мережевих помилок та API failures

## Безпека

### Захи� т облікових даних
- API ключі передають� я тільки через кон� труктор
- Не логують� я у звичайному режимі
- Викори� тання HTTPS через BinanceAdapter

### Аудит
- В� і API виклики логують� я без sensitive data
- Детальне логування фінан� ових операцій
- Timestamp в� іх операцій для аудиту

## Розширення функціонально� ті

### Можливі додавання
1. **WebSocket підтримка:** Перехід з REST на real-time updates
2. **Multi-asset підтримка:** Моніторинг додаткових активів
3. **Historical data:** Збереження і� торії балан� у
4. **Alerting:** Повідомлення про критичні зміни балан� у

### Конфігураційні розширення
- Налаштування фільтрів активів/позицій
- Кastomні інтервали опитування
- Додаткові метрики моніторингу
- Інтеграція з різними режимами торгівлі</content>
<parameter name="filePath">c:\Users\job11\Music\Olimp_v1\docs\domains\account_balance\functions.md