# =============================================================================
# OVERLAY: patched7 candidate — March 2024 window only
# =============================================================================
# Purpose:
# - isolate March 2024, where ETHUSDT x TREND_DOWN produced the main residual loss
# - use only after the minimal patched7 code path from reports/patched7_minimal_patch_proposal.md
#   is implemented and validated
#
# Note:
# - in this repo the single-backtest helper also accepts --start / --end CLI args
# - keep those CLI dates aligned with this overlay
# =============================================================================

trading:
  backtest:
    start_date: "2024-03-01"
    end_date:   "2024-03-31"
    initial_balance: 1000.0