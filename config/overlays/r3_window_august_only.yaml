# =============================================================================
# OVERLAY: R3 Backtest Window — August-only (2023-08-01..2023-08-31)
# =============================================================================
# Purpose   : Isolated August window — tests B3 generalization on unseen month.
# NOTE      : This file is NOT auto-loaded by ConfigLoader.
#             Apply via: python scripts/diagnostics/run_single_backtest.py
#                          --side B --start 2023-08-01 --end 2023-08-31
#             No predefined --rung for August-only; use --start/--end directly.
# =============================================================================
# Config used : config/aurora/  (B3 / patched3 — accepted baseline)
# Symbols     : BTCUSDT, ETHUSDT, BNBUSDT, DOGEUSDT, 1000PEPEUSDT
# Parquet     : all 5 symbols OK for 2023-08
# =============================================================================
# WHY August-only?
#   R2 (Jun–Jul) already validated B3 is not blown.
#   Aug-only isolates generalization: does B3 hold on a fresh month
#   it has never been tuned on?
#   Faster than cumulative (1 month of bars vs 3 months).
# =============================================================================

# Mirror of trading.yaml schema (minimal excerpt):
trading:
  backtest:
    start_date: "2023-08-01"    # inclusive
    end_date:   "2023-08-31"    # inclusive — last day of August 2023
    initial_balance: 1000.0

# Note: August 2023 market context
#   BTC: sideways-to-down month (approx 29k→26k range)
#   High volatility events: mid-Aug sharp sell-off
#   Good stress test for TREND_DOWN handling post-patch
