# =============================================================================
# OVERLAY: R2 Backtest Window
# =============================================================================
# Purpose  : Pin trading.backtest dates for Rung 2 (2023-06-01..2023-07-31).
# Usage    : run_single_backtest.py --rung 2 applies these dates automatically.
#            For manual override, set in trading.yaml or pass --start / --end.
# NOTE     : This file is NOT auto-loaded by ConfigLoader.
#            It is a reference document and source of truth for R2 window dates.
#            Apply via: python scripts/diagnostics/run_single_backtest.py --rung 2
# =============================================================================

# Mirror of trading.yaml schema (minimal excerpt):
trading:
  backtest:
    start_date: "2023-06-01"    # inclusive
    end_date: "2023-07-31"      # inclusive (last day of July 2023)
    initial_balance: 1000.0

# Symbols active in R2 (must have parquet in data/processed/<SYM>/5m/):
# - BTCUSDT
# - ETHUSDT
# - BNBUSDT
# - DOGEUSDT
# - 1000PEPEUSDT
# (SOLUSDT / XRPUSDT skipped — no parquet files in R2 window)
