# =============================================================================
# OVERLAY: R3 Backtest Window — Cumulative (2023-06-01..2023-08-31)
# =============================================================================
# Purpose   : Pin trading.backtest dates for Rung 3 cumulative window.
# Runner    : run_single_backtest.py applies these dates automatically via --rung 3
# NOTE      : This file is NOT auto-loaded by ConfigLoader.
#             It is a reference document / source of truth for R3 cumulative dates.
#             Apply via: python scripts/diagnostics/run_single_backtest.py --side B --rung 3
# =============================================================================
# Config used : config/aurora/  (B3 / patched3 — accepted baseline)
# Symbols     : BTCUSDT, ETHUSDT, BNBUSDT, DOGEUSDT, 1000PEPEUSDT
# Parquet     : all 5 symbols OK for 2023-06, 2023-07, 2023-08
# =============================================================================
# Rung 3 is pre-defined in LADDER_RUNGS inside run_single_backtest.py:
#   (3, 2023, 6, 2023, 8)  →  start=2023-06-01, end=2023-08-31
# =============================================================================

# Mirror of trading.yaml schema (minimal excerpt):
trading:
  backtest:
    start_date: "2023-06-01"    # inclusive — same start as R1/R2 (cumulative ladder)
    end_date:   "2023-08-31"    # inclusive — last day of August 2023
    initial_balance: 1000.0

# Context: R2 results (B3 accepted baseline)
# A (aurora_baseline): total_pnl -631.74, DD 66.52%
# B3 (patched3):       total_pnl -171.37, DD 42.91%  ← accepted baseline
#
# Watchlist for R3:
#   - BNBUSDT × MEAN_REVERSION: B3 showed -69.31 USDT (n=16, ev/t=-4.33) — monitor direction
#   - BTC: essentially flat delta (-3.76) across R2 — watch for regime mix shift in Aug
