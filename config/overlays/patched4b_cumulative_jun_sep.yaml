# =============================================================================
# OVERLAY: patched4b — Cumulative Jun-Sep window
# =============================================================================
# Experiment  : patched4b cumulative validation
# Baseline    : patched4b (BTC blocked + PEPE blocked + SMA 48/192)
# Purpose     : Confirm Sep improvement holds across full Jun-Sep window
# =============================================================================
#
# NOTE: Dates passed via --start / --end CLI args, not loaded from config at runtime.
# This file documents the window only.
#
# Config state at time of this run (patched4b):
#   BTCUSDT.allowed_regimes      : []  (blocked)
#   BTCUSDT in symbols_to_track  : False
#   1000PEPEUSDT.allowed_regimes : []  (blocked)
#   1000PEPEUSDT in symbols_to_track : False
#   ETHUSDT.allowed_regimes      : [TREND_DOWN, FLAT_LOW, FLAT_NORMAL, LOW_VOLATILITY, MEAN_REVERSION]
#   BNBUSDT.allowed_regimes      : [LOW_VOLATILITY, FLAT_NORMAL, MEAN_REVERSION]
#   SMA short/long               : 48 / 192
#
# =============================================================================
# PRIOR RESULTS (for comparison)
# =============================================================================
# patched4b Sep-only  (20260307_214809): +60.02 USDT, roi=+6.0%, DD=17.3%, WR=85%, sharpe=+0.46
# patched3  R4_CUM    (backtest_20260306_100245): -262.64 USDT, BTC=-212, ETH=+16, BNB=-66
# =============================================================================

trading:
  backtest:
    start_date: "2023-06-01"
    end_date:   "2023-09-30"
    initial_balance: 1000.0
